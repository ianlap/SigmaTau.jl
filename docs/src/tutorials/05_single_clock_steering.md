# Tutorial 5: Single-Clock Steering

Walk through `examples/clock_steering.jl`: build a `ThreeStateClock`, run
a Kalman filter, steer the clock with a PID controller.

Narrative fills in a follow-up PR.
