var documenterSearchIndex = {"docs":
[{"category":"section","location":"#SigmaTau.jl","page":"Home","text":"A Julia package for time-and-frequency stability analysis: Allan / Modified Allan / Hadamard / Total deviations, χ²-based confidence intervals, lag-1 ACF noise identification, MTIE, parabolic deviation, and calibrated power-law noise generation.","title":"SigmaTau.jl"},{"category":"section","location":"#Package-layout","page":"Home","text":"SigmaTau.jl is a single, flat Julia 1.11 package. Shared types, file IO, deviation kernels, noise identification, and EDF/CI all live under one namespace:\n\nSurface Responsibility\nShared types PhaseData, FrequencyData, StabilityResult.\nStability API Deviation kernels, EDF / CI, noise identification, MTIE, PDEV, noise_gen.","title":"Package layout"},{"category":"section","location":"#Where-to-next","page":"Home","text":"First time? See Getting Started.\nComing from Stable32? See Migrating from Stable32.\nComing from allantools? See Migrating from allantools.\nNeed theory? Start with Theory: Overview.\nWant a worked example? Pick a Tutorial.\nMaking figures? See Plotting.\nLooking up an API? Browse the API Reference.\nVerifying numerics? See Validation.\nSomething not working? Check the FAQ.","title":"Where to next"},{"category":"section","location":"#Reference-math","page":"Home","text":"Numerical conventions and confidence-interval formulas follow:\n\nNIST SP1065 Riley & Howe 2008\nGreenhall–Riley 2003 EDF approximations Greenhall 2003\nIEEE Std 1139-2022","title":"Reference math"},{"category":"section","location":"theory/pdev_confidence/#Theory:-PDEV-(PVAR)-confidence-intervals","page":"Theory: PDEV (PVAR) confidence intervals","text":"pdev computes the parabolic deviation σ_PDEV(τ), the square root of the parabolic variance PVAR — a wavelet variance built from a least-squares straight-line (equivalently, parabolic-weight) fit of the phase over each analysis window Vernotte–Lenczner–Bourgeois–Rubiola 2016. Like every other SigmaTau deviation, when ci=true it reports a per-τ noise type, an equivalent number of degrees of freedom (EDF) ν, and a χ²-based confidence interval. This page documents the EDF model behind that interval.\n\nUnlike MHTOTDEV (see \"MHTOTDEV bias and EDF\"), PVAR's EDF is published, so SigmaTau reproduces the literature model rather than measuring its own.","title":"Theory: PDEV (PVAR) confidence intervals"},{"category":"section","location":"theory/pdev_confidence/#χ-confidence-interval","page":"Theory: PDEV (PVAR) confidence intervals","text":"A variance estimator that is k·χ²ν-distributed satisfies\n\nnu = frac2E^2hatsigma^2(tau)Vhatsigma^2(tau)\n\n(Vernotte–Chen–Rubiola 2020 Eq. 17; the same estimator SigmaTau uses for every family). Given ν, the two-sided interval at confidence p on the variance is [ν/χ²_{hi}, ν/χ²_{lo}]·\\hat\\sigma^2, and the deviation bounds are the square roots — exactly the confidence_intervals path shared with ADEV/HDEV/etc.","title":"χ² confidence interval"},{"category":"section","location":"theory/pdev_confidence/#The-EDF-model","page":"Theory: PDEV (PVAR) confidence intervals","text":"For white PM noise the EDF has a closed form (Vernotte et al. 2016, Eq. 24). The 2020 paper generalizes it to all five power-law noises as\n\nnu approx frac35A(alpha)(mM) - B(mM)^2\nqquad M = N - 2m qquad B = 12\n\n(Vernotte–Chen–Rubiola 2020, Eqs. 16, 18), where m = τ/τ0 is the averaging factor, N the number of phase samples, and M = N − 2m the number of PVAR estimates (the same M used in the kernel). The amplitude coefficient is the cubic\n\nA(alpha) = 27 + tfrac14alpha + tfrac514alpha^2 - tfrac34alpha^3 \n\ndetermined from massive Monte Carlo with A(+2) = 23 fixed by the white-PM closed form. Evaluated at the five integer noise types it gives\n\nα (noise) +2 (WHPM) +1 (FLPM) 0 (WHFM) −1 (FLFM) −2 (RWFM)\nA(α) 23 27 27 28 34\n\nSigmaTau stores these integer-α values (_pvar_A), since noise identification only yields integer α.","title":"The EDF model"},{"category":"section","location":"theory/pdev_confidence/#Endpoints","page":"Theory: PDEV (PVAR) confidence intervals","text":"Two regimes need separate handling:\n\nm = 1. PVAR(τ0) ≡ AVAR(τ0): the kernel _pdev_core returns overlapping ADEV at m = 1, so the EDF uses ADEV's Greenhall–Riley value (_calc_edf_core with d = 2). This is the exact result for the identity and sidesteps the paper's note that the closed-form approximation is least accurate at m = 1, 2. (At m = 2 no exact fallback exists, so the formula is used as published; it reads somewhat high there.)\nLarge τ (m ≳ N/4). The Eq. (16) approximation degrades beyond m ≈ N/4. The paper fills N/4 < m ≤ N/2 with a semi-logarithmic interpolation (Vernotte–Chen–Rubiola 2020, Eqs. 22–23)\nnu(m) = aln m + b qquad\na = fracnu(m_1) - 1ln m_1 - ln m_2\nanchored at the Eq.-16 value at m_1 = \\mathrm{round}(1.11\\,N/4) and at ν = 1 (a single estimate) at m_2 = \\mathrm{round}(0.901\\,N/2) \\approx N/2. SigmaTau holds ν = 1 for any m ≥ m_2 rather than extrapolating below one.","title":"Endpoints"},{"category":"section","location":"theory/pdev_confidence/#Accuracy-and-scope","page":"Theory: PDEV (PVAR) confidence intervals","text":"The model reproduces the paper's Monte Carlo EDF table (Table III, N = 2048) to within the stated ≈ ±10 % across the noise types — e.g. white PM at m = 64/128/256 gives 46.5/22.1/10.0 against the tabulated 46.9/22.0/10.0. The in-suite tripwire test/stab/pdev_edf_mc.jl re-measures ν = 2E[V]²/Var[V] from a small fresh Monte Carlo and checks it against _pvar_edf at a few cells.\n\nPVAR is an unbiased wavelet variance, so — unlike the total family — no bias correction is applied to pdev.","title":"Accuracy and scope"},{"category":"section","location":"tutorials/02_compute_adev/#Computing-the-Allan-deviation","page":"Computing the Allan deviation","text":"adev returns a fully-populated StabilityResult: the σ_y(τ) point estimates, a power-law-noise classification per τ, the equivalent number of degrees of freedom, and χ² confidence bounds — all in one call. There's no separate \"compute the noise type\" step or \"compute the CI\" step; they're part of the default output.\n\nThis tutorial:\n\nRuns adev on a compact two-noise record (WPM + RWFM — the two ends of the slope table) with the default octave τ-grid (no m_values argument needed).\nRepeats the call with an explicit m_values to show how to take control of which τ values get sampled.\nWalks through every field of the returned StabilityResult.\nRenders σ_y(τ) on log-log axes with χ² error bars via the bundled RecipesBase extension.\n\nusing SigmaTau\nusing Random","title":"Computing the Allan deviation"},{"category":"section","location":"tutorials/02_compute_adev/#Fixture","page":"Computing the Allan deviation","text":"A deliberately small two-noise record: white PM falling as τ⁻¹ and random-walk FM rising as τ^(+1/2), so the curve has a clean knee. For a realistic three-noise clock record, see Phase data and frequency data.\n\nRandom.seed!(20260509)\n\nτ₀ = 1.0\nN  = 4096\nwpm  = randn(N) .* 1e-9\nrwfm = cumsum(cumsum(randn(N) .* 1e-12))\nx    = wpm .+ rwfm\npd   = PhaseData(x, τ₀)","title":"Fixture"},{"category":"section","location":"tutorials/02_compute_adev/#Run-adev-with-no-m_values-argument","page":"Computing the Allan deviation","text":"The shortest path: pass the data and nothing else. adev defaults to an octave-spaced τ-grid, m = 1, 2, 4, 8, …, up to the largest averaging factor the ADEV kernel can compute on a record of this length — m_max = ⌊(N−1)/2⌋ for ADEV, smaller for MDEV / HDEV / MHDEV; see SigmaTau._default_m_values for the per-kernel table. This matches the convention in NIST SP1065 and is the right default for a first look at a clock record.\n\nresult = adev(pd)","title":"Run adev with no m_values argument"},{"category":"section","location":"tutorials/02_compute_adev/#What's-in-the-result?","page":"Computing the Allan deviation","text":"Every relevant quantity is on the returned struct — there's no follow-up call needed for noise classification, EDF, or confidence bounds.\n\nresult.tau          # τ values (s) on the default octave grid\n\nresult.dev          # σ_y(τ) point estimates\n\nresult.noise_type   # power-law classification per τ\n\n(:WHPM, :FLPM, :WHFM, :FLFM, :RWFM)\n\nresult.edf          # equivalent degrees of freedom\n\n(Greenhall–Riley formula, used by the χ² CI)\n\nresult.ci           # χ² confidence bounds (default 68.3%, 1σ) — one\n\n(lo, hi) named tuple per τ: result.ci[1].lo\n\nci_lower(result)    # the bounds as plain vectors, via the exported\n\nci_lower / ci_upper accessor functions\n\nresult.neff         # number of analysis windows averaged per τ\n\n(the Stable32 \"#\" column; N − 2m for adev)\n\nresult.deviation_type  # which deviation kernel produced this result\n\nSkipping CI computation (when you just want the point estimates) is a single keyword:\n\nadev(pd; ci = false)\n\nresult.ci and result.edf come back empty in that mode; result.neff is always populated.","title":"What's in the result?"},{"category":"section","location":"tutorials/02_compute_adev/#Run-adev-with-an-explicit-m_values","page":"Computing the Allan deviation","text":"When you need a specific τ-grid — e.g. matching a reference table, or probing one particular τ — pass it as a Vector{Int} of averaging factors. The package multiplies by τ₀ internally to produce result.tau. Anything from a single point ([10]) to a custom decade grid works:\n\nm_grid    = [1, 2, 4, 8, 16, 32, 64, 128, 256, 512]\nresult_m  = adev(pd, m_grid)\n\nSame fields, same physics — only the sampled τ values differ.\n\nresult_m.tau ≈ m_grid .* τ₀\n\nadev accepts the same kwargs in both forms:\n\nadev(pd; ci = false)                 # default grid, no CI\nadev(pd, [10, 100]; confidence = 0.95)    # explicit grid, 95 % CI\nadev(fd)                                  # FrequencyData entry point","title":"Run adev with an explicit m_values"},{"category":"section","location":"tutorials/02_compute_adev/#The-plot-recipe","page":"Computing the Allan deviation","text":"SigmaTau ships a RecipesBase extension (ext/SigmaTauRecipesBaseExt.jl); loading any Plots-compatible backend brings in a log-log τ–σ recipe with χ² error bars sourced from result.ci.\n\nusing Plots\n\nplot(result;\n     title  = raw\"Overlapping Allan deviation (WPM + RWFM mix)\",\n     xlabel = raw\"Averaging time \\(\\tau\\) (s)\",\n     ylabel = raw\"\\(\\sigma_y(\\tau)\\)\",\n     legend = :topright,\n     lw     = 1.5)","title":"The plot recipe"},{"category":"section","location":"tutorials/02_compute_adev/#Reading-the-curve","page":"Computing the Allan deviation","text":"The slope of σy(τ) on log-log axes encodes the dominant noise type. At short τ the WPM contribution dominates and the curve falls as τ⁻¹; past a knee, the RWFM contribution takes over and rises as τ^(1/2). The `result.noisetypevector tracks this regime change automatically; in the test suite,test/stab/runtests.jl(Stable32 cross-validation`) verifies these σ_y values match Stable32's reference output to four-five significant figures across the same fixture.","title":"Reading the curve"},{"category":"section","location":"theory/noise_id/#Theory:-Noise-Identification","page":"Theory: Noise Identification","text":"Each averaging-factor m of an estimator may correspond to a different dominant noise type. The EDF formula and the χ² CI both depend on the α exponent of the local power-law process, so SigmaTau classifies the noise per τ before assigning a confidence interval.","title":"Theory: Noise Identification"},{"category":"section","location":"theory/noise_id/#Why-classify?","page":"Theory: Noise Identification","text":"The Greenhall–Riley EDF formulas (see Confidence) parameterize on α explicitly. Pretending the noise is white when it is flicker yields an overconfident CI; pretending it is random-walk when it is white yields an underconfident one. Classification matters quantitatively, not just descriptively.\n\nThe five power-law families SigmaTau recognises are white phase modulation (WHPM, α=2), flicker phase modulation (FLPM, α=1), white frequency modulation (WHFM, α=0), flicker frequency modulation (FLFM, α=−1), and random-walk frequency modulation (RWFM, α=−2). The α exponent feeds straight into the EDF and bias-correction tables.","title":"Why classify?"},{"category":"section","location":"theory/noise_id/#Lag-1-ACF-on-differenced-phase","page":"Theory: Noise Identification","text":"Riley & Greenhall 2004 (Riley and Greenhall, Apr 2004) show that the lag-1 autocorrelation r₁ of an appropriately differenced phase series maps monotonically onto the spectral exponent α. Their procedure:\n\nDetrend (quadratic) and difference the input phase m times.\nCompute r₁ on the result.\nMap r₁ to α via the published thresholds.\n\nFor r₁ near 0.5 the noise is white; near zero, flicker; near −0.5, random-walk. The α-to-r₁ mapping is implemented in src/noise.jl.\n\nidentify_noise(x, [1, 2, 4, 8]; dmin=0, dmax=2)\n\nThe first argument is the raw phase vector (a Vector{Float64}), not a PhaseData record; m_values is a vector of averaging factors (τ = m·τ₀). The result is a Vector{Symbol}, one entry per m: each entry is one of :WHPM, :FLPM, :WHFM, :FLFM, :RWFM.\n\nInternally the kernel iterates the difference operator: at each step it recomputes r₁, converts to ρ = r₁ / (1 + r₁), and stops once ρ falls below 0.25 (or dmax differences have been taken). The terminal difference order d plus ρ recovers α via α = −2(ρ + d) + 2. The iterative criterion is what makes the method robust at boundaries between adjacent power-law families.","title":"Lag-1 ACF on differenced phase"},{"category":"section","location":"theory/noise_id/#B1/R(n)-fallback","page":"Theory: Noise Identification","text":"For short records or borderline cases, SigmaTau falls back to the Allan-variance B1 ratio with the R(n) factor for WPM/FPM disambiguation, per SP1065 §6 (Riley, Jul 2008). This path is triggered automatically when the lag-1 method's confidence is low.\n\nB1 compares the classical (standard) variance of the averaged frequency to the Allan variance and discriminates RWFM/FLFM/WHFM/FPM boundaries. When B1 lands in the WPM/FPM region, an additional R(n) test — the ratio of MDEV² to ADEV² — separates the two phase-modulated families, since MDEV's extra phase-averaging window collapses WHPM faster than FLPM.","title":"B1/R(n) fallback"},{"category":"section","location":"theory/noise_id/#NEFF_RELIABLE-30","page":"Theory: Noise Identification","text":"The lag-1 method needs a minimum effective sample count for r₁ to be a reliable estimator. SigmaTau uses NEFF_RELIABLE = 30 as the threshold; below this, classification falls back to B1/R(n) regardless of r₁. The threshold tracks the Riley R 2020 recommendation (Riley, Jun 2020) and the historical SigmaTau policy mandate.","title":"NEFF_RELIABLE = 30"},{"category":"section","location":"theory/noise_id/#The-noise_type-field","page":"Theory: Noise Identification","text":"adev, mdev, etc. populate StabilityResult.noise_type with one classification per τ. Empty when classification was not requested or not possible. When a particular m fails (e.g. degenerate variance, too few points after differencing), the kernel carries forward the last reliable classification rather than emitting :unknown mid-run.","title":"The noise_type field"},{"category":"section","location":"theory/noise_id/#Implementation-notes","page":"Theory: Noise Identification","text":"5σ outlier rejection plus linear detrend run before classification.\n_noise_id_lag1acf and _noise_id_b1rn are the two internal kernels; the public identify_noise chooses between them based on N_eff.\nPer-m quadratic detrending is on by default to remove frequency offset and drift from each decimated subseries; pass detrend=false to match Stable32's no-detrend convention when comparing fixtures point-for-point.\nScale-invariance. The lag-1 ACF degeneracy guard uses a relative threshold (ssx ≤ eps · ‖x‖²) so classification is invariant under positive linear rescaling — phase records in seconds (typically ~1e-9..1e-12) classify identically to the same shape rescaled into any other units. This guard exists only to prevent 0/0 on truly constant input; meaningful tiny-magnitude records pass through normally.","title":"Implementation notes"},{"category":"section","location":"theory/noise_id/#See-also","page":"Theory: Noise Identification","text":"Theory: Confidence.\nAPI: SigmaTau.","title":"See also"},{"category":"section","location":"theory/noise_id/#References","page":"Theory: Noise Identification","text":"Riley & Greenhall, Power-law noise identification using the lag-1 autocorrelation, PTTI 2004 (Riley and Greenhall, Apr 2004).\nSP1065 §6 (Riley, Jul 2008).\nRiley, Frequency Stability Analysis Using R, 2020 (Riley, Jun 2020).","title":"References"},{"category":"section","location":"tutorials/06_three_cornered_hat/#Three-cornered-hat:-separating-individual-clock-noises-from-pairwise-comparisons","page":"Three-cornered hat: separating individual clock noises from pairwise comparisons","text":"When you compare a clock against a reference, the measured Allan deviation is the combined noise of both clocks added in quadrature. Without an absolutely-perfect reference, the noise of the clock-under-test is irrecoverable from a single comparison.\n\nThe three-cornered-hat (TCH) trick. Given three clocks and three pairwise difference records, you can solve a linear system for each clock's individual noise variance — provided the three clock noises are statistically independent. This is the standard noise-separation technique used in time-and-frequency labs to compare several standards of similar class from their pairwise comparisons alone.\n\nThis tutorial:\n\nSynthesises three independent free-running clocks of comparable precision using _gen_powerlaw_phase.\nBuilds the three pairwise difference records.\nComputes ADEV on each pair.\nSolves the TCH linear system to recover each clock's individual σ_y(τ).\nCompares the recovered σ to the synthesised \"ground truth\".","title":"Three-cornered hat: separating individual clock noises from pairwise comparisons"},{"category":"section","location":"tutorials/06_three_cornered_hat/#1.-Background-—-the-linear-system","page":"Three-cornered hat: separating individual clock noises from pairwise comparisons","text":"Let x_i(t) be the phase of clock i against an inaccessible reference, and assume the three records are statistically independent. The pairwise difference records x_{ij} = x_i − x_j are observable. Their Allan variances obey\n\nsigma^2_ij(tau) = sigma^2_i(tau) + sigma^2_j(tau)\n\n(the variance of an independent sum). The three pairwise variances yield three equations in three unknowns:\n\nbeginaligned\nsigma^2_1 = tfrac12bigl(sigma^2_12 + sigma^2_13 - sigma^2_23bigr) \nsigma^2_2 = tfrac12bigl(sigma^2_12 + sigma^2_23 - sigma^2_13bigr) \nsigma^2_3 = tfrac12bigl(sigma^2_13 + sigma^2_23 - sigma^2_12bigr)\nendaligned\n\nThe signs guarantee positivity if the three noises really are independent and the τ-grid is well-populated. Negative recoveries at any τ are a diagnostic that the independence assumption fails (typically: a shared reference offset, a temperature coupling, or small-N statistical scatter).\n\nMany extensions of the basic TCH exist (Riley's GTCH for noise correlations, Vernotte's covariance approach for confidence intervals); this tutorial sticks to the classical three-clock, independent-noise form.\n\nusing Random\nusing FFTW                                # AbstractFFTs backend for noise synth\nusing SigmaTau\nusing SigmaTau: _gen_powerlaw_phase","title":"1. Background — the linear system"},{"category":"section","location":"tutorials/06_three_cornered_hat/#2.-Synthesise-three-independent-clocks","page":"Three-cornered hat: separating individual clock noises from pairwise comparisons","text":"We build three records with similar precision:\n\nClock 1: white-FM noise at the baseline level.\nClock 2: the same noise family, 5% higher.\nClock 3: the same noise family, 10% higher.\n\nIndependent random seeds guarantee statistical independence, which is the TCH precondition. Keeping the three clocks in the same precision class makes the classical three-cornered hat well conditioned; if one clock dominates, the quieter recoveries become numerically fragile.\n\nconst N    = 8192\nconst τ₀   = 1.0\nconst m_values = unique(round.(Int, exp10.(range(0, log10(N ÷ 128); length = 12))))\nconst taus = m_values .* τ₀\n\nfunction clock_record(α, scale, seed; N = N, τ₀ = τ₀)\n    Random.seed!(seed)\n    return scale .* _gen_powerlaw_phase(α, N; tau0 = τ₀)\nend\n\nx1 = clock_record(0.0, 1.00e-12, 51)\nx2 = clock_record(0.0, 1.05e-12, 152)\nx3 = clock_record(0.0, 1.10e-12, 253)\n\nVerify the three records are equal-length and zero-mean before building differences:\n\n@info \"Clock fixtures\" N=length(x1) tau0=τ₀ taus_count=length(taus)","title":"2. Synthesise three independent clocks"},{"category":"section","location":"tutorials/06_three_cornered_hat/#3.-Pairwise-differences","page":"Three-cornered hat: separating individual clock noises from pairwise comparisons","text":"The three pairwise difference records — these are what an observer in the lab actually measures (no access to absolute time):\n\nx12 = x1 .- x2\nx13 = x1 .- x3\nx23 = x2 .- x3\n\npd12 = PhaseData(x12, τ₀)\npd13 = PhaseData(x13, τ₀)\npd23 = PhaseData(x23, τ₀)","title":"3. Pairwise differences"},{"category":"section","location":"tutorials/06_three_cornered_hat/#4.-ADEV-on-each-pair","page":"Three-cornered hat: separating individual clock noises from pairwise comparisons","text":"ci=false keeps the noise-ID / EDF machinery out of the loop — TCH operates on the raw deviations.\n\na12 = adev(pd12, m_values; ci = false).dev\na13 = adev(pd13, m_values; ci = false).dev\na23 = adev(pd23, m_values; ci = false).dev","title":"4. ADEV on each pair"},{"category":"section","location":"tutorials/06_three_cornered_hat/#5.-Solve-the-TCH-system","page":"Three-cornered hat: separating individual clock noises from pairwise comparisons","text":"Square to get variances, apply the linear inversion, then square-root back to deviations. Track any τ where the solution would go negative — those are the \"TCH break points\" and a diagnostic that the independence assumption is straining. We represent negative recoveries as NaN rather than clamping to zero: they are invalid estimates, and zeros poison log-scale plots.\n\nfunction tch_solve(a12, a13, a23)\n    v12 = a12.^2;  v13 = a13.^2;  v23 = a23.^2\n    v1  = (v12 .+ v13 .- v23) ./ 2\n    v2  = (v12 .+ v23 .- v13) ./ 2\n    v3  = (v13 .+ v23 .- v12) ./ 2\n    neg_count = count(<(0), v1) + count(<(0), v2) + count(<(0), v3)\n    positive_sqrt(v) = v > 0 ? sqrt(v) : NaN\n    σ1 = positive_sqrt.(v1)\n    σ2 = positive_sqrt.(v2)\n    σ3 = positive_sqrt.(v3)\n    return σ1, σ2, σ3, neg_count\nend\n\nσ1_tch, σ2_tch, σ3_tch, neg = tch_solve(a12, a13, a23)\nneg > 0 && @warn \"TCH yielded $neg negative variances (shown as NaN); independence assumption is straining\"","title":"5. Solve the TCH system"},{"category":"section","location":"tutorials/06_three_cornered_hat/#6.-Ground-truth-reference","page":"Three-cornered hat: separating individual clock noises from pairwise comparisons","text":"Because we synthesised the inputs, we can compute each clock's actual σ_y(τ) directly and check the recovery:\n\nσ1_truth = adev(PhaseData(x1, τ₀), m_values; ci = false).dev\nσ2_truth = adev(PhaseData(x2, τ₀), m_values; ci = false).dev\nσ3_truth = adev(PhaseData(x3, τ₀), m_values; ci = false).dev","title":"6. Ground-truth reference"},{"category":"section","location":"tutorials/06_three_cornered_hat/#7.-Compare-recovered-vs-ground-truth","page":"Three-cornered hat: separating individual clock noises from pairwise comparisons","text":"A short tabular readout at a few representative τ values:\n\nfunction readout_idx(target_tau, taus)\n    argmin(abs.(taus .- target_tau))\nend\n\nfor τt in (1.0, 10.0, 44.0)\n    i = readout_idx(τt, taus)\n    @info \"τ ≈ $(taus[i]) s\" tch1=σ1_tch[i]    truth1=σ1_truth[i]    ratio1=σ1_tch[i]/σ1_truth[i]\n    @info \"                  \" tch2=σ2_tch[i]  truth2=σ2_truth[i]    ratio2=σ2_tch[i]/σ2_truth[i]\n    @info \"                  \" tch3=σ3_tch[i]  truth3=σ3_truth[i]    ratio3=σ3_tch[i]/σ3_truth[i]\nend\n\nnote: Why ratios near 1.0 are good — and why scatter grows with τ\nThe three clocks were synthesised independently, so the TCH equations are exact in expectation. Finite-N statistical fluctuation makes individual recoveries scatter around 1.0; scatter grows with τ as the effective sample count N − 2m shrinks. For a production noise-separation analysis you'd also propagate confidence intervals through the TCH inversion (Vernotte 2010 covariance approach), use a longer record, or add fourth+ clock to overdetermine the system (extended TCH).","title":"7. Compare recovered vs ground truth"},{"category":"section","location":"tutorials/06_three_cornered_hat/#8.-Plot-recovered-vs-ground-truth-σ_y(τ)-on-log-log-axes","page":"Three-cornered hat: separating individual clock noises from pairwise comparisons","text":"using Plots\n\nfunction plot_positive_tch!(τ, σ; kwargs...)\n    keep = isfinite.(σ) .& (σ .> 0)\n    plot!(τ[keep], σ[keep]; marker = :circle, kwargs...)\nend\n\nplot(\n    taus, σ1_truth;\n    label  = \"Clock 1 truth\",\n    xscale = :log10, yscale = :log10,\n    xlabel = raw\"\\(\\tau\\) (s)\",\n    ylabel = raw\"\\(\\sigma_y(\\tau)\\)\",\n    title  = \"Three-cornered hat: recovered vs ground-truth σ_y(τ)\",\n    legend = :topright,\n    lw     = 1.5,\n)\nplot_positive_tch!(taus, σ1_tch; label = \"Clock 1 TCH\",   ls = :dash, lw = 1.5)\nplot!(taus, σ2_truth; label = \"Clock 2 truth\", lw = 1.5)\nplot_positive_tch!(taus, σ2_tch; label = \"Clock 2 TCH\",   ls = :dash, lw = 1.5)\nplot!(taus, σ3_truth; label = \"Clock 3 truth\", lw = 1.5)\nplot_positive_tch!(taus, σ3_tch; label = \"Clock 3 TCH\",   ls = :dash, lw = 1.5)","title":"8. Plot recovered vs ground-truth σ_y(τ) on log-log axes"},{"category":"section","location":"tutorials/06_three_cornered_hat/#9.-When-TCH-breaks-down","page":"Three-cornered hat: separating individual clock noises from pairwise comparisons","text":"Two real-world failure modes worth flagging:\n\nCorrelated noises. A shared reference offset, common temperature coupling, or correlated power-supply ripple violates the independence assumption and shows up as systematic bias (often negative variances at specific τ). Diagnose by repeating the analysis with a fourth, electrically-isolated clock and inspecting whether recovered σ is invariant to the choice of triplet.\nOne clock dominating. If one clock is much noisier than the other two, the pairwise differences look like that clock's noise alone, and the recovered σ for the two quieter clocks becomes ill-conditioned (high relative scatter). The classical countermeasure is to run the TCH on three clocks of similar grade — pre-screen the population before applying it.\n\nFor larger ensembles, the natural extension is to estimate every clock's σ from the full N(N−1)/2 pairwise differences via weighted least squares; Riley's generalized TCH papers are the usual starting point.","title":"9. When TCH breaks down"},{"category":"section","location":"tutorials/06_three_cornered_hat/#10.-The-one-call-version:-nch","page":"Three-cornered hat: separating individual clock noises from pairwise comparisons","text":"Everything above is what SigmaTau.nch does internally — and it generalizes to any number of clocks N ≥ 3 via the standard N-cornered-hat inversion (this three-clock case is exactly the classic formula). Feed it the pairwise StabilityResults in the cyclic order A−B, B−C, C−A (orientation of each difference record doesn't matter) and get one separated result per clock:\n\nr12 = adev(pd12, m_values; ci = false)\nr13 = adev(pd13, m_values; ci = false)\nr23 = adev(pd23, m_values; ci = false)\n\nclock1, clock2, clock3 = nch(r12, r23, r13)   # (A−B, B−C, C−A)\n\nThe one-call results match the manual derivation above (NaN where the inversion went negative, exactly like tch_solve):\n\n@assert isapprox(clock1.dev, σ1_tch; rtol = 1e-12, nans = true)\n@assert isapprox(clock2.dev, σ2_tch; rtol = 1e-12, nans = true)\n@assert isapprox(clock3.dev, σ3_tch; rtol = 1e-12, nans = true)\n@info \"nch reproduces the manual TCH solution\" clock1 clock2 clock3\n\nFor N > 3 clocks, build an upper-triangular matrix of pairwise results instead — pairwise[i, j] for i < j — and call nch(pairwise) to get a Vector{StabilityResult}, one per clock.","title":"10. The one-call version: nch"},{"category":"section","location":"validation/methodology/#Validation-Methodology","page":"Validation Methodology","text":"SigmaTau.jl is cross-validated against independent references where those references exist:\n\nStable32 (W. Riley) — desktop application, the de facto industry reference for time-and-frequency stability analysis.\nallantools (A. Wallin) — Python library, second independent numerical reference.\n\nAgreement with the shipped fixtures defines the rtol floor; the documented residuals are bias-correction policy (Stable32 corrects HTOTDEV by default but not MTOTDEV; SigmaTau corrects both) and the per-τ noise-identification convention (identify_noise(…; detrend=false) reproduces Stable32's assignments on the fixture). The raw total kernels match allantools to machine precision. Estimators without an external implementation are checked against inlined legacy kernels and internal identities.\n\nValidation is about numerical agreement, not speed. For head-to-head timings against allantools — including the ~4,000× speedup on the modified-total kernels for long records — see Performance.\n\nDetailed comparison narrative lands in a follow-up PR.","title":"Validation Methodology"},{"category":"section","location":"validation/stable32/#Validation:-Stable32","page":"Validation: Stable32","text":"This page documents agreement between SigmaTau.jl outputs and reference fixtures generated from test/fixtures/validation/stable32gen.DAT — an 8192-point phase record at τ₀ = 1 s. Two references are compared at every τ:\n\nStable32 (stable32out/stable32_data_full.csv): σ, the identified noise exponent α, and the 68.3 % confidence bounds, all printed to 5 significant figures. That rounding sets the agreement floor of roughly 5e-5 for any comparison against this file.\nallantools (allantools_out/allantools_data_full.csv): σ printed at full Float64 precision, so kernel-level agreement can be checked to machine precision.\n\nAll SigmaTau values below were computed from the shipped fixture with the current code:\n\np = read_phase(\"test/fixtures/validation/stable32gen.DAT\";\n               time_col=0, value_col=1, tau0=1.0, header=10)\n\nDefault settings throughout; the total-family tables additionally show correct_bias=false (\"raw\") columns.","title":"Validation: Stable32"},{"category":"section","location":"validation/stable32/#Summary","page":"Validation: Stable32","text":"Estimator σ vs Stable32 σ (raw) vs allantools CI bounds vs Stable32\nOADEV, MDEV, TDEV, OHDEV ≤ 0.005 % (fixture precision) ≤ 1e-13 ≤ 0.05 % at 11 of 12 τ¹\nTOTDEV ≤ 0.005 % at all 13 τ ≤ 2.8e-15 ≤ 0.07 % at 10 of 13 τ¹ ²\nMTOTDEV (raw) ≤ 0.004 % ≤ 1.1e-15 within 3.2 % (≤ 0.02 % at τ ≤ 8 s)³\nHTOTDEV ≤ 0.003 % at 11 of 12 τ¹ ≤ 4.4e-15 ≤ 0.1 % at 9 of 12 τ¹ ²\n\n¹ The single recurring residual is a noise-identification convention, not a numerical disagreement — see below. ² At the two flicker-PM τ (32 s, 64 s) the bounds differ by up to 0.8 % (TOTDEV) / 1.1 % (HTOTDEV): the published TOTVAR/HTOT EDF tables stop at α = 0, and the two programs substitute different EDF models above it. ³ Stable32's MTOT EDF model and SP1065 Table 8 drift apart at long τ; the bounds agree to ≤ 0.02 % at τ ≤ 8 s and to within 3.2 % at the longest τ.\n\nThree per-estimator facts behind those rows:\n\nTOTDEV — both programs apply the Howe/Walter B = 1 − a·τ/T correction for FLFM/RWFM. SigmaTau's default (correct_bias=true) matches Stable32's σ to ≤ 0.005 % at every τ including the FLFM row at τ = 512 s; correct_bias=false reproduces allantools' raw SP1065 eq. 25 kernel to ≤ 2.8e-15.\nHTOTDEV — Stable32 applies the Howe 2005 bias correction by default (the factors are exactly 1/√(1+a)), and so does SigmaTau: correct_bias=true (the default) matches Stable32 to ≤ 0.003 % at every matching-α τ. correct_bias=false reproduces allantools' raw kernel to ≤ 4.4e-15.\nMTOTDEV — here the two programs deliberately report different statistics. Stable32 reports the raw estimator; SigmaTau's raw kernel matches it to ≤ 0.004 % (and allantools to ≤ 1.1e-15). SigmaTau's default divides by √B(α) (SP1065 Table 11) to unbias toward the parent modified-Allan statistic, which places the default output 2.9 % (WHPM) to 12.3 % (FLFM) below Stable32 on this fixture — up to ≈ 14.5 % at RWFM per the table. Pass correct_bias=false to reproduce Stable32 and allantools.","title":"Summary"},{"category":"section","location":"validation/stable32/#The-noise-identification-residual","page":"Validation: Stable32","text":"At τ = 256 s (every estimator) Stable32 assigns α = +2 where SigmaTau's default identification assigns α = 0. This is a convention difference in the lag-1 ACF identifier: SigmaTau quadratically detrends each decimated subseries before classifying (detrend=true default); Stable32 does not. identify_noise(x, m_values; detrend=false) reproduces Stable32's α at all 12 τ on this fixture. Because the EDF — and, for the total family, the bias factor — depend on α, the flipped row carries different CI bounds (and, for HTOTDEV, a 0.25 % σ difference). The rows are marked ¹ in the tables below.","title":"The noise-identification residual"},{"category":"section","location":"validation/stable32/#Confidence-intervals","page":"Validation: Stable32","text":"Stable32 has computed its confidence intervals with the Greenhall–Riley 2003 EDF algorithm since v1.41 (stated in the paper itself, p. 268), and SigmaTau implements the same algorithm: wherever the two programs agree on α, the 68.3 % bounds agree to ≤ 0.05 % for the core estimators. The remaining total-family differences at α > 0 (footnote ² above) come from EDF substitutions outside the published coefficient tables, not from a different interval method.","title":"Confidence intervals"},{"category":"section","location":"validation/stable32/#Detailed-comparison","page":"Validation: Stable32","text":"The tables below compare Stable32, allantools, and SigmaTau at each averaging interval τ. Stable32 columns carry 5 significant figures (the fixture's precision); SigmaTau and allantools columns are truncated to 6. CI columns are the 68.3 % [lower, upper] bounds — Stable32's MinSigma/MaxSigma against SigmaTau's ci_lower/ci_upper. Rows marked ¹ are the τ = 256 s noise-identification flip.","title":"Detailed comparison"},{"category":"section","location":"validation/stable32/#Overlapping-Allan","page":"Validation: Stable32","text":"τ (s) Stable32 allantools SigmaTau Stable32 CI SigmaTau CI\n1 1.0097e+00 1.00975e+00 1.00975e+00 [9.9898e-01, 1.0209e+00] [9.9892e-01, 1.0209e+00]\n2 5.0444e-01 5.04443e-01 5.04443e-01 [4.9907e-01, 5.1000e-01] [4.9903e-01, 5.1003e-01]\n4 2.5021e-01 2.50214e-01 2.50214e-01 [2.4755e-01, 2.5297e-01] [2.4753e-01, 2.5299e-01]\n8 1.3063e-01 1.30632e-01 1.30632e-01 [1.2924e-01, 1.3207e-01] [1.2923e-01, 1.3208e-01]\n16 6.2750e-02 6.27503e-02 6.27503e-02 [6.2081e-02, 6.3442e-02] [6.2076e-02, 6.3447e-02]\n32 3.1804e-02 3.18042e-02 3.18042e-02 [3.1141e-02, 3.2512e-02] [3.1136e-02, 3.2517e-02]\n64 1.6085e-02 1.60852e-02 1.60852e-02 [1.5668e-02, 1.6537e-02] [1.5668e-02, 1.6537e-02]\n128 8.1063e-03 8.10630e-03 8.10630e-03 [7.5737e-03, 8.7695e-03] [7.5735e-03, 8.7700e-03]\n256¹ 4.5004e-03 4.50037e-03 4.50037e-03 [4.4513e-03, 4.5511e-03] [4.0951e-03, 5.0557e-03]\n512 3.1642e-03 3.16419e-03 3.16419e-03 [2.7368e-03, 3.8843e-03] [2.7365e-03, 3.8851e-03]\n1024 3.1373e-03 3.13732e-03 3.13732e-03 [2.6177e-03, 4.1767e-03] [2.6175e-03, 4.1769e-03]\n2048 4.6131e-03 4.61311e-03 4.61311e-03 [3.5914e-03, 7.7529e-03] [3.5909e-03, 7.7548e-03]","title":"Overlapping Allan"},{"category":"section","location":"validation/stable32/#Modified-Allan","page":"Validation: Stable32","text":"τ (s) Stable32 allantools SigmaTau Stable32 CI SigmaTau CI\n1 1.0097e+00 1.00975e+00 1.00975e+00 [9.9898e-01, 1.0209e+00] [9.9892e-01, 1.0209e+00]\n2 3.5650e-01 3.56499e-01 3.56499e-01 [3.5252e-01, 3.6062e-01] [3.5249e-01, 3.6065e-01]\n4 1.2611e-01 1.26111e-01 1.26111e-01 [1.2434e-01, 1.2796e-01] [1.2433e-01, 1.2797e-01]\n8 4.6674e-02 4.66743e-02 4.66743e-02 [4.5785e-02, 4.7618e-02] [4.5779e-02, 4.7624e-02]\n16 1.5818e-02 1.58178e-02 1.58178e-02 [1.5400e-02, 1.6272e-02] [1.5397e-02, 1.6275e-02]\n32 6.0448e-03 6.04484e-03 6.04484e-03 [5.7952e-03, 6.3299e-03] [5.7936e-03, 6.3318e-03]\n64 2.5945e-03 2.59450e-03 2.59450e-03 [2.4463e-03, 2.7736e-03] [2.4453e-03, 2.7747e-03]\n128 1.7347e-03 1.73473e-03 1.73473e-03 [1.5955e-03, 1.9182e-03] [1.5954e-03, 1.9183e-03]\n256¹ 1.8372e-03 1.83717e-03 1.83717e-03 [1.6585e-03, 2.0896e-03] [1.6358e-03, 2.1375e-03]\n512 2.2115e-03 2.21151e-03 2.21151e-03 [1.8801e-03, 2.8157e-03] [1.8800e-03, 2.8161e-03]\n1024 2.7671e-03 2.76714e-03 2.76714e-03 [2.2099e-03, 4.2001e-03] [2.2094e-03, 4.2012e-03]\n2048 5.2906e-03 5.29055e-03 5.29055e-03 [3.8697e-03, 1.3794e-02] [3.8687e-03, 1.3800e-02]","title":"Modified Allan"},{"category":"section","location":"validation/stable32/#Time","page":"Validation: Stable32","text":"τ (s) Stable32 allantools SigmaTau Stable32 CI SigmaTau CI\n1 5.8298e-01 5.82978e-01 5.82978e-01 [5.7676e-01, 5.8940e-01] [5.7672e-01, 5.8944e-01]\n2 4.1165e-01 4.11650e-01 4.11650e-01 [4.0705e-01, 4.1641e-01] [4.0702e-01, 4.1644e-01]\n4 2.9124e-01 2.91241e-01 2.91241e-01 [2.8715e-01, 2.9551e-01] [2.8713e-01, 2.9554e-01]\n8 2.1558e-01 2.15579e-01 2.15579e-01 [2.1147e-01, 2.1994e-01] [2.1144e-01, 2.1997e-01]\n16 1.4612e-01 1.46119e-01 1.46119e-01 [1.4226e-01, 1.5032e-01] [1.4223e-01, 1.5034e-01]\n32 1.1168e-01 1.11680e-01 1.11680e-01 [1.0707e-01, 1.1695e-01] [1.0704e-01, 1.1698e-01]\n64 9.5868e-02 9.58679e-02 9.58679e-02 [9.0391e-02, 1.0249e-01] [9.0356e-02, 1.0253e-01]\n128 1.2820e-01 1.28198e-01 1.28198e-01 [1.1791e-01, 1.4175e-01] [1.1790e-01, 1.4176e-01]\n256¹ 2.7154e-01 2.71537e-01 2.71537e-01 [2.4513e-01, 3.0884e-01] [2.4177e-01, 3.1592e-01]\n512 6.5373e-01 6.53730e-01 6.53730e-01 [5.5577e-01, 8.3233e-01] [5.5573e-01, 8.3245e-01]\n1024 1.6359e+00 1.63595e+00 1.63595e+00 [1.3065e+00, 2.4831e+00] [1.3062e+00, 2.4838e+00]\n2048 6.2556e+00 6.25562e+00 6.25562e+00 [4.5756e+00, 1.6310e+01] [4.5744e+00, 1.6317e+01]","title":"Time"},{"category":"section","location":"validation/stable32/#Overlapping-Hadamard","page":"Validation: Stable32","text":"τ (s) Stable32 allantools SigmaTau Stable32 CI SigmaTau CI\n1 1.0646e+00 1.06459e+00 1.06459e+00 [1.0522e+00, 1.0774e+00] [1.0522e+00, 1.0775e+00]\n2 5.3205e-01 5.32049e-01 5.32049e-01 [5.2587e-01, 5.3845e-01] [5.2584e-01, 5.3849e-01]\n4 2.6192e-01 2.61924e-01 2.61924e-01 [2.5888e-01, 2.6507e-01] [2.5886e-01, 2.6509e-01]\n8 1.3854e-01 1.38538e-01 1.38538e-01 [1.3693e-01, 1.4021e-01] [1.3692e-01, 1.4022e-01]\n16 6.6018e-02 6.60177e-02 6.60177e-02 [6.5250e-02, 6.6813e-02] [6.5245e-02, 6.6818e-02]\n32 3.3500e-02 3.35003e-02 3.35003e-02 [3.2736e-02, 3.4321e-02] [3.2740e-02, 3.4316e-02]\n64 1.6951e-02 1.69515e-02 1.69515e-02 [1.6477e-02, 1.7469e-02] [1.6477e-02, 1.7469e-02]\n128 8.4500e-03 8.44998e-03 8.44998e-03 [7.8517e-03, 9.2096e-03] [7.8514e-03, 9.2102e-03]\n256¹ 4.5672e-03 4.56718e-03 4.56718e-03 [4.5122e-03, 4.6242e-03] [4.1228e-03, 5.1951e-03]\n512 2.9510e-03 2.95105e-03 2.95105e-03 [2.5185e-03, 3.7257e-03] [2.5182e-03, 3.7265e-03]\n1024 2.4608e-03 2.46084e-03 2.46084e-03 [2.0108e-03, 3.4682e-03] [2.0106e-03, 3.4687e-03]\n2048 2.8169e-03 2.81691e-03 2.81691e-03 [2.1331e-03, 5.4864e-03] [2.1326e-03, 5.4890e-03]","title":"Overlapping Hadamard"},{"category":"section","location":"validation/stable32/#Total","page":"Validation: Stable32","text":"SigmaTau corrected is the default (correct_bias=true); it differs from the raw column only at the FLFM row (τ = 512 s), where the Howe/Walter B = 1 − a·τ/T correction applies — and that is the row where Stable32 matches the corrected value, confirming Stable32 applies the same correction. The SigmaTau CI column belongs to the default (corrected) output.\n\nτ (s) Stable32 allantools (raw) SigmaTau raw SigmaTau corrected Stable32 CI SigmaTau CI\n1 1.0097e+00 1.00975e+00 1.00975e+00 1.00975e+00 [9.9884e-01, 1.0210e+00] [9.9892e-01, 1.0209e+00]\n2 5.0443e-01 5.04428e-01 5.04428e-01 5.04428e-01 [4.9898e-01, 5.1006e-01] [4.9902e-01, 5.1002e-01]\n4 2.5026e-01 2.50255e-01 2.50255e-01 2.50255e-01 [2.4755e-01, 2.5305e-01] [2.4757e-01, 2.5303e-01]\n8 1.3060e-01 1.30605e-01 1.30605e-01 1.30605e-01 [1.2919e-01, 1.3206e-01] [1.2920e-01, 1.3205e-01]\n16 6.2782e-02 6.27816e-02 6.27816e-02 6.27816e-02 [6.2103e-02, 6.3483e-02] [6.2107e-02, 6.3478e-02]\n32 3.1842e-02 3.18422e-02 3.18422e-02 3.18422e-02 [3.1345e-02, 3.2364e-02] [3.1174e-02, 3.2556e-02]\n64 1.6090e-02 1.60901e-02 1.60901e-02 1.60901e-02 [1.5791e-02, 1.6407e-02] [1.5673e-02, 1.6542e-02]\n128 8.1074e-03 8.10739e-03 8.10739e-03 8.10739e-03 [7.5805e-03, 8.7619e-03] [7.5801e-03, 8.7624e-03]\n256¹ 4.4756e-03 4.47557e-03 4.47557e-03 4.47557e-03 [4.4264e-03, 4.5264e-03] [4.0807e-03, 5.0127e-03]\n512 3.1028e-03 3.05583e-03 3.05583e-03 3.10283e-03 [2.6974e-03, 3.7709e-03] [2.6974e-03, 3.7709e-03]\n1024 2.8694e-03 2.86942e-03 2.86942e-03 2.86942e-03 [2.4276e-03, 3.6939e-03] [2.4273e-03, 3.6948e-03]\n2048 3.4123e-03 3.41229e-03 3.41229e-03 3.41229e-03 [2.7445e-03, 5.0548e-03] [2.7439e-03, 5.0558e-03]\n4096 4.0832e-03 4.08324e-03 4.08324e-03 4.08324e-03 [3.1056e-03, 7.7435e-03] [3.1048e-03, 7.7483e-03]","title":"Total"},{"category":"section","location":"validation/stable32/#Modified-Total","page":"Validation: Stable32","text":"Stable32 and allantools both report the raw estimator here; the SigmaTau raw column matches both (≤ 0.004 % / ≤ 1.1e-15). The corrected column is SigmaTau's default — the raw value divided by √B(α) from SP1065 Table 11, which on this fixture is 2.9 % (WHPM), 7.5 % (FLPM), 11.3 % (WHFM), or 12.3 % (FLFM) below Stable32. The SigmaTau CI column belongs to the raw output for like-for-like comparison with Stable32's bounds.\n\nτ (s) Stable32 allantools (raw) SigmaTau raw SigmaTau corrected Stable32 CI SigmaTau CI (raw)\n1 7.1400e-01 7.13999e-01 7.13999e-01 6.93497e-01 [7.0639e-01, 7.2186e-01] [7.0634e-01, 7.2191e-01]\n2 3.5625e-01 3.56253e-01 3.56253e-01 3.46024e-01 [3.5227e-01, 3.6037e-01] [3.5225e-01, 3.6040e-01]\n4 1.2570e-01 1.25704e-01 1.25704e-01 1.22095e-01 [1.2394e-01, 1.2755e-01] [1.2393e-01, 1.2756e-01]\n8 4.6303e-02 4.63031e-02 4.63031e-02 4.49735e-02 [4.5421e-02, 4.7239e-02] [4.5415e-02, 4.7245e-02]\n16 1.5819e-02 1.58191e-02 1.58191e-02 1.53648e-02 [1.5495e-02, 1.6165e-02] [1.5472e-02, 1.6191e-02]\n32 5.8856e-03 5.88559e-03 5.88559e-03 5.44123e-03 [5.6979e-03, 6.0933e-03] [5.6612e-03, 6.1389e-03]\n64 2.4483e-03 2.44833e-03 2.44833e-03 2.26348e-03 [2.3398e-03, 2.5737e-03] [2.3192e-03, 2.6017e-03]\n128 1.5120e-03 1.51199e-03 1.51199e-03 1.34167e-03 [1.4073e-03, 1.6442e-03] [1.3982e-03, 1.6589e-03]\n256¹ 1.5468e-03 1.54684e-03 1.54684e-03 1.37260e-03 [1.4288e-03, 1.7001e-03] [1.3889e-03, 1.7747e-03]\n512 1.8467e-03 1.84668e-03 1.84668e-03 1.61965e-03 [1.5780e-03, 2.3251e-03] [1.5713e-03, 2.3468e-03]\n1024 2.2545e-03 2.25454e-03 2.25454e-03 2.00058e-03 [1.8657e-03, 3.0666e-03] [1.8461e-03, 3.1572e-03]\n2048 4.0510e-03 4.05095e-03 4.05095e-03 3.59464e-03 [3.1147e-03, 7.2325e-03] [3.0964e-03, 7.4599e-03]","title":"Modified Total"},{"category":"section","location":"validation/stable32/#Hadamard-Total","page":"Validation: Stable32","text":"Stable32 applies the Howe 2005 bias correction by default and so does SigmaTau: the corrected (default) column matches Stable32 at every matching-α τ; the raw column matches allantools. The two columns differ by exactly √(1+a) per Howe 2005 — for example 0.9975 at WHFM and 0.9225 at FLFM (τ = 512 s, where Stable32 = 3.3578e-03 matches the corrected 3.35777e-03, not the raw 3.09753e-03). The SigmaTau CI column belongs to the default (corrected) output.\n\nτ (s) Stable32 allantools (raw) SigmaTau raw SigmaTau corrected Stable32 CI SigmaTau CI\n1 1.0646e+00 1.06459e+00 1.06459e+00 1.06459e+00 [1.0531e+00, 1.0765e+00] [1.0522e+00, 1.0775e+00]\n2 5.9406e-01 5.94056e-01 5.94056e-01 5.94056e-01 [5.8764e-01, 6.0069e-01] [5.8712e-01, 6.0124e-01]\n4 3.0296e-01 3.02955e-01 3.02955e-01 3.02955e-01 [2.9968e-01, 3.0634e-01] [2.9942e-01, 3.0662e-01]\n8 1.5785e-01 1.57846e-01 1.57846e-01 1.57846e-01 [1.5614e-01, 1.5961e-01] [1.5600e-01, 1.5976e-01]\n16 7.7624e-02 7.76241e-02 7.76241e-02 7.76241e-02 [7.6785e-02, 7.8492e-02] [7.6716e-02, 7.8565e-02]\n32 3.9227e-02 3.92275e-02 3.92275e-02 3.92275e-02 [3.8615e-02, 3.9870e-02] [3.8338e-02, 4.0182e-02]\n64 1.9757e-02 1.97566e-02 1.97566e-02 1.97566e-02 [1.9389e-02, 2.0146e-02] [1.9204e-02, 2.0360e-02]\n128 9.9148e-03 9.89001e-03 9.89001e-03 9.91483e-03 [9.3157e-03, 1.0647e-02] [9.3118e-03, 1.0652e-02]\n256¹ 5.1795e-03 5.17950e-03 5.17950e-03 5.19250e-03 [5.1226e-03, 5.2383e-03] [4.7578e-03, 5.7731e-03]\n512 3.3578e-03 3.09753e-03 3.09753e-03 3.35777e-03 [2.9050e-03, 4.1199e-03] [2.9047e-03, 4.1207e-03]\n1024 2.4120e-03 2.40600e-03 2.40600e-03 2.41204e-03 [2.0370e-03, 3.1179e-03] [2.0368e-03, 3.1187e-03]\n2048 2.9382e-03 2.93086e-03 2.93086e-03 2.93822e-03 [2.3269e-03, 4.6000e-03] [2.3265e-03, 4.6009e-03]","title":"Hadamard Total"},{"category":"section","location":"validation/stable32/#Methodology","page":"Validation: Stable32","text":"See Validation Methodology for the shipped fixture strategy.","title":"Methodology"},{"category":"section","location":"theory/confidence/#Theory:-Confidence-Intervals","page":"Theory: Confidence Intervals","text":"Each StabilityResult.dev value is a point estimate of the underlying deviation. The corresponding confidence interval rests on the chi-squared distribution applied to an equivalent number of degrees of freedom (EDF) that depends on the estimator, the averaging factor m, the record length N, the record span T (for the total family), and the identified noise type α.","title":"Theory: Confidence Intervals"},{"category":"section","location":"theory/confidence/#EDF-as-a-bridge","page":"Theory: Confidence Intervals","text":"The variance estimate behaves approximately as a scaled chi-squared variable:\n\nmathrmEDF cdot frachatsigma^2_y(tau)sigma^2_y(tau)\nsim chi^2_mathrmEDF\nqquad\nmathrmEDF = frac2bigl(Ehatsigma^2_ybigr)^2operatornameVarhatsigma^2_y\n\nwhere σ̂²_y(τ) is the variance estimate, σ²_y(τ) is the true variance, and χ²_EDF is the chi-squared distribution with EDF degrees of freedom — the defining moment relation is Eq. (1) of GR03 (Greenhall and Riley, 2003). For confidence level CL (default DEFAULT_CONFIDENCE = 0.683, the 1σ convention shared with Stable32 and allantools), the lower and upper bounds on σ²_y(τ) are:\n\nsigma^2_textlo = fracmathrmEDFcdothatsigma^2chi^2_(1+mathrmCL)2\nquad\nsigma^2_texthi = fracmathrmEDFcdothatsigma^2chi^2_(1-mathrmCL)2\n\nwhere χ²_p denotes the p-quantile of the chi-squared distribution with EDF degrees of freedom: the lower variance bound divides by the upper quantile and vice versa, per SP1065 §5 (Riley, Jul 2008). Each StabilityResult.ci entry carries the deviations √σ²_lo and √σ²_hi as its lo and hi components; SigmaTau evaluates the quantiles with Distributions.jl rather than table lookup.\n\nThe χ² approximation is empirical and asymptotic in EDF: the interval narrows as N/m grows, and for a fixed record the EDF peaks near white FM. At small EDF (long τ, short records, or steep red noise) the interval is wide and asymmetric — the upper bound stretches much further than the lower bound, reflecting the long right tail of χ² at low degrees of freedom. When the EDF is non-finite or below 1, SigmaTau falls back to a normal-approximation envelope\n\nd pm K_alpha  d  z  sqrtN\n\nwhere d is the reported deviation, z is the two-sided normal quantile at the same confidence level (z ≈ 1 at CL = 0.683), and K_α is the SP1065 simple-interval noise factor — 0.99, 0.99, 0.87, 0.77, 0.75 for α = 2, 1, 0, −1, −2 (Riley, Jul 2008). The lower limit is floored at zero, since a deviation is non-negative by construction. The fallback keeps reported bounds finite at the cost of optimism in the deep red-noise regime.","title":"EDF as a bridge"},{"category":"section","location":"theory/confidence/#Greenhall–Riley-2003-(GR03)","page":"Theory: Confidence Intervals","text":"GR03 (Greenhall and Riley, 2003) gives a single numerical algorithm — explicitly not a set of closed formulas — for the EDF of the overlapped and non-overlapped ADEV/MDEV/HDEV/MHDEV estimators at the power-law noise exponents α ∈ {2, 1, 0, −1, −2, −3, −4}, subject to the restriction α + 2d > 1. SigmaTau implements the simplified version of the algorithm (the truncated sum of GR03 Eqs. 10–13) in src/edf.jl (_calc_edf_core), always with the overlapped stride S = m. The five noise types SigmaTau's noise identification can return (α ∈ {2, …, −2}) all satisfy the restriction for both d = 2 and d = 3.\n\nThe algorithm models the phase as the τ₀-difference of a pure power-law process w(t) and chains three generalized autocovariance (GACV) functions: sw(t, α), the GACV of w; sx(t, F, α) = F² Δ_{1/F} Δ_{−1/F} sw, the autocovariance of the (averaged) phase; and sz(t, F, α, d) = (Δ₁ Δ₋₁)^d sx, the autocovariance of the d-th difference process whose mean square is the variance being estimated. The EDF of an estimator with M summands is then\n\nfrac1mathrmEDF = frac1Ms_z^2(0)\nleft s_z^2(0)\n+ 2 sum_j=1^J-1 Bigl(1 - fracjMBigr) s_z^2Bigl(fracjSBigr)\n+ Bigl(1 - fracJMBigr) s_z^2Bigl(fracJSBigr) right\n\nwith the lag sum truncated at J = min(M, (d+1)·S). Symbols: d is the phase-difference order (d = 2 for the Allan family, d = 3 for the Hadamard family); F is the filter factor (F = m for the unmodified variances, F = 1 for the modified ones — this is what captures the modified-family inner average); S is the stride factor (S = m, the overlapped estimator); M = 1 + ⌊S(N − L)/m⌋ counts the estimator summands, with filter length L = m/F + m·d. SigmaTau evaluates these sums directly rather than using the asymptotic coefficient tables of GR03's \"full version\".","title":"Greenhall–Riley 2003 (GR03)"},{"category":"section","location":"theory/confidence/#Total-family-EDF","page":"Theory: Confidence Intervals","text":"GR03 explicitly excludes the total variances, and no GR03-style formulas are published for them. SigmaTau uses published coefficient tables where they exist and documented fallbacks elsewhere:\n\nTOTDEV — EDF = b(α)·(T/τ) − c(α) with the SP1065 Table 7 coefficients for α ∈ {0, −1, −2} (Riley, Jul 2008). For WHPM/FLPM (α = 2, 1) no totvar-specific value is published, so the ADEV-style GR03 EDF (d = 2, F = m, S = m) is used as a pragmatic substitute.\nMTOTDEV — the same b(α)·(T/τ) − c(α) form with the SP1065 Table 8 coefficients for all five α ∈ {2, …, −2}, valid for τ ≥ 16τ₀ (Riley, Jul 2008). Below that floor (m < 16) MTOT reduces to MDEV, so the MDEV-style GR03 EDF (d = 2, F = 1) is used instead.\nHTOTDEV — EDF = (T/τ) / (b₀(α) + b₁(α)·τ/T) with the FCS 2001 coefficients for α ∈ {0, −1, −2, −3, −4} (Howe et al., 2001); HDEV-style GR03 fallback (d = 3, F = m, S = m) at WHPM/FLPM.\nMHTOTDEV — novel to SigmaTau; no external reference exists. The b(α)·(T/τ) − c(α) coefficients were measured by Monte Carlo over the τ/τ₀ ≥ 16 validity window — see Theory: MHTOTDEV bias and EDF.\nTDEV / HTDEV / TTOTDEV — rescale MDEV, MHDEV, and MTOTDEV by τ/√3, which does not change the degrees of freedom; the EDF and the χ² bounds are inherited from the wrapped estimator and scaled by the same factor.\nPDEV — uses the published PVAR EDF model of Vernotte–Chen–Rubiola 2020 (Vernotte et al., 2020); see Theory: PDEV confidence.\nMTIE — no published EDF model; ci and confidence are accepted for API uniformity but noise_type, edf, and the CI bounds are always returned empty.","title":"Total-family EDF"},{"category":"section","location":"theory/confidence/#Bias-correction-summary","page":"Theory: Confidence Intervals","text":"Estimator B(α) applied? Notes\nADEV / MDEV / HDEV / MHDEV / TDEV / HTDEV / PDEV none Unbiased estimators\nTOTDEV per SP1065 B = 1 − a(α)·τ/T — biased low for FFM/RWFM\nMTOTDEV / TTOTDEV per SP1065 τ-independent; B ≈ 1.27 (variance) under WHFM\nHTOTDEV per FCS 2001 B = 1 + a(α), α ∈ {0, …, −4} — biased low\nMHTOTDEV Monte Carlo (SigmaTau) B = b₀ + b₁·τ/T; ≈ 1 for white/flicker noise\n\nThe bias factor B(α) = E[estimator²] / true variance is defined on the variance; the API divides the raw deviation by √B, which is equivalent. The correction is applied before the confidence interval is evaluated, so the reported χ² bounds bracket the corrected deviation. All four total-family estimators accept correct_bias (default true); pass correct_bias=false to recover the raw kernel value.\n\nThe SP1065 TOTDEV factor B = 1 − a(α)·(τ/T) uses a = 1/(3 ln 2) ≈ 0.481 for FLFM and a = 0.75 for RWFM, zero otherwise; B < 1 and rises toward 1 as τ/T → 0 (Riley, Jul 2008). MTOTDEV uses a τ-independent variance table {1.06, 1.17, 1.27, 1.30, 1.31} for α ∈ {2, 1, 0, −1, −2} (Riley, Jul 2008) (Riley, Jun 2020) — MTOT is biased high, so the correction lowers σ. HTOTDEV uses the FCS 2001 form B = 1 + a(α) with a ∈ {−0.005, −0.149, −0.229, −0.283, −0.321} for α ∈ {0, −1, −2, −3, −4}; all a < 0, so B < 1, reflecting that HTOT is biased low for the FM noises, while PM noises (α > 0) have no published model and keep B = 1 (Howe et al., 2001). MHTOTDEV is not treated as unbiased: SigmaTau applies a Monte-Carlo-measured B = b₀(α) + b₁(α)·(τ/T) that is ≈ 1 for white and flicker noise but reads high for redder FM (RWFM B ≈ 1.9 at small τ) — see Theory: MHTOTDEV bias and EDF.","title":"Bias correction summary"},{"category":"section","location":"theory/confidence/#Implementation-contract","page":"Theory: Confidence Intervals","text":"ci=true is the default for every deviation. The StabilityResult.edf and ci vectors are populated only on that path; pass ci=false to skip the χ² evaluation entirely and get them back empty (neff, the per-τ analysis-window count, is populated either way — it falls out of the kernel loop bounds for free). This is a deliberate API contract: callers that don't need confidence intervals pay no noise-identification or χ² cost. (One nuance: the total-family estimators still populate noise_type under ci=false whenever correct_bias=true, because the bias factor itself needs the per-τ noise identification.)\n\nusing SigmaTau, Random\nRandom.seed!(1)\nx = randn(2000)\nr = adev(PhaseData(x, 1.0), [10, 100]; ci=true)\n(r.edf, round.(ci_lower(r); sigdigits=3), round.(ci_upper(r); sigdigits=3))","title":"Implementation contract"},{"category":"section","location":"theory/confidence/#See-also","page":"Theory: Confidence Intervals","text":"Theory: Allan family.\nTheory: Total family.\nTheory: PDEV confidence.\nTheory: MHTOTDEV bias and EDF.\nAPI: SigmaTau.","title":"See also"},{"category":"section","location":"theory/confidence/#References","page":"Theory: Confidence Intervals","text":"Greenhall & Riley, Uncertainty of Stability Variances Based on Finite Differences, PTTI 2003 (Greenhall and Riley, 2003).\nHowe et al., Total Hadamard Variance, FCS 2001 (Howe et al., 2001).\nSP1065 §5 (Riley, Jul 2008).\nRiley, Frequency Stability Analysis Using R, 2020 (Riley, Jun 2020).\nVernotte, Chen & Rubiola, PVAR degrees of freedom, 2020 (Vernotte et al., 2020).","title":"References"},{"category":"section","location":"tutorials/04_reading_your_data/#Reading-and-saving-your-data","page":"Reading and saving your data","text":"Measurement records arrive as plain text: Stable32 .DAT exports, counter logs, CSV dumps from a data-acquisition system. SigmaTau's IO surface for all of them is small: read_phase and read_frequency bring raw records into a PhaseData or FrequencyData container, fillgaps, remove_outliers, and detrend clean them up, and save writes data records and finished analyses to plain text files that read_phase / load_result / load_suite read back. This tutorial walks that whole path.\n\nCovered here:\n\nPhase versus frequency records, and which reader to call.\nTwo-column files with the read_phase defaults.\nSingle-column Stable32 .DAT files: header, time_col = 0, value_col = 1, and an explicit tau0.\nA counter CSV in engineering units: scaling, and the tau0 units trap.\nThe escape hatch: constructing a PhaseData by hand when no reader keyword fits.\nData preparation: fillgaps for dropouts, find_outliers / remove_outliers for glitches, detrend for offsets and drift.\nSaving and sharing: save for data records, save_result / save_suite for one deviation or a whole session, and the loaders.\n\nEverything below writes only to a temporary directory and runs as-is with julia --project=examples examples/04_reading_your_data.jl.\n\nusing SigmaTau\nusing Random\n\nRandom.seed!(20260609)","title":"Reading and saving your data"},{"category":"section","location":"tutorials/04_reading_your_data/#Phase-records-and-frequency-records","page":"Reading and saving your data","text":"A phase record xk is the time difference between the device under test and the reference, in seconds, sampled every tau_0 seconds. A fractional-frequency record yk is the dimensionless frequency offset averaged over each interval. Which one you have depends on the instrument: time-interval counters and dual-mixer time-difference systems measure phase; most frequency counters report averaged fractional frequency.\n\nCall the reader that matches the file: read_phase returns a PhaseData, read_frequency a FrequencyData, and the two take identical keyword arguments. Every deviation function accepts either container and converts internally — see the phase-data tutorial for the conversion identity.","title":"Phase records and frequency records"},{"category":"section","location":"tutorials/04_reading_your_data/#A-two-column-file:-read_phase-with-defaults","page":"Reading and saving your data","text":"The most common layout is two whitespace- or comma-separated columns: a time tag and a phase value. We synthesise one — a white-FM phase record sampled at 1 s — and write it to a temporary directory.\n\ndir = mktempdir()\n\nτ₀ = 1.0\nN  = 2048\nx  = cumsum(randn(N)) .* 1e-10        # white FM: phase is a random walk\n\ntwocol = joinpath(dir, \"twocol.DAT\")\nopen(twocol, \"w\") do io\n    for k in 1:N\n        println(io, (k - 1) * τ₀, \"  \", x[k])\n    end\nend\n\nread_phase with no keyword arguments expects exactly this layout: column 1 is time (time_col = 1), column 2 is phase in seconds (value_col = 2). The sample interval is inferred from the time column as the median of diff(t) — the median rather than the mean, so a few missing rows do not bias the inferred cadence.\n\npd = read_phase(twocol)\n\nlength(pd.x), pd.tau0\n\nThe delimiter is auto-detected from the file extension: , for .csv, tab for .tsv, whitespace for everything else. Pass delim explicitly to override.","title":"A two-column file: read_phase with defaults"},{"category":"section","location":"tutorials/04_reading_your_data/#A-single-column-file:-the-classic-stumbling-block","page":"Reading and saving your data","text":"Stable32 itself usually writes a single column of phase values, preceded by a short header. The repository carries one such file as a validation fixture — an 8192-point simulated phase record produced by Stable32's noise generator. Its header looks like this:\n\ndatpath = joinpath(pkgdir(SigmaTau), \"test\", \"fixtures\", \"validation\",\n                   \"stable32gen.DAT\")\nforeach(println, readlines(datpath)[1:10])\n\nWith no time column there is nothing to infer tau_0 from, and read_phase throws an ArgumentError rather than guessing. You must say three things: skip the header (header = 10), there is no time column (time_col = 0), and the phase values are therefore in column 1 (value_col = 1) — and supply tau0 yourself. Here the header records it on the Tau: line, so we parse it from there:\n\ntau_line = first(filter(startswith(\"Tau:\"), readlines(datpath)))\nτ₀_dat   = parse(Float64, split(tau_line)[2])\n\npd_dat = read_phase(datpath; header = 10, time_col = 0, value_col = 1,\n                    tau0 = τ₀_dat)\n\nlength(pd_dat.x), pd_dat.tau0\n\nForgetting value_col = 1 is the usual failure mode: the default value_col = 2 points past the only column and the reader raises an error instead of silently reading the wrong thing.","title":"A single-column file: the classic stumbling block"},{"category":"section","location":"tutorials/04_reading_your_data/#A-counter-CSV-in-engineering-units","page":"Reading and saving your data","text":"Counter logs often carry Modified Julian Date (MJD) time tags and phase in engineering units (nanoseconds, picoseconds). We synthesise a 600-row log sampled every 10 s, with phase in nanoseconds:\n\ncsvpath = joinpath(dir, \"counter_log.csv\")\nmjd0    = 60800.0\nτ_csv   = 10.0\nM       = 600\nxns     = cumsum(randn(M)) .* 0.05    # phase in ns\n\nopen(csvpath, \"w\") do io\n    println(io, \"mjd,phase_ns\")\n    for k in 1:M\n        println(io, mjd0 + (k - 1) * τ_csv / 86400, ',', xns[k])\n    end\nend\n\nread_phase handles this directly: header = 1 skips the column-name row, and scaling = 1e-9 multiplies the values into seconds. The one thing the reader cannot do for you is convert time units. The MJD column is in days, so the inferred cadence comes out in days too:\n\nread_phase(csvpath; header = 1, scaling = 1e-9).tau0   # ≈ 1.157e-4 — days!\n\nThat call succeeds silently, and every τ in your results would be wrong by a factor of 86400. Whenever the time column is not in seconds, pass tau0 in seconds explicitly — it overrides the inference:\n\npd_csv = read_phase(csvpath; header = 1, scaling = 1e-9, tau0 = 10.0)\n\nlength(pd_csv.x), pd_csv.tau0","title":"A counter CSV in engineering units"},{"category":"section","location":"tutorials/04_reading_your_data/#The-escape-hatch:-constructing-the-container-by-hand","page":"Reading and saving your data","text":"read_phase requires the whole table to parse as numbers. A log with a text column — a lock-status flag, say — defeats it, and no keyword combination will help:\n\nlogpath = joinpath(dir, \"counter_status.log\")\nopen(logpath, \"w\") do io\n    for k in 1:M\n        println(io, mjd0 + (k - 1) * τ_csv / 86400, \"  OK  \", xns[k])\n    end\nend\n\nFor a genuinely foreign format like this, parse it with plain Julia and construct the PhaseData directly — the containers are ordinary structs, and everything downstream is identical no matter how the record got in:\n\nrows   = split.(readlines(logpath))\nxs     = [parse(Float64, r[3]) * 1e-9 for r in rows if r[2] == \"OK\"]\npd_log = PhaseData(xs, τ_csv)\n\npd_log.x ≈ pd_csv.x\n\nThree lines is typical. Reach for this only when the file truly fits no read_phase keyword combination; for anything column-shaped the reader is shorter and validates as it goes.","title":"The escape hatch: constructing the container by hand"},{"category":"section","location":"tutorials/04_reading_your_data/#Gaps-and-outliers:-fillgaps","page":"Reading and saving your data","text":"The deviation estimators expect an equispaced, gap-free record, and the kernels do not skip NaN samples — a single NaN propagates into every σ_y(τ) estimate whose sums touch it:\n\nxg = copy(x)                  # the 2048-point record from above\nxg[1001:1012] .= NaN          # a 12-sample dropout\n\nadev(PhaseData(xg, τ₀), [8]; ci = false).dev\n\nfillgaps imputes the missing samples using Howe and Schlossberger's reflect-and-filter algorithm (PTTI 2009): points adjacent to each gap are mirrored, inverted, low-pass filtered, and joined with an endpoint-matched linear ramp. The point of that machinery is physical: the fill preserves the local noise character, so the σ_y(τ) curve is not distorted by the imputed segment the way a straight-line or zero fill would distort it.\n\npd_filled = fillgaps(PhaseData(xg, τ₀))\nadev(pd_filled, [8]; ci = false).dev\n\nFor raw files where dropouts appear as missing rows rather than NaN placeholders, pass fillgaps = true to read_phase / read_frequency: the record is snapped to a uniform grid (spacing = the minimum time step), absent samples become NaN, and the same fill runs. This requires a time column.\n\nThe same machinery handles outliers. find_outliers implements standard practice (Riley, Jul 2008) — the median-absolute-deviation test on the frequency record, flagging samples more than nsigma scaled MADs from the median:\n\ny      = diff(pd_filled.x) ./ τ₀\ny[700] = 3e-9                          # inject a counter glitch\nfd_glitchy = FrequencyData(y, τ₀)\n\nfind_outliers(fd_glitchy)\n\nThe scale factor 0.6745 inside the test rescales the median absolute deviation into an estimate of the standard deviation for normally distributed data, and the default nsigma = 5 is the deglitching threshold in those units (Stable32's default for its outlier check). On a PhaseData the same call tests the first differences — a phase glitch appears as a step in frequency — and flags the phase sample(s) where each step lands.\n\nremove_outliers completes the idiom: flag, set NaN, and impute with the same Howe fill as fillgaps, so the replacement preserves the noise character:\n\nfd_clean = remove_outliers(fd_glitchy)\n\nadev(fd_clean, [8]; ci = false).dev[1]","title":"Gaps and outliers: fillgaps"},{"category":"section","location":"tutorials/04_reading_your_data/#Removing-offsets-and-drift:-detrend","page":"Reading and saving your data","text":"A constant frequency offset appears as a linear trend in phase; a linear frequency drift appears as a linear trend in yk. Deterministic drift inflates the long-τ end of the σ_y(τ) curve, masking the random-walk noise floor underneath, so the convention is to remove the drift first and characterise the residual noise.\n\ndetrend does this on either container and returns a new record, leaving the original untouched. method is one of :linear (least-squares straight line, the default), :endpoint (line through the first and last samples), :mean (subtract the mean), or :none.\n\nydrift   = randn(4096) .* 1e-12 .+ 5e-16 .* (0:4095)   # white FM + drift\nfd_drift = FrequencyData(ydrift, 1.0)\nfd_flat  = detrend(fd_drift)                            # method = :linear\n\nadev(fd_drift, [512]; ci = false).dev[1],\nadev(fd_flat,  [512]; ci = false).dev[1]\n\nThe drift contribution dominated σy(512 s) before detrending and is gone after. The same operation is available at read time through the detrend keyword — here via `readfrequency, which takes the same keywords asread_phaseand returns aFrequencyData`:\n\ndriftpath = joinpath(dir, \"drift.txt\")\nwrite(driftpath, join(ydrift, '\\n'))\n\nfd_flat2 = read_frequency(driftpath; time_col = 0, value_col = 1, tau0 = 1.0,\n                          detrend = :linear)\nfd_flat2.y ≈ fd_flat.y\n\nOne caution: the detrend modes fit at most a straight line. Applied to phase data, :linear removes a constant frequency offset — not a frequency drift. To remove a linear frequency drift, detrend the frequency representation (detrend(FrequencyData(diff(pd.x) ./ pd.tau0, pd.tau0))).","title":"Removing offsets and drift: detrend"},{"category":"section","location":"tutorials/04_reading_your_data/#Saving-a-data-record:-save","page":"Reading and saving your data","text":"The trip out of SigmaTau matters too: generate a reference record with noise_gen — or clean up a measured one — and hand the samples to software outside SigmaTau. save writes any PhaseData / FrequencyData as a plain two-column text file (sample time in seconds, value) behind a short comment header recording the provenance and the sample interval:\n\nwfm      = noise_gen(PhaseData, 1024, τ₀; sigma1 = Dict(0 => 1e-12))\ndatafile = joinpath(dir, \"white_fm.txt\")\nsave(datafile, wfm)\n\nforeach(println, readlines(datafile)[1:5])\n\nThe source line reads user for records constructed in the session and the originating path for records that came from read_phase / read_frequency — every record carries that provenance in its source field, and detrend, fillgaps, and remove_outliers preserve it. On the way back in, read_phase skips the comment header automatically and picks tau0 up from it, so the file round-trips with no keyword arguments at all:\n\nwfm2 = read_phase(datafile)\nwfm2.x ≈ wfm.x, wfm2.tau0 == wfm.tau0, wfm2.source == datafile","title":"Saving a data record: save"},{"category":"section","location":"tutorials/04_reading_your_data/#Saving-one-result:-save_result-and-load_result","page":"Reading and saving your data","text":"A finished analysis should leave the session as a file, not a screenshot. save_result writes a StabilityResult to a self-describing tab-delimited text file; load_result reads it back into an identical record. We run adev on the Stable32 fixture from earlier and save it:\n\nresult     = adev(pd_dat)        # default octave τ-grid, CI included\nresultpath = joinpath(dir, \"adev_results.tsv\")\nsave_result(resultpath, result)\n\nforeach(println, readlines(resultpath)[1:8])\n\nThe file is a plain table: two comment lines record the deviation type and whether confidence intervals were computed, then a header row and one row per τ with tau, dev, noise_type, ci_lower, ci_upper, edf, and neff. Any spreadsheet, gnuplot script, or Stable32 user can take it as-is; there is no binary format to escape from.\n\nload_result reconstructs the StabilityResult, confidence intervals and all:\n\nresult2 = load_result(resultpath)\nresult2.dev ≈ result.dev, result2.edf ≈ result.edf","title":"Saving one result: save_result and load_result"},{"category":"section","location":"tutorials/04_reading_your_data/#Saving-a-whole-session:-save_suite-and-load_suite","page":"Reading and saving your data","text":"stability computes several deviations from one record in a single call and returns a StabilitySuite. save_suite writes the whole session — every result plus the metadata needed to reproduce it — to one file, and load_suite reads it back. source_file is optional provenance, recorded in the header:\n\nsuite     = stability(pd_dat)    # adev, mdev, hdev, mhdev by default\nsuitepath = joinpath(dir, \"session.tsv\")\nsave_suite(suitepath, suite; source_file = \"stable32gen.DAT\")\n\nforeach(println, readlines(suitepath)[1:14])\n\nThe metadata header records the package version, a timestamp, the input kind, τ₀, the number of samples, the confidence level, and the deviation set; each deviation then follows as its own block in the same six-column table that save_result writes. Loading it back returns a suite you can index by deviation symbol:\n\nsuite2 = load_suite(suitepath)\nsuite2[:hdev].dev ≈ suite[:hdev].dev","title":"Saving a whole session: save_suite and load_suite"},{"category":"section","location":"tutorials/04_reading_your_data/#Where-to-go-next","page":"Reading and saving your data","text":"Phase data and frequency data — the two containers in detail, and the phase ↔ frequency identity.\nComputing the Allan deviation — every field of the StabilityResult you just wrote to disk.","title":"Where to go next"},{"category":"section","location":"performance/#Performance","page":"Performance","text":"This page records timings against allantools 2024.06 on the same input records and tau grids.\n\nnote: What is and isn't benchmarked here\nThe numbers below compare SigmaTau against allantools on identical input. Stable32 is a closed-source Windows application; SigmaTau is cross-validated against it for numerical agreement (see Validation), but it is not part of these speed comparisons. Treat \"faster than Stable32\" as unverified — the claim here is specifically faster than allantools.\n\nAll runs were on a 12-core Linux workstation, Julia 1.12.6 / Python 3.14.4, allantools 2024.06. Each kernel was called one-shot (a single call passing the full τ grid) through each library's public API. SigmaTau ran multithreaded (-t auto, 12 threads); allantools ran single-threaded numpy, as it has no parallel path. The modified-total kernels are where SigmaTau's threading and sliding-window reductions pay off most.","title":"Performance"},{"category":"section","location":"performance/#Synthetic,-30-realizations-N-25-000","page":"Performance","text":"White phase noise, identical single-column inputs fed to both libraries, τ grid 1, 2, 4, …, 512 (10 octave-spaced averaging factors). Mean wall time per kernel call across 30 realizations:\n\nKernel SigmaTau (mean) allantools (mean) Speedup\nadev 15 µs 393 µs 20.1×\nmdev 56 µs 1.13 ms 20.1×\nhdev 25 µs 341 µs 13.6×\ntdev 51 µs 991 µs 19.3×\ntotdev 254 µs 684 µs 2.7×\nmtotdev 30.3 ms 107.3 s 3,543×\nhtotdev 31.5 ms 114.3 s 3,632×\n\nThe cheap kernels (adev / mdev / hdev / tdev) finish in well under 100 µs, near the timer resolution. The more reliable comparison is the ratio. The largest difference is in the modified-total family: mtotdev and htotdev are roughly 3,500–3,600× faster. totdev (the non-modified total) gains a more modest 2.7×.","title":"Synthetic, 30 realizations × N = 25 000"},{"category":"section","location":"performance/#Real-record,-N-406-763","page":"Performance","text":"A real phase record (6krb25apr.txt, τ₀ = 1 s), same τ grid capped at m = 512, single one-shot call per kernel:\n\nKernel SigmaTau allantools Speedup\nadev <1 ms 9 ms 23.2×\nmdev 1 ms 21 ms 27.6×\nhdev <1 ms 15 ms 41.7×\ntdev 1 ms 21 ms 29.6×\ntotdev 5 ms 16 ms 3.2×\nmtotdev 0.469 s 1 803.6 s 3,842×\nhtotdev 0.455 s 1 928.0 s 4,233×\n\nEnd to end, the full seven-kernel sweep runs in 0.93 s versus allantools' 3 731.6 s (just over an hour) — about 4,000× on this record. The modified-total kernels dominate that total; on allantools mtotdev alone is ~30 minutes at this length, which is why the comparison caps the averaging factor at 512.","title":"Real record, N = 406 763"},{"category":"section","location":"performance/#Caveats","page":"Performance","text":"Memory is reported on different bases and isn't directly comparable: SigmaTau's figure is cumulative @timed bytes (every transient allocation, including reused buffers, ~2 MiB per heavy-kernel call), while allantools' is peak ΔRSS from a sampling poller (<300 KiB above the input array). Both indicate pressure; they answer different questions.\nSigmaTau's run-to-run variance is higher on the threaded kernels (~7 % relative std on the modified-total family from 12-thread scheduling jitter, versus <0.35 % for single-thread numpy). Distributions are symmetric with no long tail; the jitter shrinks with thread pinning.\nNumbers are from one workstation. Absolute times will vary with hardware and thread count; the ratios are the portable result.","title":"Caveats"},{"category":"section","location":"performance/#Reproducing","page":"Performance","text":"The figures above are a recorded snapshot; the dedicated benchmark harness is no longer kept in-tree. To reproduce them, time each public deviation through a warm-started one-shot call (pass the full τ grid in a single invocation) on identical single-column inputs, running SigmaTau with -t auto and allantools single-threaded — the method described at the top of this page.","title":"Reproducing"},{"category":"section","location":"theory/total_family/#Theory:-Total-Family","page":"Theory: Total Family","text":"A total estimator is one that operates on a data-extended version of the input record. The same finite-difference operator that defines its corresponding Allan-family estimator (ADEV, MDEV, HDEV, or MHDEV) is then applied without modification — only the input changes. The purpose is statistical: at long τ, an Allan-family estimator runs out of independent windows and its EDF (and confidence interval) collapse; extending the record gives every τ a comparable window count.\n\nThe four total estimators in standard use — TOTDEV, MTOTDEV, HTOTDEV, MHTOTDEV — do not share a single extension method. Each has its own scheme, varying in three dimensions: whether the extension is applied to the whole record or to per-τ subsegments, whether the segment is detrended first and how, and whether the operation is in phase or fractional-frequency. Every section below names the specific scheme for that estimator.\n\nThe cost of extension is a small noise-type-dependent bias B(α), quantified per estimator in SP1065 §5 (Riley, Jul 2008). SigmaTau applies it by default; see the bias-correction policy section below.\n\n","title":"Theory: Total Family"},{"category":"section","location":"theory/total_family/#TOTDEV-—-total-deviation","page":"Theory: Total Family","text":"Extension method. Whole-record symmetric reflection at both ends of the phase series x of length N, producing an extended sequence x* of length 3N − 4. The reflection is applied once to the full record, with no per-τ subsegmentation and no detrending. The overlapping ADEV second-difference operator then runs on x* exactly as it would on the original record.\n\nDefinition (SP1065 §5 Eq. 25 (Riley, Jul 2008); GHP99 (Greenhall et al., 1999)):\n\nmathrmTOTVAR(tau) = frac12(mtau_0)^2 (N-2)\nsum_i=2^N-1 bigl(x_i-m^* - 2x_i^* + x_i+m^*bigr)^2 \n\nThe sum index runs over the original interior positions; the x_{i±m}^* terms reach into the reflected ends when i ± m would otherwise leave the original record. Long-τ confidence improves substantially over ADEV because the second difference always has data to operate on, regardless of how close i sits to a boundary.\n\ntotdev(PhaseData(x, τ₀), τs)\n\n","title":"TOTDEV — total deviation"},{"category":"section","location":"theory/total_family/#MTOTDEV-—-modified-total-deviation","page":"Theory: Total Family","text":"Extension method. Per-subsegment, not whole-record. The estimator walks N − 3m + 1 overlapping subsegments of length 3m along the phase record. For each subsegment the algorithm:\n\nHalf-average detrends the subsegment (subtract the mean of the first half from the first half, the mean of the second half from the second half — preserves the offset between halves so the inner averaging step still sees real phase information).\nSymmetrically reflects the detrended segment at both ends to build an extended segment of length 9m (three copies — reversed, original, reversed).\nForms cumulative second differences and averages over the 6m valid window positions inside the extension.\n\nThe outer sum is over the N − 3m + 1 subsegments. This is HV99's construction (Howe and Vernotte, Dec 1999); SP1065 §5 reproduces the algorithm.\n\nmathrmMTOTVAR(tau) = frac12(mtau_0)^2 (N - 3m + 1)\nsum_n frac16m sum_j bigl(a_j+2 - 2a_j+1 + a_jbigr)^2 \n\nwhere a_j are the m-point averages of the cumsum-reflected extended segment for subsegment n, and the inner sum runs over 6m valid positions inside that extended segment.\n\nMTOTDEV inherits MDEV's WPM/FPM separation (slope −3/2 vs −1 under deviation) — phase-averaging the inner second-difference window splits the degeneracy that ADEV / TOTDEV cannot.\n\nmtotdev(PhaseData(x, τ₀), τs)\n\n","title":"MTOTDEV — modified total deviation"},{"category":"section","location":"theory/total_family/#TTOT-—-time-total-deviation","page":"Theory: Total Family","text":"TTOT (Time-Total Deviation) is the time-deviation rescaling of MTOTDEV, analogous to how TDEV rescales MDEV (Riley, Jul 2008). It gives a σ_x-units summary (seconds) of long-τ stability for a record with WPM-dominated noise, leveraging MTOTDEV's per-subsegment extension to extend the usable τ range beyond TDEV's reach on short records (Banerjee and Matsakis, 2023).\n\nsigma_xtextTTOT(tau) = fractausqrt3\nsigma_ytextMTOT(tau)\n\nThe √3 factor matches TDEV's exactly because TTOT inherits MTOTDEV's modified second-difference operator (Riley, Jul 2008).\n\nIn SigmaTau:\n\nttotdev(PhaseData(x, τ₀), τs)\n\nttotdev wraps mtotdev and rescales the centerline and CI bounds by τ / √3. The correct_bias, ci, and confidence kwargs pass through unchanged; the edf and noise_type columns are reused as-is since a time rescaling does not change the degrees of freedom.\n\n","title":"TTOT — time-total deviation"},{"category":"section","location":"theory/total_family/#HTOTDEV-—-Hadamard-total-deviation","page":"Theory: Total Family","text":"Extension method. Per-subsegment in the frequency domain. This is the first total estimator that does not operate directly on the phase record. Two-stage:\n\nPhase → frequency. Convert x to fractional frequency y[i] = (x[i+1] − x[i]) / τ₀, length N_y = N − 1.\nPer-subsegment processing. Walk N_y − 3m + 1 subsegments of length 3m along y. For each:\nHalf-average detrend the segment.\nSymmetric reflection to length 9m ([reverse; segment; reverse]).\nCumulative third differences (Hadamard kernel) summed over 6m positions.\n\nDocumented exception at m = 1: at the shortest averaging factor there is no useful reflection (the third-difference window is 3m + 1 = 4 samples; reflection would produce trivially-correlated extensions). SigmaTau falls back to ordinary HDEV at m = 1. This matches the reference HTOTDEV implementations and is not a numerical defect — it is the design of the FCS01 algorithm (Howe et al., 2001).\n\nThe construction originates in Howe 2000 (Howe et al., Nov 2000) with bias coefficients a(α) introduced in the FCS01 paper (Howe et al., 2001); Howe 2005 (Howe et al., 2005) contributed long-τ refinements.\n\nHTOTDEV inherits HDEV's drift insensitivity (the third-difference kernel annihilates linear-in-t terms) so a record with significant linear frequency drift can be totalized without first detrending the whole record — drift is absorbed into the per-subsegment half-average step.\n\nhtotdev(PhaseData(x, τ₀), τs)\n\n","title":"HTOTDEV — Hadamard total deviation"},{"category":"section","location":"theory/total_family/#MHTOTDEV-—-modified-Hadamard-total-deviation","page":"Theory: Total Family","text":"Extension method. Per-subsegment, half-average detrending — the same Greenhall extension MTOTDEV and HTOTDEV use. (MHTOTDEV has no canonical/published form; SigmaTau adopts the Greenhall methodology for consistency across the modified-total family.) Walks N − 4m + 1 subsegments of phase length 3m + 1 along x. For each:\n\nHalf-average slope removal — subtract a linear trend whose slope is the difference of the two half-segment means (the Greenhall slope estimate shared with MTOTDEV / HTOTDEV), removing the subsegment's mean frequency offset before the third-difference kernel.\nSymmetric reflection.\nCumulative third differences plus an m-point moving average (the modified-style inner averaging, parallel to how MDEV relates to ADEV).\n\nDrift handling. The kernel above is not drift-immune, unlike its parent MHDEV: the per-window detrend removes only the local frequency offset, so residual deterministic drift makes the reflected copies non-smooth at the window boundaries and the differences that span a boundary pick it back up — a record drifting at 2e-10/day over a 1e-11 WHFM floor read 6× high at T/τ ≈ 10 and 30× high at T/τ ≈ 5. Removing the drift per window does not work: the phase-domain two-parameter detrend inflates the WHPM bias to B ≈ 3.8 and collapses the χ² EDF model (R² = 0.32), and the frequency-domain TOTHVAR scheme gives B(WHPM) growing with m — both were measured and rejected. Instead, mhtotdev removes the record's global least-squares frequency drift before the kernel (remove_drift=true by default; the per-τ noise identification also runs on the drift-removed record). The least-squares fit is a linear projection, so a deterministic drift is removed exactly, and the bias/EDF coefficients below are measured with this removal in the loop — the calibration matches what the function computes. Pass remove_drift=false for data that is already detrended.\n\nmathrmMHTOTVAR(tau) = frac1(mtau_0)^2 (N - 4m + 1)\nsum_n frac16m^2 n_textavg\nsum_textwindow bigl(textthird-diff avgbigr)^2 \n\nwith the per-subsegment inner sum running over n_{\\text{avg}} valid m-point average windows of the cumulated third-difference sequence on the extended segment. The exact normalization and window counts are pinned against a direct reference implementation in test/stab/legacy_kernels.jl.\n\nMHTOTDEV is the long-τ extension of MHDEV: phase-averaged third differences with boundary extension.\n\ninfo: Original contribution\nMHTOTDEV is original to SigmaTau. There is no canonical paper for it; the construction follows HV99's modified-total methodology (Howe and Vernotte, Dec 1999) applied to the FCS01 Hadamard total (Howe et al., 2001), with the modified third-difference operator from Greenhall 1997 (Greenhall, 1997). The authoritative definition lives in the package source itself; the kernel sits at src/kernels.jl and the public wrapper at src/deviations.jl. Put differently: total forms were published for the Allan deviation (TOTDEV), the modified Allan deviation (MTOTDEV), and the Hadamard deviation (HTOTDEV). The modified Hadamard deviation had none — MHTOTDEV is that missing total form.\n\nmhtotdev(PhaseData(x, τ₀), τs)\n\nWhere it sits. Across the eight time-domain stability estimators that SigmaTau implements, MHTOTDEV is the all-three corner of the cube formed by three independent design axes:\n\nDifference order — second (Allan) vs third (Hadamard).\nInner averaging — none (standard) vs phase-averaged (modified).\nBoundary handling — none vs total-style extension.\n\nMHTOTDEV is Hadamard × modified × total, so it carries every property that distinguishes the family: drift rejection (by the automatic least-squares drift removal above) and α ∈ {−4, −3} convergence from Hadamard, WPM/FPM disambiguation from the phase-averaged inner kernel, and tight long-τ confidence from the per-subsegment extension. It is the right tool when a record has linear frequency drift, divergent low-frequency noise, ambiguity between WPM and FPM, and a record length too short to let any of the simpler estimators reach long τ with usable confidence.\n\nnote: Noise identification and drift on the other estimators\nmhtotdev feeds its noise identification the drift-removed record, so its bias correction and χ² intervals classify the underlying noise. Every other estimator's CI machinery runs on the record as given — on a strongly drifting record the identification can classify long-τ points as redder than the true noise even for drift-immune deviations like hdev/mhdev. For confidence intervals on a drifting record, remove the deterministic drift first (detrend(fd) on the frequency record, or detrend(pd; method=:quadratic) on phase), the same hygiene SP1065 recommends generally. See the drifting-clock tutorial.\n\n","title":"MHTOTDEV — modified Hadamard total deviation"},{"category":"section","location":"theory/total_family/#Summary:-extension-scheme-by-estimator","page":"Theory: Total Family","text":"Estimator Where Pre-extension detrend Domain m=1 special case\nTOTDEV whole record none phase no\nMTOTDEV per subsegment half-average (offset) phase no\nHTOTDEV per subsegment half-average (drift, on y) frequency yes — falls back to HDEV\nMHTOTDEV per subsegment half-average (offset)¹ phase no\n\n¹ Plus a record-level least-squares drift removal in the public wrapper (remove_drift=true default) — see the drift-handling note above.\n\n","title":"Summary: extension scheme by estimator"},{"category":"section","location":"theory/total_family/#Bias-correction-policy","page":"Theory: Total Family","text":"note: Bias correction default\nSigmaTau applies the published bias corrections by default (correct_bias=true) for the whole total family. Stable32's policy is per-estimator:TOTDEV: Stable32 applies the Howe/Walter B = 1 − a·τ/T correction (FLFM/RWFM only). SigmaTau's default matches it; correct_bias=false reproduces allantools' raw kernel.\nHTOTDEV: Stable32 applies the Howe 2005 correction (factors exactly 1/√(1+a)). SigmaTau's default matches Stable32 to ≤ 0.003 % on the validation fixture; correct_bias=false reproduces allantools instead.\nMTOTDEV: Stable32 reports the uncorrected estimator. SigmaTau's raw kernel matches it to ≤ 0.004 %; the default √B(α) correction (SP1065 Table 11) places SigmaTau's output 3 – 14 % below Stable32 depending on noise type. Pass correct_bias=false to reproduce Stable32 (and allantools).In short: correct_bias=false reproduces Stable32 for MTOTDEV but not for TOTDEV or HTOTDEV, where the default already matches Stable32 and correct_bias=false matches allantools. Side-by-side numbers are on Validation: Stable32.\n\n(Cite SP1065 §5 (Riley, Jul 2008) and FCS01 (Howe et al., 2001) for the a(α) / B(α) tables.)\n\n","title":"Bias correction policy"},{"category":"section","location":"theory/total_family/#Demonstration","page":"Theory: Total Family","text":"using SigmaTau, Random\nRandom.seed!(7)\n\n# Short record, comparing ADEV vs TOTVAR confidence at the largest τ\nN = 1024\nx = cumsum(randn(N))   # WFM\nτs = [1, 4, 16, 64, 256]\n\na = adev(PhaseData(x, 1.0), τs; ci=true)\nt = totdev(PhaseData(x, 1.0), τs; ci=true)\n\n# CI half-width at the largest τ — TOTVAR should be tighter\nci_half(r, i) = (r.ci[i].hi - r.ci[i].lo) / 2\nlast_τ = length(τs)\nround.((ci_half(a, last_τ), ci_half(t, last_τ)); sigdigits=3)\n\nThe TOTVAR half-width at the largest τ should be smaller than ADEV's on this short record — the data-extension is doing its job.\n\n","title":"Demonstration"},{"category":"section","location":"theory/total_family/#Implementation-notes","page":"Theory: Total Family","text":"All four total kernels live in src/kernels.jl.\nBias correction is applied in src/deviations.jl via the bias_correction helper from src/edf.jl.\nThe MHTOTDEV EDF model edf = b·(T/τ) − c and the bias B = b₀ + b₁·(τ/T) are Monte-Carlo-measured (no published form exists for MHTOTDEV); see Theory: MHTOTDEV bias and EDF.\n\n","title":"Implementation notes"},{"category":"section","location":"theory/total_family/#See-also","page":"Theory: Total Family","text":"Theory: Allan family — the corresponding non-extended estimators.\nTheory: Confidence — EDF and χ² intervals.\nValidation: Stable32 — numerical comparison and the B(α) table.\n\n","title":"See also"},{"category":"section","location":"theory/total_family/#References","page":"Theory: Total Family","text":"TOTVAR: Greenhall, Howe & Percival 1999 (Greenhall et al., 1999).\nMTOT: Howe & Vernotte 1999 (Howe and Vernotte, Dec 1999).\nHTOT: Howe 2000 (Howe et al., Nov 2000); FCS01 (Howe et al., 2001); Howe 2005 (Howe et al., 2005).\nSP1065 §5 (Riley, Jul 2008).","title":"References"},{"category":"section","location":"faq/#FAQ-and-Troubleshooting","page":"FAQ","text":"Short answers to the questions that come up most often, with pointers into the rest of the documentation. If your question is about Stable32 equivalence, start with Migrating from Stable32.","title":"FAQ and Troubleshooting"},{"category":"section","location":"faq/#I-get-\"Package-Plots-not-found\"-when-running-an-example","page":"FAQ","text":"The example scripts live in their own Julia environment. The package's runtime Project.toml deliberately does not depend on Plots or PGFPlotsX — they are visualisation-only — so a clean checkout run under --project=. fails with Package Plots not found. Run the examples under the examples project instead:\n\njulia --project=examples -e 'using Pkg; Pkg.instantiate()'   # first run only\njulia --project=examples examples/00_julia_for_metrologists.jl\n\nexamples/Project.toml carries Plots and PGFPlotsX and points at the package source via a path entry, so the scripts always run against your checkout.","title":"I get \"Package Plots not found\" when running an example"},{"category":"section","location":"faq/#How-do-I-read-a-single-column-.DAT-file?","page":"FAQ","text":"read_phase defaults to the common two-column layout (time_col=1, value_col=2), inferring the sample interval from the time column. A bare single-column .DAT has neither, so you must say so explicitly:\n\np = read_phase(\"clock.DAT\"; time_col=0, value_col=1, tau0=1.0)\n\ntime_col=0 means \"no time column\" and value_col=1 reads the lone column. tau0 is required here because with no timestamps there is nothing to infer the sample interval from; read_phase throws an ArgumentError if you omit it. read_frequency takes the same keywords.","title":"How do I read a single-column .DAT file?"},{"category":"section","location":"faq/#Why-do-my-confidence-intervals-differ-slightly-from-Stable32?","page":"FAQ","text":"The deviation values themselves agree — the core kernels are validated against Stable32 to the 5-significant-figure precision of its published outputs (see Validation and the Stable32 comparison tables). The intervals around them can differ for two reasons.\n\nFirst, SigmaTau identifies the dominant noise type independently at every τ (lag-1 ACF with a B1/R(n) fallback), and by default it quadratically detrends each decimated subseries before classifying; on a borderline record a τ point can land in a different power-law family than Stable32 assigns, which changes the EDF and hence the interval. (identify_noise(x, m_values; detrend=false) reproduces Stable32's no-detrend convention point-for-point.)\n\nSecond, for the total family above α = 0 the published EDF coefficient tables run out, and the two programs substitute different EDF models there. For the core estimators the EDF model is the same: SigmaTau uses the Greenhall–Riley spectral-sum formulas (Greenhall and Riley, 2003), which Stable32 has also used since v1.41 — wherever the two programs agree on the noise type, the interval bounds agree to within 0.05 % (see Validation: Stable32).","title":"Why do my confidence intervals differ slightly from Stable32?"},{"category":"section","location":"faq/#When-should-I-use-the-Hadamard-family-instead-of-Allan?","page":"FAQ","text":"Use hdev / mhdev / htdev when the record contains linear frequency drift, or noise redder than random-walk FM. The Hadamard kernel is a third difference of phase rather than a second difference, and the third difference annihilates a linear-in-t term in y(t) — so constant frequency drift drops out of the statistic instead of adding a spurious +τ slope, which is exactly what happens to ADEV on a drifting rubidium or cesium record (Riley, Jul 2008). The same extra difference order keeps the variance integral convergent down to α = −4, where the Allan family diverges for α ≤ −3. For a drift-free record in the usual −2 ≤ α ≤ +2 range, ADEV suffices and has slightly tighter intervals. See Theory: Allan family.","title":"When should I use the Hadamard family instead of Allan?"},{"category":"section","location":"faq/#Why-are-MHTOTDEV's-confidence-intervals-\"Monte-Carlo-calibrated\"?","page":"FAQ","text":"MHTOTDEV is novel to SigmaTau: no other library computes it and no published reference gives its bias factor or its EDF. Every other total estimator inherits a published coefficient table; MHTOTDEV has none, so SigmaTau measured both quantities by Monte Carlo against synthesized known-α noise, following the methodology of the NIST total-variance papers (Howe et al., Nov 2000). The fitted coefficients ship in the package (the EDF table and the bias_correction(…, :mhtot, …) entries), and the full procedure — estimators, fit forms, validity window, and the resulting coefficient table — is documented in Theory: MHTOTDEV bias and EDF so the calibration is reproducible.","title":"Why are MHTOTDEV's confidence intervals \"Monte-Carlo-calibrated\"?"},{"category":"section","location":"faq/#What-does-the-τ/τ-16-note-on-the-total-estimators-mean?","page":"FAQ","text":"The EDF coefficient models for the modified and Hadamard totals are stated to be valid only for τ/τ₀ ≥ 16, where τ₀ is the sample interval; Howe et al. note their Hadamard-total EDF model \"should be used only if … τ/τ₀ ≥ 16\" (Howe et al., Nov 2000), and the SP1065 MTOTDEV coefficient formula carries the same floor. Below that floor the boundary extension that defines a total estimator confers no real advantage over the plain estimator, and the coefficient fits degrade. The σ values themselves are valid at every τ — the window applies to the EDF and bias models, not the deviation. SigmaTau respects it: for MTOTDEV at m < 16 the EDF falls back to the modified-Allan Greenhall–Riley value (which matches Stable32's reported degrees of freedom there), and the MHTOTDEV coefficients were fit only over the τ/τ₀ ≥ 16 window.","title":"What does the τ/τ₀ ≥ 16 note on the total estimators mean?"},{"category":"section","location":"faq/#Which-Julia-version-do-I-need?","page":"FAQ","text":"Julia 1.11 or later. The [compat] section of Project.toml declares julia = \"1.11\", and the examples environment requires the same.","title":"Which Julia version do I need?"},{"category":"section","location":"faq/#How-do-I-choose-a-τ-grid?","page":"FAQ","text":"Three ways. Calling a deviation with data only uses the default octave-spaced grid (1, 2, 4, 8, …), the same spacing Stable32 defaults to. Passing a Vector{Int} of averaging factors gives full control (τ = m·τ₀, where m is the averaging factor and τ₀ the sample interval). Passing a TauMode selector picks a named spacing:\n\nadev(p)                            # octave (default)\nadev(p, [1, 2, 4, 8, 16, 32, 64])  # explicit averaging factors\nadev(p, Decade)                    # decade-spaced\n\nThe selectors are AllTaus, Octave, HalfOctave, QuarterOctave, Decade, and HalfDecade; tau_values(mode, N, kernel) returns the underlying integer grid. All grids are clamped to the kernel's algorithmic m-max for the record length. Octave spacing is the usual choice: denser grids (AllTaus) look smoother but adjacent points are strongly correlated, so they add little independent information.","title":"How do I choose a τ grid?"},{"category":"section","location":"tutorials/03_drifting_clock_hadamard/#Characterizing-a-drifting-clock-with-the-Hadamard-family","page":"Characterizing a drifting clock with the Hadamard family","text":"A rubidium standard or GPSDO in its first weeks after power-on has linear frequency drift on top of its white-FM noise floor — the physics package is still settling toward thermal equilibrium. Drift at the 1e-10-per-day level is common in early life. On a record like this, ADEV reports the drift, not the clock: the deterministic ramp swamps the random noise at long τ. The Hadamard family exists for exactly this record.\n\nThis tutorial:\n\nSynthesises one day of 1 Hz data — calibrated WHFM noise plus an explicit linear frequency drift.\nRuns adev and shows the drift ramp masking the noise floor.\nRuns hdev and shows the third difference removing the drift.\nIntroduces mhdev and htdev — the time-deviation analog, original to this package.\nTruncates to a three-hour record and compares mhdev against mhtotdev degrees of freedom and confidence intervals at long τ.\nCloses with a summary of which estimator to reach for.\n\nusing SigmaTau\nusing Random","title":"Characterizing a drifting clock with the Hadamard family"},{"category":"section","location":"tutorials/03_drifting_clock_hadamard/#One-day-of-a-drifting-clock","page":"Characterizing a drifting clock with the Hadamard family","text":"noise_gen synthesises a calibrated power-law mixture; here a single component, white frequency modulation (α = 0) at σy(1 s) = 1e-11 — a typical rubidium / GPSDO class. `noisegenhas no drift parameter, because deterministic drift is not noise: we add the termD \\, t` to the fractional-frequency series by hand.\n\nRandom.seed!(20260609)\n\nτ₀ = 1.0                    # 1 Hz sampling\nN  = 86_400                 # one day of data\nσ1 = 1.0e-11                # WHFM level: σ_y(τ = 1 s)\nD  = 2.0e-10 / 86_400       # drift rate, 2e-10 per day, in 1/s\n\nfd_noise = noise_gen(FrequencyData, N, τ₀; sigma1 = Dict(0 => σ1))\n\nt  = (0:N-1) .* τ₀\ny  = fd_noise.y .+ D .* t   # drift injected explicitly\nfd = FrequencyData(y, τ₀)\n\nA common octave τ-grid for every estimator in this tutorial, from 1 s out to 8192 s (≈ 2.3 h):\n\nm_values = [2^k for k in 0:13]\ntaus     = m_values .* τ₀","title":"One day of a drifting clock"},{"category":"section","location":"tutorials/03_drifting_clock_hadamard/#ADEV:-the-drift-ramp","page":"Characterizing a drifting clock with the Hadamard family","text":"For a pure linear frequency drift, the Allan deviation is\n\nsigma_y(tau) = fracDtausqrt2\n\nwhere D is the drift rate in fractional frequency per second and tau the averaging time. The second difference of phase does not annihilate a term linear in t in y(t); it passes it through as a residual proportional to tau, so the drift appears as a rising τ⁺¹ ramp. The ramp crosses the WHFM floor sigma_1 tau^-12 where the two terms are equal:\n\nτ_cross = (sqrt(2) * σ1 / D)^(2 / 3)\nprintln(\"drift overtakes the WHFM floor near τ ≈ \", round(τ_cross; digits = 1), \" s\")\n\nr_adev = adev(fd, m_values)\n\nusing Plots\n\nplot(r_adev;\n     label  = \"ADEV\",\n     xlabel = raw\"Averaging time \\(\\tau\\) (s)\",\n     ylabel = raw\"\\(\\sigma_y(\\tau)\\)\",\n     title  = \"ADEV of a drifting clock: the ramp masks the floor\",\n     legend = :bottomleft,\n     lw     = 1.5)\nplot!(taus, σ1 ./ sqrt.(taus); ls = :dash, label = raw\"WHFM floor \\(\\sigma_1\\tau^{-1/2}\\)\")\nplot!(taus, D .* taus ./ sqrt(2); ls = :dot, label = raw\"drift ramp \\(D\\tau/\\sqrt{2}\\)\")\n\nPast the crossover the curve tracks the drift line, not the clock's noise. Stable32 users handle this by detrending first (the Drift function) and re-running; the Hadamard family makes that step unnecessary.","title":"ADEV: the drift ramp"},{"category":"section","location":"tutorials/03_drifting_clock_hadamard/#HDEV:-the-third-difference-removes-the-drift","page":"Characterizing a drifting clock with the Hadamard family","text":"hdev replaces ADEV's second difference of phase with a third difference. A linear term in y(t) is quadratic in phase, and a third difference annihilates any quadratic — so constant frequency drift contributes exactly zero, with no detrending pass over the data. See the Allan-family theory page for the kernel definition.\n\nr_hdev = hdev(fd, m_values)\n\nplot(r_adev;\n     label  = \"ADEV\",\n     xlabel = raw\"Averaging time \\(\\tau\\) (s)\",\n     ylabel = raw\"\\(\\sigma_y(\\tau)\\)\",\n     title  = \"ADEV vs HDEV on the same drifting record\",\n     legend = :bottomleft,\n     lw     = 1.5)\nplot!(r_hdev; label = \"HDEV\", lw = 1.5)\nplot!(taus, σ1 ./ sqrt.(taus); ls = :dash, label = raw\"WHFM floor \\(\\sigma_1\\tau^{-1/2}\\)\")\n\nHDEV follows the true τ⁻¹/² noise floor across the whole grid. The clock under the drift is recovered without touching the data.","title":"HDEV: the third difference removes the drift"},{"category":"section","location":"tutorials/03_drifting_clock_hadamard/#MHDEV-and-HTDEV","page":"Characterizing a drifting clock with the Hadamard family","text":"mhdev adds an m-point phase average inside the third-difference window, exactly as MDEV does inside ADEV's. The averaging changes the WPM slope from −1 to −3/2 while leaving FPM at −1, so MHDEV distinguishes white-PM from flicker-PM where HDEV (like ADEV) cannot — and it keeps the third difference's drift immunity. On this WHFM-dominated record MHDEV simply parallels HDEV; its value shows on records where a drifting clock also has short-τ PM noise of ambiguous color.\n\nr_mhdev = mhdev(fd, m_values)\n\nhtdev is the time-deviation analog: it rescales MHDEV by\n\nsigma_xmathrmHT(tau) = fractausqrt103sigma_ymathrmMH(tau)\n\nwhere sigma_ymathrmMH is MHDEV; HTDEV has units of seconds (it is a σx quantity). HTDEV is to MHDEV what TDEV is to MDEV — a time-error summary built on the drift-immune third-difference kernel — and it is original to this package: SP1065, IEEE 1139-2022, and NBS-TN-1337 do not define it (see [the HTDEV section of the theory page](../theory/allanfamily.md)).\n\nr_htdev = htdev(fd, m_values)\n\nprintln(\"HTDEV is the τ/√(10/3) rescale of MHDEV — max deviation from the identity: \",\n        maximum(abs.(r_htdev.dev .- r_mhdev.dev .* r_mhdev.tau ./ sqrt(10 / 3))))\n\nA few rows of the time-error budget for this clock, drift excluded by construction:\n\nfor i in (1, 5, 9, 13)\n    println(\"τ = \", lpad(round(Int, r_htdev.tau[i]), 5), \" s   σ_x = \",\n            round(r_htdev.dev[i]; sigdigits = 3), \" s\")\nend","title":"MHDEV and HTDEV"},{"category":"section","location":"tutorials/03_drifting_clock_hadamard/#Short-records:-MHTOTDEV","page":"Characterizing a drifting clock with the Hadamard family","text":"One day was generous. Suppose the record stops after three hours — a warm-up check before a deployment, say. At the longest reachable τ, mhdev has very few independent third differences left, so its χ² confidence interval is wide. mhtotdev extends each analysis subsegment by detrended reflection (the Greenhall total-variance methodology), keeping every subsegment in play at long τ and raising the equivalent degrees of freedom. Both its bias correction (correct_bias = true by default) and its EDF model are Monte-Carlo-calibrated against synthesized known-noise — MHTOTDEV is original to this package and has no published coefficient tables — and that calibration is valid only for τ/τ₀ ≥ 16, the same floor the Hadamard-total literature quotes. See the total-family theory page and MHTOTDEV bias and EDF for the methodology.\n\nUnlike its parent mhdev, the total kernel is not drift-immune on its own — the boundary extension re-admits residual drift — so mhtotdev removes the record's least-squares frequency drift itself before analyzing (remove_drift = true, the default; per-window drift removal was evaluated in both the phase and frequency domains and measurably damages the statistic, so the global removal is the design). Since drift is deterministic, removing it discards no statistical information; it is the same practice SP1065 recommends before any total estimator. One piece of hygiene remains for the comparison below: mhdev's noise identification runs on the record as given and would classify the longest-τ points as redder than the true noise on a record drifting this strongly. Detrending the record once lets both estimators' χ² intervals classify the underlying noise:\n\nN_short  = 10_800   # three hours at 1 Hz\nfd_short = detrend(FrequencyData(y[1:N_short], τ₀))   # noise-ID hygiene for the mhdev comparison\nm_short  = [16, 32, 64, 128, 256, 512, 1024, 2048]   # all ≥ the τ/τ₀ ≥ 16 floor\n\nr_mh  = mhdev(fd_short, m_short)\nr_mht = mhtotdev(fd_short, m_short)\n\nThe octave-by-octave comparison, with the default 68.3% (1σ) confidence level:\n\nci_width(r, i) = (r.ci[i].hi - r.ci[i].lo) / r.dev[i]\n\nprintln(\"τ (s)    T/τ    edf mhdev  edf mhtotdev  rel CI mhdev  rel CI mhtotdev\")\nfor i in eachindex(m_short)\n    Ttau = (N_short - 1) * τ₀ / r_mh.tau[i]\n    println(lpad(round(Int, r_mh.tau[i]), 5), \"  \",\n            lpad(round(Ttau; digits = 1), 6), \"  \",\n            lpad(round(r_mh.edf[i];  digits = 1), 9), \"  \",\n            lpad(round(r_mht.edf[i]; digits = 1), 12), \"  \",\n            lpad(round(ci_width(r_mh,  i); digits = 2), 12), \"  \",\n            lpad(round(ci_width(r_mht, i); digits = 2), 15))\nend\n\nRead the table from the bottom up. From τ = 16 s through τ = 1024 s the extension raises the equivalent degrees of freedom by a steady ≈ 30 % and tightens the relative confidence interval accordingly. The gain collapses only at the kernel's reach limit (mhtotdev needs at least one subsegment, N − 4m + 1 ≥ 1, so τ cannot exceed T/4, where T is the record duration): at the last octave both estimators are down to an EDF of 2–3, and no extension trick manufactures much independent information that close to the record length. The relative gain is uniform, but it buys the most where the unextended interval is widest — the last usable decade, which is exactly where a warm-up check needs its answer. Plotted with CI bands:\n\nplot(r_mh;\n     label   = \"MHDEV (3 h record)\",\n     ci_band = true,\n     xlabel  = raw\"Averaging time \\(\\tau\\) (s)\",\n     ylabel  = raw\"\\(\\sigma_y(\\tau)\\)\",\n     title   = \"MHDEV vs MHTOTDEV confidence on a short record\",\n     legend  = :bottomleft,\n     lw      = 1.5)\nplot!(r_mht; label = \"MHTOTDEV (3 h record)\", ci_band = true, lw = 1.5)","title":"Short records: MHTOTDEV"},{"category":"section","location":"tutorials/03_drifting_clock_hadamard/#Which-estimator,-when","page":"Characterizing a drifting clock with the Hadamard family","text":"All four Hadamard-family estimators reject linear frequency drift. Within the family:\n\nprintln(\"\"\"\nSymptom                                              Reach for\n---------------------------------------------------  ---------\ndrift contaminates ADEV at long τ                    hdev\ndrift + ambiguous PM noise (WPM vs FPM) at short τ   mhdev\ntime-error budget (σ_x) for a drifting clock         htdev\ndrifting clock, record too short for long-τ CI       mhtotdev\n\"\"\")\n\nSymptom Reach for\nDrift contaminates ADEV at long τ hdev\nDrift plus ambiguous PM noise (WPM vs FPM) at short τ mhdev\nTime-error budget (σ_x) for a drifting clock htdev\nDrifting clock on a record too short for long-τ confidence mhtotdev\n\nFor records without drift the Allan-family counterparts (adev, mdev, tdev, mtotdev) are slightly more efficient — tighter CI per τ on the same record — so the Hadamard family is the tool for the drifting clock, not the default for every clock.","title":"Which estimator, when"},{"category":"section","location":"theory/overview/#Theory:-Overview","page":"Theory: Overview","text":"Time-domain frequency stability analysis describes how a clock's output varies on different timescales. The classical sample variance diverges for the dominant noise types in oscillator data (flicker FM, random-walk FM), so the field uses convergent finite-difference variance estimators indexed by averaging time τ. SigmaTau implements the standard family of these estimators along with their confidence intervals.","title":"Theory: Overview"},{"category":"section","location":"theory/overview/#Phase-residuals-and-fractional-frequency","page":"Theory: Overview","text":"The fractional frequency of an oscillator is its frequency error normalized by its nominal frequency,\n\ny(t) = fracf(t) - f_mathrmnomf_mathrmnom\n\nwhere f(t) is the instantaneous output frequency and f_nom the nominal (nameplate) frequency; y is dimensionless. Sampled at interval τ₀, phase and fractional frequency are two equivalent representations of the same data:\n\ny_i = fracx_i+1 - x_itau_0 qquad\nx_n = x_0 + tau_0 sum_i=0^n-1 y_i\n\nx(t) (seconds) is the time-error of the clock relative to a reference; y_i is the mean fractional frequency over the i-th interval. SigmaTau stores phase as PhaseData and frequency as FrequencyData; the deviation API accepts either, converting via prefix-sum where needed.\n\n(Cite (Banerjee and Matsakis, 2023) for modern presentation; SP1065 §4 (Riley, Jul 2008) for the conventions.)","title":"Phase residuals and fractional frequency"},{"category":"section","location":"theory/overview/#Power-law-noise-model","page":"Theory: Overview","text":"Oscillator noise is modeled as a sum of power-law processes indexed by α, the spectral exponent of the one-sided PSD S_y(f) ∝ f^α:\n\nα Noise type Symbol ADEV slope (μ_dev) MDEV slope\n+2 White PM WPM −1 −3/2\n+1 Flicker PM FPM −1 (with log term) −1\n0 White FM WFM −1/2 −1/2\n−1 Flicker FM FFM 0 0\n−2 Random-walk FM RWFM +1/2 +1/2\n−3 Flicker walk FM FWFM (Allan diverges)¹ (diverges)¹\n−4 Random run FM RRFM (Allan diverges)¹ (diverges)¹\n\n¹ The Allan family's variance integral converges only for α > −3: its transfer function rolls off as f² toward zero frequency, so the integrand behaves as f^(α+2) there and the integral is finite exactly when α + 2 > −1. The Hadamard family (HDEV / MHDEV / HTOTDEV / MHTOTDEV) replaces the second difference with a third difference, gaining another factor of f², and converges for α > −5 — in practice, down through random-run FM at α = −4 — so records with very-low-frequency power-law content should be analyzed with the Hadamard family rather than ADEV / MDEV (Greenhall, 1997).\n\nADEV's degeneracy on WPM/FPM is the historical motivation for MDEV. HDEV adds drift insensitivity by going to a third difference.\n\n(Cite SP1065 §4–5 (Riley, Jul 2008); IEEE 1139-2022 (IEEE Ultrasonics and Society, 2022).)","title":"Power-law noise model"},{"category":"section","location":"theory/overview/#Estimator-family-map","page":"Theory: Overview","text":"Estimator Difference order Phase-averaged? Boundary-extended? Drift-insensitive\nADEV 2nd no no no\nMDEV 2nd yes no no\nHDEV 3rd no no yes\nMHDEV 3rd yes no yes\nTDEV 2nd yes no no\nHTDEV 3rd yes no yes\nTOTDEV 2nd no yes no\nMTOTDEV 2nd yes yes no\nHTOTDEV 3rd no yes yes\nMHTOTDEV 3rd yes yes yes","title":"Estimator family map"},{"category":"section","location":"theory/overview/#MTIE-—-Maximum-Time-Interval-Error","page":"Theory: Overview","text":"MTIE complements the variance estimator family with a peak-to-peak phase metric: at each averaging interval τ, MTIE reports the largest phase excursion observed in any window of length τ across the entire record (Riley, Jul 2008). Where the Allan family characterizes the spread of the phase residual, MTIE characterizes the worst-case excursion — the relevant figure of merit for synchronization applications where a single large transient is operationally significant (telecom synchronization masks, GNSS holdover budgets, financial-trading timestamping). Banerjee & Matsakis frame it as the canonical time-distribution-network metric (Banerjee and Matsakis, 2023).\n\nmathrmMTIE(tau) = max_1 le i le N-m\nBiglmax_0 le k le m x_i+k - min_0 le k le m x_i+kBigr\nqquad m = tau  tau_0\n\nnote: Implementation status\nImplemented as mtie in SigmaTau (block-windowed _mtie_core kernel with allocation-free per-window peak/trough scan). Returns a StabilityResult with mtie units of seconds; no CI bounds — no published EDF model for the peak statistic.","title":"MTIE — Maximum Time Interval Error"},{"category":"section","location":"theory/overview/#Notation-used-throughout-these-pages","page":"Theory: Overview","text":"x[i]: phase residual (seconds), sample interval τ₀\ny[i]: fractional frequency, y_i = (x[i+1] − x[i]) / τ₀\nm: averaging factor; τ = m·τ₀\nN: phase sample count; M = N − 1: frequency sample count\nα: power-law exponent of S_y(f)\nμ: corresponding slope of σ²_y(τ) on log-log axes\nσ²_y(τ): fractional-frequency variance; σ_y(τ): fractional-frequency deviation. Used by ADEV / MDEV / HDEV / MHDEV.\nσ²_x(τ): time variance; σ_x(τ): time deviation, units of seconds. Used by TDEV and HTDEV.","title":"Notation used throughout these pages"},{"category":"section","location":"theory/overview/#Slope-demonstration","page":"Theory: Overview","text":"On log-log axes the slope of σ_y(τ) identifies the dominant noise type. Pure white PM and pure random-walk FM bracket the table above:\n\nusing SigmaTau, Plots, Random\nRandom.seed!(42)\n\n# Synth: 4096 samples WPM (α=2) and RWFM (α=−2) at τ₀ = 1 s\nN = 4096\nwpm  = randn(N)\nrwfm = cumsum(cumsum(randn(N)))    # double integration of white noise\n\nτs = [2^k for k in 0:9]\nr_wpm  = adev(PhaseData(wpm,  1.0), τs; ci=false)\nr_rwfm = adev(PhaseData(rwfm, 1.0), τs; ci=false)\n\nplot(r_wpm.tau, r_wpm.dev;\n     xscale=:log10, yscale=:log10, marker=:circle,\n     xlabel=raw\"\\(\\tau\\) (s)\", ylabel=raw\"\\(\\sigma_y(\\tau)\\)\",\n     label=raw\"WPM (\\(\\alpha = +2\\))\", legend=:bottomleft)\nplot!(r_rwfm.tau, r_rwfm.dev; marker=:diamond, label=raw\"RWFM (\\(\\alpha = -2\\))\")\n\n# Reference slope guides, anchored at each curve's first point.\nplot!(r_wpm.tau, r_wpm.dev[1] .* (r_wpm.tau ./ r_wpm.tau[1]) .^ -1;\n      ls=:dash, color=:gray, label=raw\"\\(\\tau^{-1}\\) guide\")\nplot!(r_rwfm.tau, r_rwfm.dev[1] .* (r_rwfm.tau ./ r_rwfm.tau[1]) .^ (1/2);\n      ls=:dot, color=:gray, label=raw\"\\(\\tau^{+1/2}\\) guide\")\n\nThe WPM record falls along the τ⁻¹ guide (μ = −1); the RWFM record rises along τ^(+1/2) (μ = +1/2). Read slopes against the guide lines, not the tick spacing — the τ grid here is octave-spaced while the ordinate ticks are decades.","title":"Slope demonstration"},{"category":"section","location":"theory/overview/#Where-to-next","page":"Theory: Overview","text":"Allan family — ADEV, MDEV, HDEV, TDEV, MHDEV, HTDEV.\nTotal family — boundary-extended estimators.\nConfidence — EDF and χ² intervals.\nNoise identification — lag-1 ACF classification.","title":"Where to next"},{"category":"section","location":"theory/overview/#References","page":"Theory: Overview","text":"NIST SP1065, Handbook of Frequency Stability Analysis, Riley & Howe\n(Riley, Jul 2008)\nIEEE Standard Definitions of Physical Quantities for Fundamental Frequency and Time Metrology, IEEE 1139-2022. (IEEE Ultrasonics and Society, 2022)\nBanerjee & Matsakis, An Introduction to Modern Timekeeping and Time Transfer, Springer 2023. (Banerjee and Matsakis, 2023)","title":"References"},{"category":"section","location":"theory/spectral/#Theory:-Spectral-densities","page":"Theory: Spectral densities","text":"The deviation families (Allan, Total) describe stability in the time domain, as a function of averaging time τ. The same noise is equivalently described in the frequency domain by a power spectral density (PSD). SigmaTau provides the three standard frequency-domain measures of IEEE Std 1139-2022 (IEEE Ultrasonics and Society, 2022): the fractional-frequency PSD S_y(f), the phase PSD S_x(f), and single-sideband phase noise ℒ(f).","title":"Theory: Spectral densities"},{"category":"section","location":"theory/spectral/#Definitions","page":"Theory: Spectral densities","text":"For a clock with phase residual x(t) (seconds) and fractional frequency y(t) = ẋ(t), the one-sided PSDs are related by the time-derivative transfer function y = ẋ ⇒ S_y(f) = (2πf)² S_x(f):\n\nS_x(f) quad mathrms^2Hz qquad\nS_y(f) = (2pi f)^2 S_x(f) quad 1mathrmHz\n\nSingle-sideband phase noise converts the phase record to radians at the carrier frequency ν₀ and reports half the phase PSD on a decibel scale (IEEE 1139-2022 §3.5):\n\nS_phi(f) = (2pi nu_0)^2 S_x(f) quad mathrmrad^2Hz qquad\nmathscrL(f) = tfrac12 S_phi(f) quad mathrmdBcHz","title":"Definitions"},{"category":"section","location":"theory/spectral/#Power-law-model","page":"Theory: Spectral densities","text":"Oscillator noise is a sum of power laws. In the fractional-frequency PSD the exponent is the SP1065 (Riley, Jul 2008) index α:\n\nS_y(f) = sum_alpha=-2^2 h_alpha f^alpha\n\nα Noise type S_y(f) S_x(f) = S_y/(2πf)²\n2 white phase (WPM) f² f⁰ (flat)\n1 flicker phase (FPM) f¹ f⁻¹\n0 white frequency (WFM) f⁰ f⁻²\n−1 flicker frequency (FFM) f⁻¹ f⁻³\n−2 random-walk freq. (RWFM) f⁻² f⁻⁴\n\nThe h_α coefficients are the same ones noise_gen takes as input, which makes synthesis and spectral estimation an exact round-trip: synthesize a known mixture, estimate the PSD, recover the slope.","title":"Power-law model"},{"category":"section","location":"theory/spectral/#Estimator:-Welch's-method","page":"Theory: Spectral densities","text":"Sy, Sx, and L estimate the PSD by Welch's method. The record is split into overlapping segments of length nperseg, each segment is mean-removed and multiplied by a window (Hann by default), a periodogram is taken with a real FFT, and the periodograms are averaged. Overlapping segments trade frequency resolution (set by nperseg) for lower variance (set by the segment count).\n\nThe normalization is the one-sided \"density\" convention: power is folded onto positive frequencies and scaled by the window power and sample rate, so the estimate is variance-preserving,\n\nsum_k S_y(f_k)Delta f approx operatornamevar(y)\n\nover the analysis band [0, f_s/2] with f_s = 1/τ₀. Longer nperseg reaches lower frequencies (f_min = f_s / nperseg) at the cost of fewer segments and a noisier estimate; the defaults (nperseg = min(N, 256), 50 % overlap) follow the common scipy convention.\n\nnote: Discrete S_x ↔ S_y identity\nSx runs Welch on the phase record and Sy on the differenced frequency record, so the continuous identity S_x = S_y/(2πf)² holds only approximately on sampled data — closely at low frequency, where the discrete integrator matches the continuous one, and degrading toward the Nyquist frequency.","title":"Estimator: Welch's method"},{"category":"section","location":"theory/spectral/#Usage","page":"Theory: Spectral densities","text":"using SigmaTau\n\np = read_phase(\"clock.DAT\"; time_col=0, value_col=1, tau0=1.0)\n\nsy = Sy(p)                       # fractional-frequency PSD, 1/Hz\nsx = Sx(p)                       # phase PSD, s²/Hz\nl  = L(p; f_carrier=10e6)        # SSB phase noise at a 10 MHz carrier, dBc/Hz\n\nsy.freq        # one-sided frequency grid (Hz)\nsy.psd         # S_y(f)\nl.psd          # ℒ(f) in dBc/Hz\n\n# Tune the Welch trade-off: a longer segment reaches lower frequencies.\nSy(p; nperseg=4096, noverlap=2048, window=:hamming)\n\nEach call returns a SpectralResult carrying the frequency grid, the spectrum, its units, and the Welch parameters used. Loading a Plots backend renders S_y / S_x on log-log axes and ℒ(f) on a dB-versus-log-frequency axis:\n\nusing Plots\nplot(Sy(p))\nplot(L(p; f_carrier=10e6))","title":"Usage"},{"category":"section","location":"tutorials/00_julia_for_metrologists/#Julia-for-metrologists-—-a-Stable32-user's-guide","page":"Julia for metrologists — a Stable32 user's guide","text":"If you've been using Stable32 to calculate Allan deviations, this page is for you. In 10 minutes you'll load phase data, compute your first adev, inspect the result, and save it to disk — without clicking a single dialog box.","title":"Julia for metrologists — a Stable32 user's guide"},{"category":"section","location":"tutorials/00_julia_for_metrologists/#Why-Julia?","page":"Julia for metrologists — a Stable32 user's guide","text":"Three things make Julia a compelling upgrade from Stable32:\n\nIt's fast without configuration. Julia compiles to native machine code. Large datasets that bring Python or MATLAB to a crawl run at C speed after the first call.\nIt's interactive like MATLAB, free like Python. The REPL gives you an instant feedback loop. No license server, no toolbar to configure.\nIt reads the same .DAT files Stable32 does. Phase records exported from your counter or measurement system work as-is.","title":"Why Julia?"},{"category":"section","location":"tutorials/00_julia_for_metrologists/#Installation-in-five-minutes","page":"Julia for metrologists — a Stable32 user's guide","text":"Step 1 — install Julia via juliaup (the recommended version manager):\n\n# macOS / Linux\ncurl -fsSL https://install.julialang.org | sh\n\n# Windows (PowerShell)\nwinget install julia -s msstore\n\nStep 2 — open a Julia REPL and install SigmaTau.jl:\n\njulia> ]   # enter package mode — the prompt changes to pkg>\npkg> add SigmaTau\npkg> <Backspace>   # return to normal julia> prompt\njulia> using SigmaTau\n\nThat's it. SigmaTau is now on your path.","title":"Installation in five minutes"},{"category":"section","location":"tutorials/00_julia_for_metrologists/#Three-ways-to-use-Julia","page":"Julia for metrologists — a Stable32 user's guide","text":"REPL (interactive) — type julia at a terminal. Results appear immediately. This is the closest thing to a Stable32 command window. Great for exploration and one-off calculations.\n\nScript — put your code in a .jl file and run it with julia myscript.jl. Useful for reproducible, documented workflows that you want to run again later.\n\nPluto.jl (reactive notebook) — a browser-based notebook where every cell reruns automatically when you change any value. Think Excel but for Julia code. Install it once with ] add Pluto and launch with:\n\nusing Pluto\nPluto.run()\n\nFor getting started, the REPL is the fastest path. The tutorials in this documentation are all runnable scripts; everything here can also be pasted directly into a Pluto cell.","title":"Three ways to use Julia"},{"category":"section","location":"tutorials/00_julia_for_metrologists/#Typing-τ,-σ,-and-friends","page":"Julia for metrologists — a Stable32 user's guide","text":"Julia identifiers can be Greek letters, and this documentation uses them the way the field does: τ for averaging time, σ for a deviation, τ₀ for the sample interval. You type them with LaTeX-style abbreviations followed by Tab. In the REPL and in VS Code (with the Julia extension), \\tau + Tab gives τ, \\sigma + Tab gives σ, and a subscript follows the same pattern: \\tau + Tab, then \\_0 + Tab gives τ₀. Pluto notebooks support the same completion.\n\nNone of this is required — every Greek identifier in SigmaTau's API has a plain-ASCII spelling (tau0, dev, …), and your own variables can be named however you like. The completion is just how you read and write the symbols comfortably when you want them.","title":"Typing τ, σ, and friends"},{"category":"section","location":"tutorials/00_julia_for_metrologists/#Loading-phase-data-from-a-file","page":"Julia for metrologists — a Stable32 user's guide","text":"Stable32 exports phase records as plain-text .DAT files: a 10-line header followed by one phase value per line (in seconds). Here is how to load one:\n\nlines = readlines(\"my_data.DAT\")\nx     = parse.(Float64, strip.(lines[11:end]))  # skip the 10-line header\npd    = PhaseData(x, 1.0)                        # 1.0 s sample interval\n\nPhaseData takes your phase vector x and the sample interval τ₀ in seconds. If you have fractional-frequency data instead of phase, use FrequencyData(y, τ₀) — the deviation functions accept both.","title":"Loading phase data from a file"},{"category":"section","location":"tutorials/00_julia_for_metrologists/#Your-first-Allan-deviation","page":"Julia for metrologists — a Stable32 user's guide","text":"Here we generate synthetic white-FM phase data and compute the overlapping Allan deviation at four averaging times:\n\nimport Random\nRandom.seed!(20260510)\nusing SigmaTau\n\npd = PhaseData(randn(512) .* 1e-9, 1.0)   # 512-point, 1 s sample interval\n\nresult = adev(pd, [1, 2, 4, 8, 16, 32])\n\nThe return value is a StabilityResult. The two most important fields:\n\nprintln(\"τ values (s): \", result.tau)\nprintln(\"ADEV values:  \", result.dev)","title":"Your first Allan deviation"},{"category":"section","location":"tutorials/00_julia_for_metrologists/#What's-inside-a-StabilityResult","page":"Julia for metrologists — a Stable32 user's guide","text":"Field Stable32 equivalent Notes\ntau τ column Averaging times in seconds\ndev σ_y(τ) column Allan deviation values\nnoise_type noise ID column e.g. :WHFM, :RWFM\nci error bars (lo, hi) 1σ confidence bounds per τ\nedf EDF column Equivalent degrees of freedom\nneff # column Number of analysis windows averaged\ndeviation_type window title :adev, :mdev, :totdev, etc.\n\nConfidence intervals are computed by default (set ci=false to skip). The noise type is identified automatically by the lag-1 autocorrelation method (NIST SP1065 §4.2). Each ci entry is a named tuple — read one bound as result.ci[1].lo, or get whole vectors with the ci_lower / ci_upper accessor functions.\n\nprintln(\"\\nNoise types:  \", result.noise_type)\nprintln(\"CI lower:     \", ci_lower(result))\nprintln(\"CI upper:     \", ci_upper(result))\nprintln(\"EDF:          \", result.edf)\nprintln(\"neff:         \", result.neff)","title":"What's inside a StabilityResult"},{"category":"section","location":"tutorials/00_julia_for_metrologists/#Overlaying-multiple-deviations-on-one-plot","page":"Julia for metrologists — a Stable32 user's guide","text":"This is something Stable32 makes awkward — overlaying ADEV and MDEV from the same dataset on a single log-log stability plot. In Julia it's two lines:\n\nusing Plots\n\nr_adev  = adev(pd,  [1, 2, 4, 8, 16, 32])\nr_mdev  = mdev(pd,  [1, 2, 4, 8, 16, 32])\nr_tdev  = tdev(pd,  [1, 2, 4, 8, 16, 32])\n\nplot(r_adev, label=\"ADEV\")     # uses the built-in plot recipe\nplot!(r_mdev, label=\"MDEV\")    # ! means \"add to the existing plot\"\nplot!(r_tdev, label=\"TDEV\")\n\nplot(result) automatically uses a log-log axis and draws shaded error bars from the ci bounds. Every additional plot!(...) call overlays on the same figure.","title":"Overlaying multiple deviations on one plot"},{"category":"section","location":"tutorials/00_julia_for_metrologists/#Saving-your-result-to-disk","page":"Julia for metrologists — a Stable32 user's guide","text":"save_result writes a tab-separated file you can open in Excel or any text editor. load_result reads it back into a StabilityResult:\n\ntmpdir = mktempdir()\npath   = joinpath(tmpdir, \"my_adev.tsv\")\n\nsave_result(path, result)\nresult2 = load_result(path)\n\nprintln(\"\\nRound-trip check — devs match: \", result2.dev ≈ result.dev)\n\nThe file format is human-readable plain text: two comment lines encode the deviation type and format version, followed by a header row and one data row per τ value.","title":"Saving your result to disk"},{"category":"section","location":"tutorials/00_julia_for_metrologists/#Where-to-go-next","page":"Julia for metrologists — a Stable32 user's guide","text":"Tutorial 01: Phase and frequency data — deep dive into PhaseData / FrequencyData construction, tau0, and the _freq_to_phase conversion that all deviation functions use internally.\nTutorial 02: Computing ADEV and reading the result — full walkthrough of adev options, the ci and confidence keyword arguments, and how StabilityResult fields map to the Stable32 output window.\nTheory: overview — the five power-law noise types, estimator families, and the mathematical framework behind SigmaTau.","title":"Where to go next"},{"category":"section","location":"reference/stab/#SigmaTau-—-Stability-analysis","page":"SigmaTau — Stability analysis","text":"","title":"SigmaTau — Stability analysis"},{"category":"section","location":"reference/stab/#Allan-family-deviations","page":"SigmaTau — Stability analysis","text":"","title":"Allan family deviations"},{"category":"section","location":"reference/stab/#Total-family-deviations","page":"SigmaTau — Stability analysis","text":"","title":"Total family deviations"},{"category":"section","location":"reference/stab/#MTIE,-TIE-rms,-and-PDEV","page":"SigmaTau — Stability analysis","text":"","title":"MTIE, TIE rms, and PDEV"},{"category":"section","location":"reference/stab/#Theo1-and-ThêoH","page":"SigmaTau — Stability analysis","text":"","title":"Theo1 and ThêoH"},{"category":"section","location":"reference/stab/#Tau-grids-and-suite-API","page":"SigmaTau — Stability analysis","text":"The exported TauMode instances are AllTaus, Octave, HalfOctave, QuarterOctave, Decade, and HalfDecade.","title":"Tau grids and suite API"},{"category":"section","location":"reference/stab/#N-cornered-hat","page":"SigmaTau — Stability analysis","text":"","title":"N-cornered hat"},{"category":"section","location":"reference/stab/#Noise-identification","page":"SigmaTau — Stability analysis","text":"","title":"Noise identification"},{"category":"section","location":"reference/stab/#Spectral-densities","page":"SigmaTau — Stability analysis","text":"","title":"Spectral densities"},{"category":"section","location":"reference/stab/#EDF,-bias,-and-confidence-intervals","page":"SigmaTau — Stability analysis","text":"","title":"EDF, bias, and confidence intervals"},{"category":"section","location":"reference/stab/#I/O-and-preprocessing","page":"SigmaTau — Stability analysis","text":"","title":"I/O and preprocessing"},{"category":"section","location":"reference/stab/#Advanced-/-research-kernels","page":"SigmaTau — Stability analysis","text":"These are not exported, but remain reachable as SigmaTau._adev_core and friends for benchmarking, parity validation against reference implementations, and research use. The underscore prefix signals that the calling convention is not stability-guaranteed across minor versions; prefer the wrappers above for application code.","title":"Advanced / research kernels"},{"category":"function","location":"reference/stab/#SigmaTau.adev","page":"SigmaTau — Stability analysis","text":"adev(data, m_values; ci, confidence)\n\n\nOverlapping Allan deviation σ_y(τ) for a phase record, per IEEE 1139-2022 §C.2. EDF for the χ²-based CI uses the truncated-sum algorithm of Greenhall & Riley 2003.\n\nm_values selects the analysis-interval factors (τ = m·τ₀). When ci=true, the result populates per-τ noise type, χ²-based confidence bounds, and equivalent degrees of freedom.\n\nExamples\n\njulia> using SigmaTau\n\njulia> p = PhaseData(collect(1.0:10.0), 1.0);\n\njulia> r = adev(p, [1, 2]; ci=false);\n\njulia> round.(r.dev; sigdigits=4)\n2-element Vector{Float64}:\n 0.0\n 0.0\n\n\n\n\n\n","title":"SigmaTau.adev"},{"category":"function","location":"reference/stab/#SigmaTau.mdev","page":"SigmaTau — Stability analysis","text":"mdev(data, m_values; ci, confidence)\n\n\nModified Allan deviation Mod σ_y(τ) for a phase record, per IEEE 1139-2022 §C.3.\n\nUses a phase-averaged second difference; better than adev at separating white-PM from flicker-PM noise.\n\nExamples\n\njulia> using SigmaTau\n\njulia> p = PhaseData(collect(1.0:10.0), 1.0);\n\njulia> r = mdev(p, [1, 2]; ci=false);\n\njulia> round.(r.dev; sigdigits=4)\n2-element Vector{Float64}:\n 0.0\n 0.0\n\n\n\n\n\n","title":"SigmaTau.mdev"},{"category":"function","location":"reference/stab/#SigmaTau.hdev","page":"SigmaTau — Stability analysis","text":"hdev(data::PhaseData, m_values::Vector{Int}; ci::Bool=true, confidence::Float64=DEFAULT_CONFIDENCE)\n\nComputes the Overlapping Hadamard Deviation for the given PhaseData.\n\n\n\n\n\n","title":"SigmaTau.hdev"},{"category":"function","location":"reference/stab/#SigmaTau.mhdev","page":"SigmaTau — Stability analysis","text":"mhdev(data::PhaseData, m_values::Vector{Int}; ci::Bool=true, confidence::Float64=DEFAULT_CONFIDENCE)\n\nComputes the Modified Hadamard Deviation for the given PhaseData.\n\n\n\n\n\n","title":"SigmaTau.mhdev"},{"category":"function","location":"reference/stab/#SigmaTau.tdev","page":"SigmaTau — Stability analysis","text":"tdev(data::PhaseData, m_values::Vector{Int}; ci::Bool=true, confidence::Float64=DEFAULT_CONFIDENCE)\n\nComputes the Time Deviation (TDEV) for the given PhaseData.\n\nTDEV has units of seconds (it is a σx quantity, not σy), defined as σ_x(τ) = (τ/√3) · σ_y,MDEV(τ). Confidence-interval bounds inherit MDEV's χ²/Gaussian limits scaled by the same τ/√3 factor.\n\n\n\n\n\n","title":"SigmaTau.tdev"},{"category":"function","location":"reference/stab/#SigmaTau.htdev","page":"SigmaTau — Stability analysis","text":"htdev(data::PhaseData, m_values::Vector{Int}; ci::Bool=true, confidence::Float64=DEFAULT_CONFIDENCE)\n\nComputes the Hadamard time deviation (HTDEV) for the given PhaseData — a third-difference time deviation, related to MHDEV by σ_x,HT(τ) = τ / √(10/3) · σ_y,MH(τ). HTDEV has units of seconds (it is a σ_x quantity); HTDEV is to MHDEV what TDEV is to MDEV. The construction is original to this package; SP1065, IEEE 1139-2022, and NBS-TN-1337 do not define it.\n\n\n\n\n\n","title":"SigmaTau.htdev"},{"category":"function","location":"reference/stab/#SigmaTau.totdev","page":"SigmaTau — Stability analysis","text":"totdev(data::PhaseData, m_values::Vector{Int}; ci::Bool=true, correct_bias::Bool=true, confidence::Float64=DEFAULT_CONFIDENCE)\n\nComputes the Total Deviation for the given PhaseData, using the canonical Howe 1995 / NIST SP1065 eqn 25 endpoint mean-flip extension (see _totdev_core).\n\ncorrect_bias=true (default) applies the SP1065 noise-type-dependent unbias correction σ_unbiased = σ_raw / √B, where B = E[TOTVAR]/AVAR is the variance-scale bias factor from bias_correction. TOTVAR is biased low for FFM and RWFM as τ approaches T, so the correction increases σ at long τ. Set correct_bias=false to return the raw kernel value (Stable32 actually applies the correction by default, contrary to older notes — verify against the build you compare with).\n\n\n\n\n\n","title":"SigmaTau.totdev"},{"category":"function","location":"reference/stab/#SigmaTau.mtotdev","page":"SigmaTau — Stability analysis","text":"mtotdev(data::PhaseData, m_values::Vector{Int}; ci::Bool=true, correct_bias::Bool=true, confidence::Float64=DEFAULT_CONFIDENCE)\n\nComputes the Modified Total Deviation for the given PhaseData, using the canonical Greenhall 2003 per-window time-reverse extension (see _mtotdev_core).\n\ncorrect_bias=true (default) applies the SP1065 Table 11 unbias correction σ_unbiased = σ_raw / √B, where B ∈ {1.06, 1.17, 1.27, 1.30, 1.31} for α ∈ {2, 1, 0, -1, -2}. MTOT is biased high, so the correction drops σ by roughly 3 – 13 %. Pass correct_bias=false to return the raw kernel — matches Stable32 and allantools, which do not apply this correction.\n\n\n\n\n\n","title":"SigmaTau.mtotdev"},{"category":"function","location":"reference/stab/#SigmaTau.ttotdev","page":"SigmaTau — Stability analysis","text":"ttotdev(data::PhaseData, m_values::Vector{Int}; ci::Bool=true, correct_bias::Bool=true, confidence::Float64=DEFAULT_CONFIDENCE)\n\nTime-Total Deviation. Wraps mtotdev and rescales by τ/√3, analogous to tdev wrapping mdev. TTOTDEV has units of seconds (it is a σ_x quantity, not σ_y) and gives a time-deviation summary of long-τ stability with MTOTDEV's per-subsegment extended window — useful for telecom / time-transfer analyses on records too short for ordinary TDEV at the τ of interest.\n\nThe correct_bias, ci, and confidence kwargs flow through to the underlying mtotdev call unchanged. Confidence-interval bounds inherit MTOTDEV's χ²/Gaussian limits scaled by the same τ / √3 factor; the EDF column is reused as-is (a time rescaling does not change the degrees of freedom).\n\n\n\n\n\n","title":"SigmaTau.ttotdev"},{"category":"function","location":"reference/stab/#SigmaTau.htotdev","page":"SigmaTau — Stability analysis","text":"htotdev(data::PhaseData, m_values::Vector{Int}; ci::Bool=true, correct_bias::Bool=true, confidence::Float64=DEFAULT_CONFIDENCE)\n\nComputes the Hadamard Total Deviation for the given PhaseData, using the canonical Greenhall 2003 per-window time-reverse extension (see _htotdev_core).\n\ncorrect_bias=true (default) applies the FCS 2001 (Howe & Tasset) Table I unbias correction σ_unbiased = σ_raw / √B, where B = 1 + a ∈ {0.995, 0.851, 0.771, 0.717, 0.679} for α ∈ {0, -1, -2, -3, -4}. HTOT is biased low (a < 0), so the correction raises σ — substantially for divergent FM. PM noises (α > 0) get B = 1 (no published model). Pass correct_bias=false to return the raw kernel value (Stable32 actually applies the correction by default — see TOTDEV docstring for the same caveat).\n\n\n\n\n\n","title":"SigmaTau.htotdev"},{"category":"function","location":"reference/stab/#SigmaTau.mhtotdev","page":"SigmaTau — Stability analysis","text":"mhtotdev(data::PhaseData, m_values::Vector{Int}; ci::Bool=true, correct_bias::Bool=true, remove_drift::Bool=true, confidence::Float64=DEFAULT_CONFIDENCE)\n\nModified Hadamard Total Deviation, using the Greenhall methodology SigmaTau adopts for this novel estimator (see _mhtotdev_core).\n\nremove_drift=true (default) removes the record's least-squares frequency drift (the quadratic phase term) before the analysis, and the per-τ noise identification runs on the drift-removed record. Unlike MHDEV, the total estimator is not drift-immune: its per-window detrend removes only the local frequency offset, so residual deterministic drift re-enters through the reflected window boundaries and reads several times high at long τ. Removing the global least-squares drift is exact for a deterministic drift (the fit is a linear projection) and discards no statistical information; it is the same practice SP1065 prescribes before TOTVAR / MTOTVAR. Per-window drift removal was evaluated and rejected — both the phase-domain and frequency-domain variants measurably damage the statistic at the PM noise types (see the \"MHTOTDEV bias and EDF\" theory page). Pass remove_drift=false for data that is already detrended.\n\ncorrect_bias=true (default) applies the Monte-Carlo-measured unbias correction σ_unbiased = σ_raw / √B, where B = b0 + b1·(τ/T) is the τ/T-linear bias from bias_correction(…, :mhtot, …); the coefficients are measured with the default drift removal in the loop, so the calibration matches what this function computes. Pass correct_bias=false for the raw kernel value. EDF uses the measured _coeff_mhtot fit. See the \"MHTOTDEV bias and EDF\" theory page for the measurement methodology.\n\n\n\n\n\n","title":"SigmaTau.mhtotdev"},{"category":"function","location":"reference/stab/#SigmaTau.mtie","page":"SigmaTau — Stability analysis","text":"mtie(data, m_values; ci, confidence)\n\n\nMaximum Time Interval Error (MTIE) for a phase record, per ITU-T G.810. For each averaging factor m, returns the largest peak-to-peak phase excursion observed in any sliding window of m+1 samples (spanning τ = m·τ₀).\n\nMTIE is a σx quantity (units of seconds), reported as a single deterministic envelope rather than a statistical σ — there is no published EDF / χ² confidence model for it, so `noisetype,ci, andedfare returned empty even whenci=true(neffis still populated).ciandconfidence` are accepted for API uniformity with the other deviations but are no-ops here.\n\nExamples\n\njulia> using SigmaTau\n\njulia> p = PhaseData([0.0, 1.0, 0.5, 2.0, 1.5], 1.0);\n\njulia> r = mtie(p, [1, 3]);\n\njulia> r.dev\n2-element Vector{Float64}:\n 1.5\n 2.0\n\n\n\n\n\n","title":"SigmaTau.mtie"},{"category":"function","location":"reference/stab/#SigmaTau.tierms","page":"SigmaTau — Stability analysis","text":"tierms(data, m_values; ci, confidence)\n\n\nRMS Time Interval Error (TIE rms) for a phase record, per NIST SP1065 §5.2.18 eq. 37 (Riley, Jul 2008). For each averaging factor m, returns the root-mean-square phase change over all N − m spans of τ = m·τ₀:\n\nmathrmTIErms(mtau_0) = sqrtfrac1N-m\n    sum_i=1^N-m bigl(x_i+m - x_ibigr)^2\n\nThis is the estimator Stable32 and allantools (tierms) compute. TIE rms is a σ_x quantity (units of seconds) used by the telecommunications industry alongside MTIE; for a record with no frequency offset it approaches the standard deviation of the fractional-frequency fluctuations multiplied by τ, similar in behavior to TDEV.\n\nLike mtie, TIE rms has no published EDF / χ² confidence model, so noise_type, ci, and edf are returned empty even when ci=true (neff carries the N − m span count). ci and confidence are accepted for API uniformity with the other deviations but are no-ops here.\n\nExamples\n\njulia> using SigmaTau\n\njulia> p = PhaseData([0.0, 1.0, 0.5, 2.0, 1.5], 1.0);\n\njulia> r = tierms(p, [1, 3]);\n\njulia> round.(r.dev; sigdigits=4)\n2-element Vector{Float64}:\n 0.9682\n 1.458\n\n\n\n\n\n","title":"SigmaTau.tierms"},{"category":"function","location":"reference/stab/#SigmaTau.pdev","page":"SigmaTau — Stability analysis","text":"pdev(data, m_values; ci, confidence)\n\n\nParabolic deviation σ_PDEV(τ) for a phase record, per Vernotte–Lenczner– Bourgeois–Rubiola (IEEE T-UFFC 63(4), 2016) and Vernotte–Chen–Rubiola 2020. PDEV is built from a least-squares parabolic fit and is the recommended estimator when evaluating the uncertainty of an ω-averaged frequency measurement. At τ = τ₀ (i.e. m = 1) PDEV reduces to overlapping ADEV.\n\nWhen ci=true, the result populates per-τ noise type, EDF, and χ²-based confidence bounds using the PVAR EDF model of Vernotte–Chen–Rubiola 2020 (arXiv:2005.13631), ν ≈ 35 / (A(α)·(m/M) − 12·(m/M)²) with M = N − 2m. At m = 1 the EDF is ADEV's (the PVAR(τ₀) ≡ AVAR(τ₀) identity); the large-τ region (m ≳ N/4) uses the paper's semi-log interpolation down to ν = 1 at m = N/2. The approximation is accurate to ≈ ±10 % except at m = 2, where it reads high. PVAR is an unbiased wavelet variance, so no bias correction is applied.\n\nExamples\n\njulia> using SigmaTau\n\njulia> p = PhaseData(collect(1.0:10.0), 1.0);\n\njulia> r = pdev(p, [1, 2]; ci=false);\n\njulia> round.(r.dev; sigdigits=4)\n2-element Vector{Float64}:\n 0.0\n 0.0\n\n\n\n\n\n","title":"SigmaTau.pdev"},{"category":"function","location":"reference/stab/#SigmaTau.theo1","page":"SigmaTau — Stability analysis","text":"theo1(data::PhaseData, m_values::Vector{Int}; ci::Bool=true, correct_bias::Bool=true, confidence::Float64=DEFAULT_CONFIDENCE)\n\nThêo1 deviation per NIST SP1065 (Riley 2008) §5.2.15 eq. 30 — a two-sample variance that mimics the Allan variance while extending the usable averaging-time range to 75 % of the record span (see _theo1_core for the kernel).\n\nConventions (SP1065 / Stable32):\n\nm_values are Thêo1 averaging factors and must be even with 2 ≤ m ≤ N − 1; other entries produce NaN rows. SP1065 states the statistic for m ≥ 10. Use tau_values(mode, N, :theo1) for an even-rounded grid.\nThe reported r.tau is the effective averaging time τ = 0.75·m·τ0 (SP1065 §5.2.15), as plotted by Stable32. allantools' theo1 computes the same deviation values (eq. 30 with the Howe & Peppler 0.75 normalization, which SP1065's printed eq. 30 omits — its own Table 2 requires it) but reports them at τ = m·τ0; SigmaTau resolves that definitional offset in favor of SP1065/Stable32, so r.dev matches allantools at the same m while r.tau is allantools' τ scaled by 0.75.\ncorrect_bias=true (default) applies the ThêoBR automatic bias removal of SP1065 §5.2.16 eq. 33: the Thêo1 variance is multiplied by the average of the AVAR(m=9+3i)/Theo1(m=12+4i) ratios over i = 0…⌊N/6 − 3⌋ (matched-τ ladders spanning the long-τ ADEV overlap region), removing the noise-dependent Thêo1 bias without explicit noise identification. ThêoBR needs N ≥ 19 samples; shorter records yield NaN rows (pass correct_bias=false for raw Thêo1, which Stable32 reports as its \"Theo1\" run).\n\nThe EDF for the χ²-based CI uses the empirical per-noise-type Thêo1 formulas of SP1065 §5.2.15 Table 3 (with r = 0.75m), for both the raw and bias-removed forms. The kernel's double sum is O(N·m) per τ — far costlier than the O(N) Allan kernels at large m.\n\n\n\n\n\n","title":"SigmaTau.theo1"},{"category":"function","location":"reference/stab/#SigmaTau.theoh","page":"SigmaTau — Stability analysis","text":"theoh(data::PhaseData, m_values::Vector{Int}; ci::Bool=true, confidence::Float64=DEFAULT_CONFIDENCE)\n\nThêoH (\"hybrid-ThêoBR\") per NIST SP1065 (Riley 2008) §5.2.16 eq. 34: a composite σ_y(τ) curve that is overlapping ADEV at short averaging times and the bias-removed Thêo1 (ThêoBR, eq. 33) beyond, extending a single continuous stability plot from τ0 out to 75 % of the record span.\n\nm_values are τ-grid factors (target τ = m·τ0, any positive integers — unlike theo1 they need not be even). Each point is assigned per eq. 34's crossover k = 20 % of the record span T = (N−1)·τ0:\n\nm·τ0 ≤ k — overlapping ADEV at factor m; reported at τ = m·τ0.\nm·τ0 > k — ThêoBR at the even Thêo1 factor nearest 4m/3 (exact whenever m is divisible by 3); reported at its true effective τ = 0.75·m_theo·τ0.\n\nr.tau therefore always holds the realized τ of each point, which on the Thêo1 segment can differ from the requested m·τ0 by up to one even-factor rounding step. The ThêoBR scaling needs N ≥ 19; shorter records yield NaN on the Thêo1 segment. CIs use the ADEV (Greenhall–Riley) EDF on the ADEV segment and the SP1065 Table 3 Thêo1 EDF on the Thêo1 segment. ThêoBR/ThêoH always carry the eq. 33 bias removal — there is no correct_bias switch (raw Thêo1 is available via theo1(...; correct_bias=false)).\n\n\n\n\n\n","title":"SigmaTau.theoh"},{"category":"type","location":"reference/stab/#SigmaTau.TauMode","page":"SigmaTau — Stability analysis","text":"TauMode\n\nSelector for the averaging-factor grid used by the public deviation API and stability. Pass an instance in place of an explicit m_values vector, e.g. adev(pd, Octave).\n\nInstances:\n\nTauMode Spacing Example grid (large m_max)\nAllTaus every integer 1, 2, 3, 4, 5, …\nOctave factor 2 1, 2, 4, 8, 16, …\nHalfOctave factor √2 1, 2, 3, 4, 6, 8, 11, 16, …\nQuarterOctave factor 2^(1/4) 1, 2, 3, 4, 5, 6, 7, 8, 10, …\nDecade factor 10 1, 10, 100, 1000, …\nHalfDecade factor √10 1, 3, 10, 32, 100, …\n\nOctave is the package default and reproduces the grid that the zero-arg convenience methods (adev(pd), …) have always used. Grids are rounded to integers, deduplicated, and clamped to each kernel's algorithmic m-max (see tau_values).\n\n\n\n\n\n","title":"SigmaTau.TauMode"},{"category":"function","location":"reference/stab/#SigmaTau.tau_values","page":"SigmaTau — Stability analysis","text":"tau_values(mode::TauMode, N::Int, kernel::Symbol) → Vector{Int}\n\nAveraging factors m (with τ = m·τ₀) for the grid mode, bounded above by kernel's algorithmic m-max for an N-sample record. The return value is the same Vector{Int} accepted by the explicit m_values form of every deviation (e.g. adev(pd, tau_values(Decade, length(pd.x), :adev))), and the deviation functions accept a TauMode directly (adev(pd, Decade)) as shorthand.\n\nNote the name reads like seconds, but the returned values are averaging factors, matching the existing m_values convention. kernel is the deviation symbol (:adev, :mdev, …) that determines the m-max clamp; see _kernel_m_max.\n\nFor :theo1 (defined at even averaging factors only, SP1065 eq. 30) odd grid entries are rounded up to the next even value, deduplicated, and clamped to the even m-max. :theoh grids are plain integers — its m-values are τ-grid factors (target τ = m·τ0) and the even-factor conversion for the Thêo1 segment happens inside theoh.\n\ntau_values(Octave, N, kernel) equals _default_m_values(N, kernel).\n\n\n\n\n\n","title":"SigmaTau.tau_values"},{"category":"constant","location":"reference/stab/#SigmaTau.DEFAULT_DEVIATIONS","page":"SigmaTau — Stability analysis","text":"Default deviation set computed by stability when devs is not given: overlapping Allan, modified Allan, overlapping Hadamard, and modified Hadamard. All four are fractional-frequency (σy) quantities, so the default suite overlays on one ordinate; time deviations (tdev, htdev) are σx quantities in seconds and must be requested explicitly.\n\n\n\n\n\n","title":"SigmaTau.DEFAULT_DEVIATIONS"},{"category":"function","location":"reference/stab/#SigmaTau.stability","page":"SigmaTau — Stability analysis","text":"stability(data::PhaseData; devs=DEFAULT_DEVIATIONS, taus=Octave,\n          ci=true, confidence=DEFAULT_CONFIDENCE, kwargs...) → StabilitySuite\n\nCompute a suite of stability deviations from one record in a single call — the batch analog of adev, mdev, …. Each symbol in devs is dispatched to its public deviation function with a per-deviation averaging-factor grid derived from taus, so every deviation gets a grid valid for its own algorithmic m-max. Returns a StabilitySuite. Also accepts FrequencyData.\n\ndevs is any iterable of deviation symbols drawn from (:adev, :mdev, :tdev, :hdev, :mhdev, :htdev, :totdev, :mtotdev, :ttotdev, :htotdev, :mhtotdev, :mtie, :tierms, :pdev, :theo1, :theoh). taus is a TauMode (e.g. Octave, Decade) or an explicit Vector{Int} of averaging factors applied to every deviation. The extra keyword argument correct_bias is forwarded only to the deviations that accept it (the total family and theo1); pdev and theoh receive ci/confidence; mtie/tierms receive only ci. Note that an explicit taus vector is passed to every deviation verbatim — :theo1 interprets it as even Thêo1 factors (odd entries give NaN rows) and :theoh as τ-grid factors, so prefer a TauMode when mixing the Theo family with others.\n\nExamples\n\njulia> using SigmaTau\n\njulia> pd = PhaseData(collect(1.0:64.0), 1.0);\n\njulia> suite = stability(pd; devs=(:adev, :hdev), taus=Octave, ci=false);\n\njulia> keys(suite)\n2-element Vector{Symbol}:\n :adev\n :hdev\n\njulia> suite[:adev].deviation_type\n:adev\n\n\n\n\n\n","title":"SigmaTau.stability"},{"category":"function","location":"reference/stab/#SigmaTau.nch","page":"SigmaTau — Stability analysis","text":"nch(pairwise::AbstractMatrix{StabilityResult}) → Vector{StabilityResult}\nnch(r_ab, r_bc, r_ca) → Vector{StabilityResult}\n\nN-cornered-hat noise separation (the classic Gray–Allan three-cornered hat at N = 3; see NIST SP1065 §10.7 (Riley, Jul 2008)). Given the pairwise deviations between every pair of N ≥ 3 clocks, estimates each clock's individual deviation under the assumption that the clock noises are statistically independent, so the measured pairwise variances satisfy s²ᵢⱼ(τ) = σ²ᵢ(τ) + σ²ⱼ(τ).\n\nWith Rᵢ = Σ_{j≠i} s²ᵢⱼ (clock i's row sum) and S = Σ_{j<k} s²ⱼₖ (the sum over all pairs), the separation is\n\nhatsigma^2_i(tau) = frac1N-2left( R_i - fracSN-1 right)\n\nDerivation: each σ²ᵢ appears in N − 1 pairs, so S = (N−1)·Σᵢσ²ᵢ; and Rᵢ = (N−1)σ²ᵢ + Σ_{j≠i}σ²ⱼ = (N−2)σ²ᵢ + Σⱼσ²ⱼ = (N−2)σ²ᵢ + S/(N−1). Solving for σ²ᵢ gives the formula above. At N = 3 it reduces to the classic three-cornered hat: R_A = s²_AB + s²_AC, S = s²_AB + s²_AC + s²_BC, so σ̂²_A = R_A − S/2 = ½(s²_AB + s²_AC − s²_BC).\n\npairwise[i, j] (for i < j; the transposed entry is also accepted — fill either triangle, or both) is the deviation computed on the difference record between clocks i and j. All entries must share the same deviation_type and the same τ grid; otherwise an ArgumentError is thrown. The 3-argument convenience form takes the three pairwise results in the cyclic order A−B, B−C, C−A and returns [A, B, C] (the orientation of each difference record does not matter).\n\nReturned conventions (one StabilityResult per clock, in clock order):\n\ndeviation_type is kept from the inputs — the statistic is still e.g. an ADEV; the hat merely separates the pairwise variances into per-clock contributions.\nNon-positive variance estimates (common when one clock dominates or the independence assumption fails) are reported as NaN in dev rather than clamped to zero — they are invalid estimates, and zeros poison log-scale plots.\nci, edf, and noise_type are empty: there is no established confidence model for the hat-separated estimates (the inversion mixes χ² variables of differing sign), so no bounds are fabricated.\nneff is the elementwise minimum over all pairwise inputs — every separated estimate uses every measured pair, so the least-averaged input bounds its quality.\n\nExamples\n\njulia> using SigmaTau\n\njulia> x1, x2, x3 = randn(256), randn(256), randn(256);\n\njulia> r(a, b) = adev(PhaseData(a .- b, 1.0), [1, 2, 4]; ci=false);\n\njulia> clocks = nch(r(x1, x2), r(x2, x3), r(x3, x1));\n\njulia> length(clocks), clocks[1].deviation_type\n(3, :adev)\n\n\n\n\n\n","title":"SigmaTau.nch"},{"category":"function","location":"reference/stab/#SigmaTau.identify_noise","page":"SigmaTau — Stability analysis","text":"identify_noise(x::Vector{Float64}, m_values::Vector{Int}; dmin::Int=0, dmax::Int=2, detrend::Bool=true) → Vector{Symbol}\n\nIdentifies dominant power-law noise type using lag-1 autocorrelation for phase data with B1-ratio fallback.\n\nThe full record always passes through a 5σ outlier filter before the per-m loop; that step is unconditional and unrelated to the polynomial detrend below.\n\ndetrend\n\nWhen true (default), each decimated-by-m subseries is quadratically detrended before the lag-1 ACF / B1 ratio is computed. Allantools' autocorr_noise_id does the same thing (detrend(x, deg=2) in ci.py) to remove a frequency offset (linear phase ramp) and drift (quadratic phase term) from the slow content of each averaging window.\n\nWhen false, the per-m polynomial fit is skipped. This matches Stable32's noise-ID convention; use it when you need α values that line up with a Stable32-generated reference fixture point-for-point.\n\n\n\n\n\n","title":"SigmaTau.identify_noise"},{"category":"function","location":"reference/stab/#SigmaTau.noise_gen","page":"SigmaTau — Stability analysis","text":"noise_gen(::Type{PhaseData},     N, tau0; sigma1=Dict(), h=Dict()) → PhaseData\nnoise_gen(::Type{FrequencyData}, N, tau0; sigma1=Dict(), h=Dict()) → FrequencyData\n\nSynthesize a length-N clock record whose fractional-frequency power spectrum is a sum of user-specified power laws S_y(f) = h_α f^α. Returns either a PhaseData (integrated to phase with sample interval tau0) or a FrequencyData (raw y sequence) depending on the first argument.\n\nα index\n\nα Noise type Phase PSD slope\n2 WPM — white phase flat\n1 FPM — flicker phase 1/f\n0 WFM — white frequency 1/f²\n-1 FFM — flicker frequency 1/f³\n-2 RWFM — random-walk freq. 1/f⁴\n\nSpecifying the mixture\n\nPass either sigma1 or h (not both):\n\nsigma1[α] = σ — target Allan deviation σy(τ=τ₀) of the α-component. Convenient when working from a clock spec sheet that quotes σy(1 s) directly.\nh[α] = h_α — fractional-frequency PSD coefficient in S_y(f) = h_α f^α. Unambiguous everywhere.\n\nFor WPM (α=2) and FPM (α=1) the SP1065 σy(τ) ↔ hα relation depends on a measurement bandwidth f_h. This function uses the Nyquist convention f_h = 1/(2τ₀) — the highest frequency representable on a record sampled at τ₀.\n\nEach per-α component is rescaled to hit its requested amplitude exactly for the drawn realization, so the empirical σ_y(τ=τ₀) of an isolated component equals the requested value to numerical precision; the natural slope of the power-law carries through to larger τ. Components from different α are statistically independent.\n\nSeed the global RNG (Random.seed!) before calling for reproducible output, or pass an AbstractRNG as rng to draw from a specific stream (e.g. for a Monte Carlo with independent per-realization seeds).\n\nExamples\n\nusing SigmaTau, Random\nRandom.seed!(0)\n\n# Composite: WFM at σ_y(1 s) = 1e-12 plus RWFM at σ_y(1 s) = 1e-14\np = noise_gen(PhaseData, 8192, 1.0; sigma1 = Dict(0 => 1e-12, -2 => 1e-14))\n\n# Same record but as fractional-frequency\ny = noise_gen(FrequencyData, 8192, 1.0; sigma1 = Dict(0 => 1e-12, -2 => 1e-14))\n\n# Specify by h_α (here pure WFM with h_0 = 2e-24 → σ_y(1 s) ≈ 1e-12)\nnoise_gen(PhaseData, 8192, 1.0; h = Dict(0 => 2e-24))\n\n\n\n\n\n","title":"SigmaTau.noise_gen"},{"category":"function","location":"reference/stab/#SigmaTau.Sy","page":"SigmaTau — Stability analysis","text":"Sy(data::FrequencyData; nperseg=nothing, noverlap=nothing, window=:hann) → SpectralResult\nSy(data::PhaseData;     nperseg=nothing, noverlap=nothing, window=:hann) → SpectralResult\n\nOne-sided power spectral density of the fractional frequency y(t), S_y(f) in units of 1/Hz (IEEE Std 1139-2022 §3.4), estimated by Welch's method at sample rate fs = 1/τ₀.\n\nPhaseData input is differenced to fractional frequency (y[k] = (x[k+1] − x[k])/τ₀) first. nperseg / noverlap default to the scipy convention (min(N, 256) with 50 % overlap); raise nperseg for finer low-frequency resolution, lower it for more averaging. window is :hann, :hamming, or :rectangular.\n\nThe estimate is variance-preserving: Σ S_y(f)·Δf ≈ var(y) over [0, fs/2].\n\n\n\n\n\n","title":"SigmaTau.Sy"},{"category":"function","location":"reference/stab/#SigmaTau.Sx","page":"SigmaTau — Stability analysis","text":"Sx(data::PhaseData;     nperseg=nothing, noverlap=nothing, window=:hann) → SpectralResult\nSx(data::FrequencyData; nperseg=nothing, noverlap=nothing, window=:hann) → SpectralResult\n\nOne-sided power spectral density of the phase (time) residual x(t), S_x(f) in units of s²/Hz (IEEE Std 1139-2022 §3.3), estimated by Welch's method at sample rate fs = 1/τ₀.\n\nFrequencyData input is integrated to phase (x[k] = τ₀·Σⱼ y[j]) first. The estimators are related by S_x(f) = S_y(f) / (2πf)²; the two agree under that identity at low frequency, where the discrete integrator matches the continuous one. Keyword arguments match Sy.\n\n\n\n\n\n","title":"SigmaTau.Sx"},{"category":"function","location":"reference/stab/#SigmaTau.L","page":"SigmaTau — Stability analysis","text":"L(data::PhaseData;     f_carrier, nperseg=nothing, noverlap=nothing, window=:hann) → SpectralResult\nL(data::FrequencyData; f_carrier, nperseg=nothing, noverlap=nothing, window=:hann) → SpectralResult\n\nSingle-sideband phase noise ℒ(f) in dBc/Hz (IEEE Std 1139-2022 §3.5).\n\nConverts the seconds-valued phase residual to phase in radians at the carrier f_carrier (Hz, required), S_φ(f) = (2π·f_carrier)²·S_x(f), and reports ℒ(f) = (1/2)·S_φ(f) on a decibel scale, 10·log₁₀(ℒ_linear). Useful for direct comparison against oscillator-vendor phase-noise plots.\n\nThe DC bin (f = 0) is dropped, since ℒ(0) is not meaningful and the dB conversion would be -Inf. Keyword arguments otherwise match Sy.\n\n\n\n\n\n","title":"SigmaTau.L"},{"category":"function","location":"reference/stab/#SigmaTau.calculate_edf","page":"SigmaTau — Stability analysis","text":"calculate_edf(method::Symbol, devs::Vector{Float64}, noises::Vector{Symbol}, m_values::Vector{Int}, taus::Vector{Float64}, N::Int, T::Float64) → Vector{Float64}\n\nEquivalent degrees of freedom for the chosen method. Uses the Greenhall–Riley spectral integrals for ADEV / MDEV / HDEV / MHDEV (via _calc_edf_core), the published EDF coefficient tables for the total-family estimators, and the SP1065 Table 3 empirical formulas for :theo1 (via _theo1_edf).\n\nWPM/FLPM fallback for TOTDEV and HTOTDEV: NIST SP1065 Table 9 and FCS 2001 publish coefficients for α ∈ {0, -1, -2} only. For α ∈ {1, 2} this routine falls back to the ADEV-style (d=2, F=m, S=m) or HDEV-style (d=3, F=m, S=m) Greenhall–Riley formula so every noise type yields a finite EDF instead of NaN. S=m matches the overlapped convention applied to the four overlapped variants (ADEV/MDEV/HDEV/MHDEV); both TOTDEV and HTOTDEV operate on a stride-1 phase record (Howe's reflected-boundary extension preserves overlap), so the overlapped EDF is the consistent choice. The substitute is pragmatic, not canonical — it is dominated by the same noise-shape contribution as ADEV/HDEV at WPM/FLPM, but is documented as a policy choice rather than a derivation.\n\n\n\n\n\n","title":"SigmaTau.calculate_edf"},{"category":"function","location":"reference/stab/#SigmaTau.bias_correction","page":"SigmaTau — Stability analysis","text":"bias_correction(noises::Vector{Symbol}, var_type::Symbol, taus::Vector{Float64}, T::Float64) → Vector{Float64}\n\nVariance-scale bias factor B(α) = E[estimator²] / true_variance in the SP1065 / FCS 2001 convention. Apply as σ_unbiased = σ_raw / √B, i.e., the caller takes the square root before dividing the deviation.\n\nB < 1 → estimator biased low (raw σ understates the truth).\nB > 1 → estimator biased high (raw σ overstates the truth).\n\nRecognised var_type values: :totvar, :mtot, :htot, :mhtot.\n\nConventions per variance type:\n\n:totvar — Howe/Walter (1994). B = 1 − a·τ/T with a = 1/(3·ln 2) for FFM, a = 0.75 for RWFM, zero elsewhere. Biased low for the divergent FM noises only.\n:mtot   — SP1065 Table 11 / Greenhall 1999. Independent of τ. Biased high (B > 1); MTOT overstates MVAR. Corroborated by Howe & Vernotte 2003 \"Generalization of the Total variance approach to the modified Allan variance\", Table 1 — 100-trial Monte Carlo with N_xmax = 16384 gives Bias = (1 − √(MTOTVAR/MVAR))·100% ranging from ~−2% (WHPM) to ~−18% (RWFM), i.e. √(MTOTVAR/MVAR) ∈ [1.02, 1.18], implying B ∈ [1.04, 1.39] — consistent with the table values below.\nThe Riley document \"Confidence Intervals and Bias Corrections for the Stable32 Variance Functions\" lists B values (0.94, 0.83, 0.73, 0.70, 0.69) that contradict both SP1065 and Howe & Vernotte 2003 in direction; treated as a doc typo and ignored. Stable32 empirically does NOT apply MTOT bias to its default output regardless of doc claims, so correct_bias=false on mtotdev matches Stable32; correct_bias=true applies the textbook-correct unbias σ ← σ/√B.\n:htot   — FCS 2001 (Howe & Tasset) Table I a column. B = 1 + a with the published a ∈ {-0.005, -0.149, -0.229, -0.283, -0.321} for α ∈ {0, -1, -2, -3, -4}. Biased low (B < 1) — HTOT understates the divergent-FM variance. B = 1 for α > 0 (no published model; matches Stable32).\n:mhtot  — measured by Monte Carlo (tools/mc_mhtotdev.jl) over the τ/τ0 ≥ 16 validity window, since MHTOTDEV is novel to SigmaTau and no external reference exists. Modeled τ/T-linearly, B = b0 + b1·(τ/T) (like :totvar): b0 ∈ {1.064, 0.984, 1.019, 1.213, 1.943} and b1 ∈ {0.017, 0.036, -0.048, -0.321, -3.588} for α ∈ {2, 1, 0, -1, -2}. MHTOTDEV is ≈ unbiased for white/flicker noise (b1 ≈ 0, B ≈ 1) and reads high for redder FM (RWFM B ≈ 1.9 at small τ, falling toward 1 as τ→T). See docs theory/mhtotdev_bias_edf.md.\n\nAnything unrecognised falls through to B = 1.\n\n\n\n\n\n","title":"SigmaTau.bias_correction"},{"category":"function","location":"reference/stab/#SigmaTau.confidence_intervals","page":"SigmaTau — Stability analysis","text":"confidence_intervals(devs::Vector{Float64}, edfs::Vector{Float64}, noises::Vector{Symbol}, N::Int, confidence::Float64) → (Vector{Float64}, Vector{Float64})\n\nReturns lower and upper confidence limits. Uses Distributions.jl for accurate χ² limits.\n\n\n\n\n\n","title":"SigmaTau.confidence_intervals"},{"category":"constant","location":"reference/stab/#SigmaTau.DEFAULT_CONFIDENCE","page":"SigmaTau — Stability analysis","text":"Package-wide default confidence factor used by every public deviation API with a χ²/EDF confidence model (adev, mdev, tdev, hdev, mhdev, htdev, totdev, mtotdev, ttotdev, htotdev, mhtotdev, pdev, theo1, theoh) when confidence is not supplied. (mtie and tierms have no published EDF model and ignore it.)\n\nSet to 0.683 (1-sigma) — the time-and-frequency stability convention used by Stable32, allantools' published error bars, and the Greenhall–Riley uncertainty papers. Override per call by passing confidence=0.95 (or any other level) explicitly.\n\n\n\n\n\n","title":"SigmaTau.DEFAULT_CONFIDENCE"},{"category":"function","location":"reference/stab/#SigmaTau.read_phase","page":"SigmaTau — Stability analysis","text":"read_phase(path; tau0=nothing, time_col=1, value_col=2, header=0,\n                 delim=nothing, scaling=1.0,\n                 detrend=:none, fillgaps=false) → PhaseData\n\nRead a phase-data file and return a PhaseData ready for stability analysis. The returned record carries path in its source field.\n\nLeading # comment lines (the header that save writes) are skipped automatically, and a # tau0: <value> entry in that header supplies the sample interval when the tau0 keyword is omitted, so files written by save(path, data) round-trip with no keywords at all.\n\nKeyword arguments\n\ntau0     — sample interval in seconds. When omitted, taken from a              # tau0: comment header if present, else auto-inferred from              the time column (median of diff(t)); required when              time_col=0 and neither fallback applies.\ntime_col — 1-based column index for timestamps (0 = no time column).\nvalue_col — 1-based column index for phase samples.\nheader   — number of non-comment header lines to skip (counted after any              leading # comment lines).\ndelim    — field delimiter (Char). Auto-detected from extension when              nothing (, for .csv, tab for .tsv, whitespace otherwise).\nscaling  — multiply phase samples by this factor (e.g. 1e-12 for ps→s).\ndetrend  — one of :none, :mean, :endpoint, :linear. See detrend.\nfillgaps — when true, equispace the record on the minimum time spacing              and impute NaNs with fillgaps (Howe's algorithm).              Requires a time column.\n\nExamples\n\npd = read_phase(\"phase.csv\")                          # 2-col CSV, auto tau0\npd = read_phase(\"p.tsv\"; detrend=:linear)             # remove linear trend\npd = read_phase(\"p.txt\"; time_col=0, tau0=1.0)        # no time column\npd = read_phase(\"g.csv\"; fillgaps=true, scaling=1e-9) # ns→s + Howe fill\n\n\n\n\n\n","title":"SigmaTau.read_phase"},{"category":"function","location":"reference/stab/#SigmaTau.read_frequency","page":"SigmaTau — Stability analysis","text":"read_frequency(path; tau0=nothing, time_col=1, value_col=2, header=0,\n                     delim=nothing, scaling=1.0,\n                     detrend=:none, fillgaps=false) → FrequencyData\n\nRead a fractional-frequency file. Same keyword arguments and comment-header handling as read_phase; only the return type differs. The returned record carries path in its source field.\n\n\n\n\n\n","title":"SigmaTau.read_frequency"},{"category":"function","location":"reference/stab/#SigmaTau.detrend","page":"SigmaTau — Stability analysis","text":"detrend(pd::PhaseData; method::Symbol=:linear) → PhaseData\ndetrend(fd::FrequencyData; method::Symbol=:linear) → FrequencyData\n\nReturn a new PhaseData / FrequencyData with a detrended sample vector. The original is left untouched.\n\nmethod is one of :linear (least-squares straight line, default), :endpoint (line through first and last samples), :mean (zero-mean), or :none (returns a copy unchanged).\n\nExamples\n\npd = PhaseData(cumsum(randn(1024)) .+ 1e-3 .* (0:1023), 1.0)\npd_clean = detrend(pd)                    # :linear default\npd_zero  = detrend(pd; method=:mean)\n\n\n\n\n\n","title":"SigmaTau.detrend"},{"category":"function","location":"reference/stab/#SigmaTau.fillgaps","page":"SigmaTau — Stability analysis","text":"fillgaps(pd::PhaseData) → PhaseData\nfillgaps(fd::FrequencyData) → FrequencyData\n\nImpute NaN samples using Howe & Schlossberger's reflect-and-FFT-filter algorithm, which preserves the local noise character and the AVAR/MDEV curves of the surrounding data. Returns a new PhaseData / FrequencyData with the same tau0. The input is treated as already equispaced — for raw (t, x) tables with irregular sampling, use read_phase with fillgaps=true.\n\nSingle-sample gaps are filled with the mean of their neighbours; multi-sample gaps are filled by reflecting sizegap points from the larger neighbouring run (or sizegap/2 from each side when neither side has enough), inverting, low-pass filtering, and adding an endpoint-matched linear ramp.\n\nReturns the input unchanged if there are no NaN samples.\n\n\n\n\n\n","title":"SigmaTau.fillgaps"},{"category":"function","location":"reference/stab/#SigmaTau.find_outliers","page":"SigmaTau — Stability analysis","text":"find_outliers(data::Union{PhaseData,FrequencyData}; nsigma::Real=5) → Vector{Int}\n\nReturn the (sorted, unique) indices of outlying samples using the Stable32-style median-absolute-deviation test (Riley, Jul 2008): a sample is an outlier when\n\n|yᵢ − median(y)| / (MAD / 0.6745) > nsigma\n\nwhere MAD = median(|y − median(y)|) and 0.6745 rescales the MAD into an estimate of the standard deviation for normally distributed data. nsigma=5 is Stable32's default deglitching threshold.\n\nFor FrequencyData the test runs directly on the samples y. For PhaseData it runs on the first differences Δᵢ = x[i+1] − x[i] (a phase glitch appears as a step in frequency), and each flagged difference Δᵢ flags phase sample i+1 — the sample where the step lands. An isolated phase glitch at x[j] therefore produces two flagged differences (Δⱼ₋₁ up, Δⱼ down) and flags samples j and j+1.\n\nNaN samples (existing gaps) are excluded from the median/MAD estimates and are never flagged. Returns an empty vector when nothing exceeds the threshold.\n\nSee also remove_outliers, fillgaps.\n\n\n\n\n\n","title":"SigmaTau.find_outliers"},{"category":"function","location":"reference/stab/#SigmaTau.remove_outliers","page":"SigmaTau — Stability analysis","text":"remove_outliers(data::Union{PhaseData,FrequencyData}; nsigma::Real=5)\n    → PhaseData | FrequencyData\n\nDetect outliers with find_outliers and return a new record with the flagged samples replaced by imputed values from fillgaps (Howe & Schlossberger's reflect-and-filter algorithm, which preserves the local noise character). The tau0 and source fields carry over; the input is left untouched. When nothing is flagged the input is returned as-is.\n\nThis is the Stable32 deglitching idiom (Riley, Jul 2008): flag samples more than nsigma scaled MADs from the median, gap them, and fill.\n\nExamples\n\nfd = read_frequency(\"counter_log.csv\")\nfd_clean = remove_outliers(fd)               # 5-sigma default\nfd_strict = remove_outliers(fd; nsigma=3)\n\nSee also find_outliers, fillgaps.\n\n\n\n\n\n","title":"SigmaTau.remove_outliers"},{"category":"function","location":"reference/stab/#SigmaTau.save","page":"SigmaTau — Stability analysis","text":"save(path::AbstractString, data::PhaseData)      → path\nsave(path::AbstractString, data::FrequencyData)  → path\nsave(path::AbstractString, r::StabilityResult)   → path\nsave(path::AbstractString, s::StabilitySuite; source_file=\"\") → path\n\nWrite a SigmaTau object to a plain text file at path. Returns path so the call can be chained.\n\nFor PhaseData / FrequencyData the file is two delimited columns — sample time (i-1)·τ₀ in seconds and the sample value — preceded by a comment header recording the provenance:\n\n# SigmaTau phase data\n# source: user\n# tau0: 1.0\n0.0     -1.7881e-10\n1.0     2.1406e-10\n…\n\nsource is \"user\" for records constructed directly from a vector and the originating file path for records that came from read_phase / read_frequency. The delimiter follows the extension convention of the readers: , for .csv, tab otherwise. read_phase / read_frequency skip the comment header automatically and pick up # tau0: when no tau0 keyword is given, so read_phase(save(path, pd)) reproduces pd exactly — and any external tool can consume the file as a plain two-column table. Typical use: generate noise with noise_gen and hand it to software outside SigmaTau.\n\nFor StabilityResult / StabilitySuite the call delegates to the tab-delimited writers (save_result / save_suite, which remain available under their original names); read those files back with load_result / load_suite.\n\nnote: Note\nFileIO.jl also exports a save function. If both packages are loaded, qualify the call as SigmaTau.save(path, data).\n\nExamples\n\npd = noise_gen(PhaseData, 4096, 1.0; sigma1=Dict(0 => 1e-12))\nsave(\"white_pm.txt\", pd)\npd2 = read_phase(\"white_pm.txt\")     # tau0 and values round-trip\n\nr = adev(pd)\nsave(\"white_pm_adev.tsv\", r)         # same file save_result writes\n\nSee also read_phase, read_frequency, load_result, load_suite.\n\n\n\n\n\n","title":"SigmaTau.save"},{"category":"function","location":"reference/stab/#SigmaTau.save_result","page":"SigmaTau — Stability analysis","text":"save_result(path::AbstractString, r::StabilityResult) → path\n\nWrite a single StabilityResult to a tab-delimited text file at path.\n\nThe file is self-describing: comment lines encode the format version, deviation type, and whether confidence intervals were computed (calc_ci), followed by a column-name header and one data row per τ value. All seven data columns are always present; the ci tuples flatten to numeric ci_lower / ci_upper columns, written as \"\" / NaN (along with noise_type and edf) when calc_ci=false. neff is always written.\n\nReturns path so the call can be chained. For a whole stability session (multiple deviations + metadata) use save_suite.\n\nsave is the canonical generic entry point — save(path, r) delegates here, and this name remains as an alias.\n\nExamples\n\npd = PhaseData(randn(256), 1.0)\nr  = adev(pd, [1, 2, 4, 8, 16])\nsave_result(\"my_adev.tsv\", r)\nr2 = load_result(\"my_adev.tsv\")\nr2.dev ≈ r.dev   # true\n\nSee also load_result, save_suite.\n\n\n\n\n\n","title":"SigmaTau.save_result"},{"category":"function","location":"reference/stab/#SigmaTau.load_result","page":"SigmaTau — Stability analysis","text":"load_result(path::AbstractString) → StabilityResult\n\nRead a single StabilityResult previously written by save_result.\n\nThe # calc_ci= header line determines whether confidence-interval fields are reconstructed; the on-disk ci_lower / ci_upper columns zip back into the ci tuple vector. When calc_ci=false, noise_type, ci, and edf are returned as empty vectors, matching the standard deviation API contract. Files written before the neff column existed load with neff filled with zeros. Files written by save_suite are rejected — use load_suite for those.\n\nSee also save_result.\n\n\n\n\n\n","title":"SigmaTau.load_result"},{"category":"function","location":"reference/stab/#SigmaTau.save_suite","page":"SigmaTau — Stability analysis","text":"save_suite(path::AbstractString, suite::StabilitySuite; source_file=\"\") → path\n\nWrite a whole analysis session (multiple deviations + session metadata) to a self-describing tab-delimited text file at path (format v2). Round-trips via load_suite.\n\nA key=value metadata header records the package version, an ISO-8601 timestamp, the (optional) source_file, the input kind/τ₀/N, the confidence level, the tau mode, and the deviation set; each result then follows as a # --- result <sym> --- block reusing the same seven-column table as save_result. Returns path.\n\nsave is the canonical generic entry point — save(path, suite) delegates here, and this name remains as an alias.\n\n\n\n\n\n","title":"SigmaTau.save_suite"},{"category":"function","location":"reference/stab/#SigmaTau.load_suite","page":"SigmaTau — Stability analysis","text":"load_suite(path::AbstractString) → StabilitySuite\n\nRead a StabilitySuite previously written by save_suite (format v2), reconstructing every result block and the session metadata (data_kind, tau0, n, confidence, tau_mode). Files written by save_result are rejected — use load_result for those.\n\n\n\n\n\n","title":"SigmaTau.load_suite"},{"category":"function","location":"reference/stab/#SigmaTau._adev_core","page":"SigmaTau — Stability analysis","text":"_adev_core(x::Vector{Float64}, m_values::Vector{Int}, tau0::Float64) → Vector{Float64}\n\nComputes the Overlapping Allan Deviation (ADEV) for a set of averaging factors m. This is a highly optimized, SIMD-vectorized loop with no allocations in the inner loop.\n\n\n\n\n\n","title":"SigmaTau._adev_core"},{"category":"function","location":"reference/stab/#SigmaTau._mdev_core","page":"SigmaTau — Stability analysis","text":"_mdev_core(x::Vector{Float64}, m_values::Vector{Int}, tau0::Float64) → Vector{Float64}\n\nComputes the Modified Allan Deviation (MDEV) using prefix sums for O(N) performance per m.\n\n\n\n\n\n","title":"SigmaTau._mdev_core"},{"category":"function","location":"reference/stab/#SigmaTau._tdev_core","page":"SigmaTau — Stability analysis","text":"_tdev_core(x::Vector{Float64}, m_values::Vector{Int}, tau0::Float64) → Vector{Float64}\n\nComputes the Time Deviation (TDEV).\n\n\n\n\n\n","title":"SigmaTau._tdev_core"},{"category":"function","location":"reference/stab/#SigmaTau._hdev_core","page":"SigmaTau — Stability analysis","text":"_hdev_core(x::Vector{Float64}, m_values::Vector{Int}, tau0::Float64) → Vector{Float64}\n\nComputes the Overlapping Hadamard Deviation (HDEV) for a set of averaging factors m. This uses a highly optimized, SIMD-vectorized loop with no allocations in the inner loop.\n\n\n\n\n\n","title":"SigmaTau._hdev_core"},{"category":"function","location":"reference/stab/#SigmaTau._mhdev_core","page":"SigmaTau — Stability analysis","text":"_mhdev_core(x::Vector{Float64}, m_values::Vector{Int}, tau0::Float64) → Vector{Float64}\n\nComputes the Modified Hadamard Deviation (MHDEV) using prefix sums for O(N) performance per m.\n\n\n\n\n\n","title":"SigmaTau._mhdev_core"},{"category":"function","location":"reference/stab/#SigmaTau._totdev_core","page":"SigmaTau — Stability analysis","text":"_totdev_core(x::Vector{Float64}, m_values::Vector{Int}, tau0::Float64) → Vector{Float64}\n\nComputes the Total Deviation (TOTDEV) for a set of averaging factors m, using the Howe 1995 / NIST SP1065 eqn 25 endpoint mean-flip extension with no per-window detrend — the canonical TOTDEV form (matches allantools).\n\n\n\n\n\n","title":"SigmaTau._totdev_core"},{"category":"function","location":"reference/stab/#SigmaTau._mtotdev_core","page":"SigmaTau — Stability analysis","text":"_mtotdev_core(x::Vector{Float64}, m_values::Vector{Int}, tau0::Float64) → Vector{Float64}\n\nComputes the Modified Total Deviation (MTOTDEV), using Greenhall 2003's per-window time-reverse extension with half-mean slope removal — the canonical MTOTDEV form.\n\n\n\n\n\n","title":"SigmaTau._mtotdev_core"},{"category":"function","location":"reference/stab/#SigmaTau._htotdev_core","page":"SigmaTau — Stability analysis","text":"_htotdev_core(x::Vector{Float64}, m_values::Vector{Int}, tau0::Float64) → Vector{Float64}\n\nComputes the Hadamard Total Deviation (HTOTDEV) on the frequency series y = diff(x) / tau0, using Greenhall 2003's per-window time-reverse extension with half-mean slope removal on y — the canonical HTOTDEV form.\n\n\n\n\n\n","title":"SigmaTau._htotdev_core"},{"category":"function","location":"reference/stab/#SigmaTau._mhtotdev_core","page":"SigmaTau — Stability analysis","text":"_mhtotdev_core(x::Vector{Float64}, m_values::Vector{Int}, tau0::Float64) → Vector{Float64}\n\nComputes the Modified Hadamard Total Deviation (MHTOTDEV).\n\nMHTOTDEV is novel to SigmaTau; no canonical/published form exists. SigmaTau adopts the Greenhall methodology — per-window half-mean slope removal + a 3× time-reverse extension on phase — by consistency with MTOTDEV and HTOTDEV.\n\nThe kernel alone is NOT drift-immune: the per-window detrend removes only the local frequency offset, so residual deterministic drift re-enters through the reflected window boundaries. Per-window drift removal was evaluated in both the phase domain (two-parameter detrend) and the frequency domain (TOTHVAR scheme + modified operator) and rejected — both measurably damage the statistic at the PM noise types (B(WHPM) ≈ 3.8 with the χ² EDF model collapsing, and B(WHPM) growing ∝ m, respectively). The public mhtotdev wrapper instead removes the record's least-squares frequency drift before calling this kernel (remove_drift=true default), which is exact for a deterministic drift and leaves the kernel's all-α statistics intact.\n\n\n\n\n\n","title":"SigmaTau._mhtotdev_core"},{"category":"function","location":"reference/stab/#SigmaTau._mtie_core","page":"SigmaTau — Stability analysis","text":"_mtie_core(x::Vector{Float64}, m_values::Vector{Int}, tau0::Float64) → Vector{Float64}\n\nMaximum Time Interval Error per ITU-T G.810. For each averaging factor m, slides a window of m+1 phase samples (spanning τ = m·τ₀) across the record and returns the largest peak-to-peak phase excursion observed in any window.\n\nUnits are seconds (a σ_x quantity, like TDEV) — MTIE is itself a phase measure, not a relative-frequency measure, so no τ rescaling is applied.\n\nImplementation\n\nMonotonic-deque sliding window: each m value runs in O(N) total work via two parallel index deques (one tracking the running window maximum, one the running minimum). Each phase sample enters and leaves each deque at most once, so overall complexity is O(N · |m_values|).\n\nThe deque is materialised as a single pre-allocated Vector{Int} of length N with explicit head/tail cursors per m, avoiding any DataStructures.jl dependency or per-step allocation.\n\n\n\n\n\n","title":"SigmaTau._mtie_core"},{"category":"function","location":"reference/stab/#SigmaTau._tierms_core","page":"SigmaTau — Stability analysis","text":"_tierms_core(x::Vector{Float64}, m_values::Vector{Int}, tau0::Float64) → Vector{Float64}\n\nRMS Time Interval Error per NIST SP1065 §5.2.18 eq. 37. For each averaging factor m, the root-mean-square of the phase change over all N − m spans of τ = m·τ₀:\n\nmathrmTIErms(mtau_0) = sqrtfrac1N-m\n    sum_i=1^N-m bigl(x_i+m - x_ibigr)^2\n\nUnits are seconds (a σ_x quantity, like MTIE/TDEV) — TIE rms is a phase measure, so no τ rescaling is applied. Matches allantools' tierms (its per-window max−min of the two samples x[i], x[i+m] is exactly |x[i+m] − x[i]|). Returns NaN where m ≥ N.\n\n\n\n\n\n","title":"SigmaTau._tierms_core"},{"category":"function","location":"reference/stab/#SigmaTau._pdev_core","page":"SigmaTau — Stability analysis","text":"_pdev_core(x::Vector{Float64}, m_values::Vector{Int}, tau0::Float64) → Vector{Float64}\n\nParabolic deviation σ_PDEV(τ) per Vernotte–Lenczner–Bourgeois–Rubiola (IEEE T-UFFC 63(4), 2016) and Vernotte 2020. Built from a least-squares parabolic fit to the phase record over each window:\n\nsigma^2_textPDEV(mtau_0) = frac72(N-2m)  m^4  (mtau_0)^2\n   sum_i=1^N-2m left\n       sum_k=0^m-1 left(fracm-12 - kright)\n       bigl(x_i+k - x_i+k+mbigr)\n   right^2\n\nFor m = 1 the parabolic weight collapses to zero, so we fall back to overlapping ADEV (the canonical PDEV(τ₀) ≡ ADEV(τ₀) identity from Vernotte 2015). For m > 1 the weighted parabolic sum is evaluated by a rolling two-sum recurrence, reducing each averaging factor from O(Nm) to O(N).\n\n\n\n\n\n","title":"SigmaTau._pdev_core"},{"category":"function","location":"reference/stab/#SigmaTau._theo1_core","page":"SigmaTau — Stability analysis","text":"_theo1_core(x::Vector{Float64}, m_values::Vector{Int}, tau0::Float64) → Vector{Float64}\n\nComputes the raw Thêo1 deviation per NIST SP1065 (Riley 2008) §5.2.15 eq. 30, with the 0.75 normalization of the original Howe & Peppler 2003 definition (used by Stable32 and allantools; SP1065's printed eq. 30 omits the 0.75, but its own Table 2 bias value a = 1.00 for white FM only holds with the factor included):\n\nmathrmTheo1(m tau_0 N) = frac1075(N-m)(mtau_0)^2\n   sum_i=1^N-m sum_delta=0^m2-1 frac1m2-delta\n   bigl(x_i - x_i-delta+m2) + (x_i+m - x_i+delta+m2)bigr^2\n\nDefined for even m with 2 ≤ m ≤ N − 1 (SP1065 states the statistic for m ≥ 10; smaller even factors are computed but carry little Theo1 benefit over ADEV). Odd or out-of-range m yield NaN. The effective averaging time of a Thêo1 value is τ = 0.75·m·τ0 (SP1065 §5.2.15).\n\nThe double sum is O((N−m)·m/2) per averaging factor — O(N·m) — unlike the O(N) Allan kernels; the largest factors dominate, so a full even-m sweep on a long record costs O(N³) overall. The loop is δ-major with a SIMD inner i-loop and no allocations; entries of m_values are processed in parallel across threads.\n\n\n\n\n\n","title":"SigmaTau._theo1_core"},{"category":"function","location":"reference/stab/#SigmaTau._theobr_ratio","page":"SigmaTau — Stability analysis","text":"_theobr_ratio(x::Vector{Float64}, tau0::Float64) → Float64\n\nThêoBR automatic bias-removal factor per NIST SP1065 (Riley 2008) §5.2.16 eq. 33: the average over i = 0…n, n = ⌊N/6 − 3⌋, of the variance ratios AVAR(m = 9+3i) / Theo1(m = 12+4i). The two factor ladders sample the same effective τ ((9+3i)·τ0 = 0.75·(12+4i)·τ0), so each term is an AVAR/Theo1 comparison at matched averaging time. Multiply a Thêo1 variance by this factor (a deviation by its square root) to get the bias-removed ThêoBR estimate.\n\nRatio terms whose AVAR has no analysis window (eq. 33's ladder reaches m ≈ N/2, where N − 2m < 1 for some record lengths) or whose Thêo1 is not finite/positive are skipped and the average is taken over the computable terms. Returns NaN when no term is computable (N < 19).\n\n\n\n\n\n","title":"SigmaTau._theobr_ratio"},{"category":"function","location":"reference/stab/#SigmaTau._kernel_m_max","page":"SigmaTau — Stability analysis","text":"_kernel_m_max(N::Int, kernel::Symbol) → Int\n\nLargest averaging factor m for which kernel's core L-check still yields at least one analysis window, given N samples. Shared by tau_values and _default_m_values.\n\nPer-kernel m-max (derived from the L/Ne guard in each _*_core):\n\nkernel m_max\n:adev, :pdev (N − 2) ÷ 2\n:totdev (N − 1) ÷ 2\n:mdev, :tdev (N − 1) ÷ 3\n:mtotdev, :ttotdev N ÷ 3\n:hdev (N − 2) ÷ 3\n:htotdev (N − 1) ÷ 3\n:mhdev, :htdev (N − 1) ÷ 4\n:mhtotdev N ÷ 4\n:mtie, :tierms N − 1\n:theo1 2·((N − 1) ÷ 2) (largest even ≤ N − 1)\n:theoh ⌊0.75 · 2·((N − 1) ÷ 2)⌋ (τ-grid units)\n\nThe ordinary and modified estimators require ≥2 analysis windows at the largest τ — a single window is one raw difference (EDF ≈ 1) — so their caps sit one step below the total family, whose subsequence extension keeps a single subsequence a valid long-τ estimate. (HTOTDEV's general branch runs on y = diff(x) of length N−1, giving (N−1) ÷ 3; MTOTDEV runs on phase directly, giving N ÷ 3.) Thêo1 is defined at even m ≤ N − 1 (SP1065 eq. 30); ThêoH's grid is in τ units (τ = m·τ0), reaching Thêo1's effective-τ limit 0.75·(N−1)·τ0.\n\nThrows ArgumentError for an unknown kernel symbol or an N too short to admit any m ≥ 1.\n\n\n\n\n\n","title":"SigmaTau._kernel_m_max"},{"category":"function","location":"reference/stab/#SigmaTau._default_m_values","page":"SigmaTau — Stability analysis","text":"_default_m_values(N::Int, kernel::Symbol) → Vector{Int}\n\nOctave-spaced default averaging-factor grid [1, 2, 4, …, 2^k] bounded above by the kernel's algorithmic m-max — the largest m for which the core L-check still yields at least one window. Used by the zero-arg convenience methods on every public deviation API (adev(pd), mdev(pd), …) so callers can skip the m_values argument.\n\nPer-kernel m-max (derived from the L/Ne guard in each _*_core):\n\nkernel m_max\n:adev, :pdev (N − 2) ÷ 2\n:totdev (N − 1) ÷ 2\n:mdev, :tdev (N − 1) ÷ 3\n:mtotdev, :ttotdev N ÷ 3\n:hdev (N − 2) ÷ 3\n:htotdev (N − 1) ÷ 3\n:mhdev, :htdev (N − 1) ÷ 4\n:mhtotdev N ÷ 4\n:mtie, :tierms N − 1\n:theo1 2·((N − 1) ÷ 2) (largest even ≤ N − 1)\n:theoh ⌊0.75 · 2·((N − 1) ÷ 2)⌋ (τ-grid units)\n\nThe ordinary and modified estimators require ≥2 analysis windows at the largest τ (a single window is one raw difference, EDF ≈ 1), so their caps sit one step below the total family, whose subsequence extension keeps a single subsequence a valid long-τ estimate. (HTOTDEV's general branch runs on y = diff(x) of length N−1, giving (N−1) ÷ 3; MTOTDEV runs on phase directly, giving N ÷ 3.) :theo1 grids are rounded to even averaging factors and deduplicated (see tau_values).\n\nThrows ArgumentError for unknown kernel symbols or N too short to admit any m ≥ 1.\n\n\n\n\n\n","title":"SigmaTau._default_m_values"},{"category":"section","location":"theory/validation/#Validation","page":"Validation","text":"Numerical validation is part of the methodological foundation: a deviation estimator is only as useful as its agreement with the established references the time-and-frequency community already trusts. SigmaTau is cross-validated against:\n\nStable32 (Riley & Howe 2008) — desktop application, the de facto industry reference for time-and-frequency stability analysis. Output precision in the public fixtures is roughly five significant figures.\nallantools (A. Wallin) — open-source Python library, second independent numerical reference. Output precision is full Float64.\nInlined legacy kernels and internal identities — implementations are pinned to the Riley & Howe 2008 and IEEE 1139-2022 definitions; bias corrections and EDF expressions follow Greenhall & Riley 2003.\n\nAgreement at the precision floor of the tightest reference is the bar. Where references disagree, the disagreement is documented and attributed to a specific definitional choice (bias-correction policy, noise-identification convention), not a defect.","title":"Validation"},{"category":"section","location":"theory/validation/#Agreement-classes","page":"Validation","text":"","title":"Agreement classes"},{"category":"section","location":"theory/validation/#Tight-agreement-(fixture-precision)","page":"Validation","text":"The primary kernels — overlapping ADEV, MDEV, TDEV, overlapping HDEV, MHDEV — agree with both Stable32 and allantools to within the precision of Stable32's published outputs (5 significant figures, rtol ≈ 5e-5). The implementations are O(N) prefix-sum forms, exact-by-construction up to floating-point round-off; against allantools, agreement holds to rtol ≈ 1e-11 (full Float64 precision).\n\nRepresentative comparison from test/fixtures/validation/stable32out/:\n\nEstimator τ (s) Stable32 allantools SigmaTau\nOADEV 1.0 1.00970e+00 1.00975e+00 1.00975e+00\nOADEV 64 1.60850e-02 1.60852e-02 1.60852e-02\nMDEV 16 1.58180e-02 1.58178e-02 1.58178e-02\nTDEV 64 9.58680e-02 9.58679e-02 9.58679e-02\nOHDEV 256 4.56720e-03 4.56718e-03 4.56718e-03\n\n(All rows for OADEV, MDEV, TDEV, and OHDEV are tabulated on Validation: Stable32.)","title":"Tight agreement (fixture precision)"},{"category":"section","location":"theory/validation/#The-total-family","page":"Validation","text":"The Total family — TOTDEV, HTOTDEV, MTOTDEV — agrees with both references once each program's bias-correction policy is accounted for. The raw kernels (correct_bias=false) match allantools to machine precision (≤ 4.4e-15 on the shipped fixture); against Stable32 the picture is per-estimator:\n\nTOTDEV: both programs apply the Howe/Walter B = 1 − a·τ/T correction for the divergent FM noises. SigmaTau's default matches Stable32's σ to ≤ 0.005 % at every τ, including the longest.\nHTOTDEV: Stable32 applies the Howe 2005 bias correction by default (factors exactly 1/√(1+a)), and so does SigmaTau — the default output matches Stable32 to ≤ 0.003 % at every matching-α τ.\nMTOTDEV: Stable32 reports the raw estimator; SigmaTau's raw kernel matches it to ≤ 0.004 % (the fixture's 5-significant-figure floor). SigmaTau's default additionally divides by √B(α) (SP1065 Table 11) to unbias toward the parent modified-Allan statistic, which places it 3 – 14 % below Stable32 depending on the identified noise type. This is a deliberate policy difference, not a kernel disagreement; pass correct_bias=false to reproduce Stable32 (and allantools).\n\nBoth corrected and raw outputs are exposed so users can reproduce either convention.","title":"The total family"},{"category":"section","location":"theory/validation/#Confidence-intervals","page":"Validation","text":"SigmaTau's χ² confidence intervals derive equivalent degrees of freedom from Greenhall & Riley 2003 — the same algorithm Stable32 has used since v1.41 (stated in that paper, p. 268). Wherever the two programs agree on the identified noise type, the 68.3 % bounds agree to ≤ 0.05 % for the core estimators. The two residuals are documented on Validation: Stable32: a noise-identification detrend convention that flips one τ on the shipped fixture (identify_noise(x, m_values; detrend=false) reproduces Stable32's α at every τ), and total-family EDF substitutions above α = 0, where the published TOTVAR/HTOT coefficient tables end.","title":"Confidence intervals"},{"category":"section","location":"theory/validation/#See-also","page":"Validation","text":"Validation: Methodology — three-way reference framing and rtol-floor policy.\nValidation: Stable32 — full per-estimator comparison tables across all τ.","title":"See also"},{"category":"section","location":"tutorials/01_phase_data/#Phase-data-and-frequency-data","page":"Phase data and frequency data","text":"SigmaTau operates on two timing-data containers: PhaseData and FrequencyData. Both wrap a regularly-sampled vector and the sample interval tau_0. Every public deviation function (adev, mdev, hdev, the total family, …) accepts either container — the package converts internally as needed.\n\nThis tutorial:\n\nSynthesizes one day of realistic clock data by hand — white PM plus white FM plus random-walk FM, the mixture behind the classic bathtub-shaped ADEV curve.\nWraps the record in a PhaseData, derives the matching FrequencyData, and walks the phase ↔ frequency identity that ties the two containers together.\nRuns adev on the record and reads off the three noise regimes.\nRegenerates an equivalent record in one call with noise_gen, the built-in synthesizer, and shows the two records agree.\n\nThe companion tutorial Computing the Allan deviation digs into every field of the result object adev returns.\n\nusing SigmaTau\nusing Random","title":"Phase data and frequency data"},{"category":"section","location":"tutorials/01_phase_data/#A-realistic-noise-mixture","page":"Phase data and frequency data","text":"A real clock record is never a single noise type. Three components cover a typical 1 Hz measurement of a good oscillator, each specified by its Allan deviation at one second, σ_y(1 s):\n\nWhite FM (WFM), σ_y(1 s) = 1e-11 — the oscillator's own noise floor; every clock has one, and 1e-11 is rubidium / GPSDO class.\nWhite PM (WPM), σ_y(1 s) = 5e-11 — fast phase jitter added by the measurement chain (counter quantization, cables) rather than by the clock itself.\nRandom-walk FM (RWFM), σ_y(1 s) = 1e-14 — slow frequency wander from temperature and aging, negligible at 1 s but growing with τ.\n\nOn log-log axes these contribute ADEV slopes of τ⁻¹ (WPM), τ^(−1/2) (WFM), and τ^(+1/2) (RWFM). The amplitudes are chosen so each slope owns a visible stretch of a one-day record: the WPM leg crosses under the WFM floor near τ = (5e-11 / 1e-11)² · 1 s = 25 s, and the RWFM rise overtakes the floor near τ = (1e-11 / 1e-14) · 1 s = 1000 s. Falling, then shallow, then rising — the bathtub.\n\nRandom.seed!(20260509)\n\nτ₀ = 1.0          # sample interval (s) — 1 Hz data\nN  = 86_400       # number of samples — one day\n\nσ1_wpm  = 5.0e-11   # WPM  σ_y(1 s) target\nσ1_wfm  = 1.0e-11   # WFM  σ_y(1 s) target\nσ1_rwfm = 1.0e-14   # RWFM σ_y(1 s) target","title":"A realistic noise mixture"},{"category":"section","location":"tutorials/01_phase_data/#Synthesis-by-hand","page":"Phase data and frequency data","text":"Each of these power laws has a textbook recipe built from randn and cumsum; the only care required is the constant that converts a σ_y(1 s) target into the white-noise amplitude that produces it.\n\nWPM is independent Gaussian samples directly in phase. Independent phase samples of rms sigma_x give sigma_y(tau_0) = sqrt3sigma_xtau_0 — the m = 1 second difference x_k+2 - 2x_k+1 + x_k of independent samples has variance 6sigma_x^2 — so the amplitude is sigma_x = sigma_1tau_0sqrt3:\n\nx_wpm = randn(N) .* (σ1_wpm * τ₀ / sqrt(3))\n\nWFM is independent Gaussian samples in fractional frequency. Independent frequency samples of rms sigma_w give sigma_y(tau_0) = sigma_w exactly, so the target is the amplitude with no conversion. Phase is the running integral of frequency, x_k = tau_0 sum_j le k y_j, which is a cumsum:\n\ny_wfm = randn(N) .* σ1_wfm\nx_wfm = cumsum(y_wfm) .* τ₀\n\nRWFM is a random walk in fractional frequency — a cumsum of white innovations, integrated once more to reach phase. A walk with per-step rms sigma_r gives sigma_y(tau_0) = sigma_rsqrt2 — at m = 1 the Allan variance of a random walk is sigma_r^22 — so the innovation amplitude is sigma_r = sqrt2sigma_1:\n\ny_rwfm = cumsum(randn(N)) .* (sqrt(2) * σ1_rwfm)\nx_rwfm = cumsum(y_rwfm) .* τ₀\n\nThe three processes are independent, so the record is their plain sum in phase:\n\nx  = x_wpm .+ x_wfm .+ x_rwfm\npd = PhaseData(x, τ₀)\n\nA PhaseData exposes the sample vector and the cadence:\n\nlength(pd.x), pd.tau0\n\nThe raw record shows the fast WPM jitter riding on the slow RWFM wander; the WFM floor between them is what adev digs out below.\n\nusing Plots\nt = (0:N-1) .* τ₀\nplot(t, pd.x;\n     xlabel = raw\"Time \\(t\\) (s)\",\n     ylabel = raw\"Phase residual \\(x(t)\\) (s)\",\n     title  = \"One day of synthetic clock phase (WPM + WFM + RWFM)\",\n     legend = false,\n     lw     = 0.8)","title":"Synthesis by hand"},{"category":"section","location":"tutorials/01_phase_data/#The-same-record-as-fractional-frequency","page":"Phase data and frequency data","text":"Fractional-frequency residuals are the first difference of phase,\n\ny_k = fracx_k+1 - x_ktau_0\n\nwhere x_k is the phase residual in seconds and y_k is dimensionless. Differencing an N-point phase record yields N − 1 frequency points. Construct a FrequencyData directly when your instrument reports frequency offsets instead of phase:\n\nfd = FrequencyData(diff(pd.x) ./ τ₀, τ₀)\n\nlength(fd.y), fd.tau0","title":"The same record as fractional frequency"},{"category":"section","location":"tutorials/01_phase_data/#Phase-frequency-round-trip","page":"Phase data and frequency data","text":"Integrating goes the other way, up to a constant: cumsum(fd.y) .* τ₀ returns x_k+1 - x_1, because the absolute phase offset was lost in the difference and cannot come back. Deviation estimators difference the data anyway, so the lost offset never enters any σ_y(τ):\n\nx_back = cumsum(fd.y) .* τ₀\nmaximum(abs.(x_back .- (pd.x[2:end] .- pd.x[1])))\n\nThis diff / cumsum pair is exactly the conversion SigmaTau runs internally (_phase_to_freq / _freq_to_phase in src/grids.jl) whenever a deviation function receives the \"other\" container, so both representations give the same answer — to within the one sample the difference drops:\n\nr_pd = adev(pd, [1, 16, 256]; ci = false)\nr_fd = adev(fd, [1, 16, 256]; ci = false)\nmaximum(abs.(r_fd.dev ./ r_pd.dev .- 1))","title":"Phase ↔ frequency round trip"},{"category":"section","location":"tutorials/01_phase_data/#The-bathtub","page":"Phase data and frequency data","text":"An octave τ-grid from 1 s to 16384 s (≈ 4.6 h) spans all three regimes. adev takes the averaging factors m; the averaging times are tau = mtau_0.\n\nm_values = [2^k for k in 0:14]\ntaus     = m_values .* τ₀\n\nr_manual = adev(pd, m_values)\n\nplot(r_manual;\n     label  = \"ADEV, manual mix\",\n     xlabel = raw\"Averaging time \\(\\tau\\) (s)\",\n     ylabel = raw\"\\(\\sigma_y(\\tau)\\)\",\n     title  = \"The WPM + WFM + RWFM bathtub\",\n     legend = :topright,\n     lw     = 1.5)\nplot!(taus[1:8],   σ1_wpm  ./ taus[1:8];          ls = :dash, label = raw\"WPM \\(\\sigma_1\\tau^{-1}\\)\")\nplot!(taus,        σ1_wfm  ./ sqrt.(taus);        ls = :dash, label = raw\"WFM \\(\\sigma_1\\tau^{-1/2}\\)\")\nplot!(taus[8:end], σ1_rwfm .* sqrt.(taus[8:end]); ls = :dash, label = raw\"RWFM \\(\\sigma_1\\tau^{1/2}\\)\")\n\nThree slopes, two knees. The curve falls along the WPM guide line, flattens onto the WFM floor past the τ ≈ 25 s crossover, and turns upward where the RWFM wander overtakes the floor near τ ≈ 1000 s. Each dashed guide is the ideal single-noise slope anchored at that component's σy(1 s) target. The error bars come from the χ² confidence machinery that runs by default — the companion tutorial [Computing the Allan deviation](02compute_adev.md) unpacks them.","title":"The bathtub"},{"category":"section","location":"tutorials/01_phase_data/#The-built-in-generator","page":"Phase data and frequency data","text":"Hand-rolling scale factors is the old-fashioned way, and it only goes so far: the two flicker noises (FPM, FFM) have no randn / cumsum construction at all. noise_gen wraps a spectral shaper that covers all five power laws through one interface — pass the target σy(1 s) per noise type, keyed by the exponent α of the fractional-frequency power spectrum ``Sy(f) = h_\\alpha f^\\alpha``:\n\nα Noise type this record\n2 WPM 5e-11\n1 FPM —\n0 WFM 1e-11\n-1 FFM —\n-2 RWFM 1e-14\n\npd_gen = noise_gen(PhaseData, N, τ₀;\n                   sigma1 = Dict(2 => σ1_wpm, 0 => σ1_wfm, -2 => σ1_rwfm))\n\nOne call replaces the whole synthesis section above. Each component is rescaled so its empirical σy(1 s) hits the requested target exactly for the drawn realization, the components are statistically independent, and the first argument selects the output container — `noisegen(FrequencyData, …)returns theysequence instead. Reproducibility works the same way as the manual path: seed the global RNG beforehand, or pass anAbstractRNGvia therngkeyword. If you work from PSD coefficients rather than a spec sheet, thehkeyword acceptsh_\\alphavalues directly in place ofsigma1`.\n\nThe two records are independent draws of the same nominal mixture, so what should agree is the shape — the same three slopes and the same two knees — not the point values. Pointwise differences come from two sources: each record is a single realization (the scatter grows toward long τ as the degrees of freedom shrink), and the two routes discretize the power laws differently (sample-domain randn / cumsum versus a shaped discrete spectrum), which shifts individual octaves by up to ~15 %:\n\nr_gen = adev(pd_gen, m_values)\n\nplot(r_manual;\n     label  = \"manual mix (randn + cumsum)\",\n     xlabel = raw\"Averaging time \\(\\tau\\) (s)\",\n     ylabel = raw\"\\(\\sigma_y(\\tau)\\)\",\n     title  = raw\"Manual synthesis vs noise\\_gen: same mixture, same bathtub\",\n     legend = :topright,\n     lw     = 1.5)\nplot!(r_gen; label = raw\"noise\\_gen mix\", lw = 1.5)\n\nprintln(\"τ (s)    ADEV manual  ADEV noise_gen  ratio   edf\")\nfor i in eachindex(m_values)\n    println(lpad(round(Int, r_manual.tau[i]), 5), \"    \",\n            rpad(round(r_manual.dev[i]; sigdigits = 3), 11), \"  \",\n            rpad(round(r_gen.dev[i];    sigdigits = 3), 14), \"  \",\n            rpad(round(r_gen.dev[i] / r_manual.dev[i]; digits = 2), 6), \"  \",\n            round(r_manual.edf[i]; digits = 1))\nend\n\nThe ratio sits near one through the bathtub floor and walks away from it over the last octaves. Read the edf column alongside: by τ = 16384 s a one-day record retains fewer than five equivalent degrees of freedom, so two independent realizations can legitimately differ there by a factor of two. That scatter is a property of the record length, not of either generator.","title":"The built-in generator"},{"category":"section","location":"tutorials/01_phase_data/#Where-to-go-next","page":"Phase data and frequency data","text":"Computing the Allan deviation — every field of the StabilityResult returned by adev, the default τ-grid, and the plot recipe used above.\nCharacterizing a drifting clock — noise_gen in action on a record where ADEV is the wrong tool.","title":"Where to go next"},{"category":"section","location":"getting_started/#Getting-Started","page":"Getting Started","text":"","title":"Getting Started"},{"category":"section","location":"getting_started/#Installation","page":"Getting Started","text":"SigmaTau.jl is a single registerable package. From the Pkg REPL:\n\npkg> add https://github.com/ianlap/SigmaTau.jl\n\nusing SigmaTau brings the shared types (PhaseData, FrequencyData, StabilityResult) and every public deviation, noise-ID, and IO function into scope under one flat namespace.","title":"Installation"},{"category":"section","location":"getting_started/#A-minimal-example","page":"Getting Started","text":"using SigmaTau\nusing Random\nRandom.seed!(42)\n\n# 10 minutes of phase residuals at 1 Hz, white-PM-like\nphase = randn(600)\np = PhaseData(phase, 1.0)\n\n# Compute overlapping Allan deviation at three τ values\nr = adev(p, [1, 4, 16]; ci=true)\n\n# Round for stable display\nround.(r.dev; sigdigits=4)\n\nThe result is a StabilityResult populated with tau, dev, per-τ noise_type, χ²-based confidence bounds ci (one (lo, hi) tuple per τ — r.ci[1].lo, or plain vectors via the ci_lower / ci_upper accessors), edf per Greenhall–Riley, and neff, the number of analysis windows averaged at each τ.","title":"A minimal example"},{"category":"section","location":"getting_started/#Where-to-next","page":"Getting Started","text":"Migrating from Stable32 — coming from Stable32? Start here for the run-type and result-column mapping, then the tutorial below for a longer walkthrough.\nMigrating from allantools — the function-name and calling-convention mapping for Python allantools users.\nTutorial 0: Julia for metrologists — covers installation, loading .DAT files, your first adev, overlaying plots, and saving results to disk.\nTutorial 1: Phase data — PhaseData / FrequencyData basics.\nTutorial 2: Computing Allan deviation — adev and the StabilityResult it returns (incl. EDF, χ² CI, noise type).\nTutorial 3: A drifting clock and the Hadamard family — hdev, mhdev, htdev, and mhtotdev on a record with linear frequency drift.\nTutorial 4: Reading and saving your data — read_phase / read_frequency for .DAT files and counter CSVs, fillgaps / detrend data prep, and save_result / save_suite for sharing results.\nTutorial 6: Three-cornered hat — separating clock noise from a three-clock comparison.\nTheory: Overview — what σ_y(τ) means.\nFAQ — common stumbling blocks, including the Package Plots not found error.","title":"Where to next"},{"category":"section","location":"theory/mhtotdev_bias_edf/#Theory:-MHTOTDEV-bias-and-EDF","page":"Theory: MHTOTDEV bias and EDF","text":"MHTOTDEV (modified Hadamard total deviation) is novel to SigmaTau — no other library computes it, and no published reference gives its bias factor or its equivalent degrees of freedom (EDF). Every other total-family estimator inherits a published coefficient table (TOTVAR and MTOT from SP1065 Tables 7–8; HTOT from the FCS 2001 paper). MHTOTDEV has none, so SigmaTau measures both by Monte Carlo against synthesized known-noise.\n\nThis page documents the methodology; it is the reproducible basis for the shipped _coeff_mhtot EDF coefficients and the bias_correction(:mhtot, …) table. The harness is tools/mc_mhtotdev.jl. The estimator under test is the shipped mhtotdev pipeline, including its default global least-squares drift removal (remove_drift=true; see Theory: Total family), so the coefficients calibrate exactly the statistic users compute.\n\nThe procedure follows the NIST total-variance literature: Howe, Beard, Greenhall, Vernotte & Riley, A Total Estimator of the Hadamard Function Used for GPS Operations (32nd PTTI, 2000) — for the bias and EDF definitions and the τ/τ0 ≥ 16 validity window — and Vernotte & Howe, Generalization of the Total Variance Approach to the Different Classes of Structure Functions — for the Monte Carlo EDF estimator and the extension methodology.","title":"Theory: MHTOTDEV bias and EDF"},{"category":"section","location":"theory/mhtotdev_bias_edf/#The-reference:-MHDEV","page":"Theory: MHTOTDEV bias and EDF","text":"MHTOTDEV is the total (boundary-extended) form of the modified-Hadamard variance MHVAR. The natural unbiased reference is therefore MHDEV (= mhdev), whose EDF SigmaTau already computes from first principles via the Greenhall–Riley spectral sum (_calc_edf_core with difference order d = 3, inner-average F = 1). Writing the per-τ variance estimates as\n\nhat V = mathrmMHTOTVAR(tau) = mathttmhtotdev^2 qquad\nhat W = mathrmMHVAR(tau)    = mathttmhdev^2 \n\nboth are computed on the same realization at matched averaging factor m, so finite-sample fluctuations are correlated and partly cancel in their ratio.","title":"The reference: MHDEV"},{"category":"section","location":"theory/mhtotdev_bias_edf/#Estimators","page":"Theory: MHTOTDEV bias and EDF","text":"EDF. A variance estimator that is χ²-distributed with ν degrees of freedom satisfies Var[\\hat V] = 2\\,E[\\hat V]^2/ν, so from R realizations\n\nwidehatmathrmedf = frac2bar V^2s_V^2\n\nwith \\bar V the sample mean and s_V^2 the Bessel-corrected sample variance. A nonparametric bootstrap over the R realizations gives the standard error. The relative precision scales as SE/edf ≈ √(2/R); the full sweep uses R = 3000 per cell, giving ≈2.6 %.\n\nBias. Following the normalized-bias definition of Howe et al. 2000 (eqn 6), nbias = E[\\hat V]/E[\\hat W] - 1, the variance-scale bias factor is the ratio of means\n\nB(alpha) = fracEhat VEhat W = 1 + mathrmnbias approx fracbar Vbar W\n\nmatching the bias_correction convention σ_unbiased = σ_raw / √B used across the total family. B > 1 means MHTOTDEV reads high (correction lowers σ), B < 1 means it reads low.\n\nEDF model. The total-variance literature offers two empirical EDF forms: the Mod-Totvar form (Vernotte & Howe, eqn 2) and the TotHvar form (Howe et al. 2000, eqn 7),\n\nmathrmedf(tau) = bfracTtau - c\nqquadtextvsqquad\nmathrmedf(tau) = fracTtaub_0 + b_1tauT\n\nwith T = (N-1)\\,τ_0. MHTOTDEV is the modified-Hadamard total — a hybrid of the two — so the harness fits both by weighted least squares (weights 1/\\mathrm{SE}^2) and selects the higher-R² form per α. Critically, the fit is restricted to the validity window τ/τ_0 ≥ 16: Howe et al. 2000 note their EDF model \"should be used only if … τ/τ_0 ≥ 16,\" below which the data-extension confers no advantage over the plain estimator and the fit degrades. Coefficients, standard errors, and R² are reported per α for both forms.","title":"Estimators"},{"category":"section","location":"theory/mhtotdev_bias_edf/#Analytic-cross-check","page":"Theory: MHTOTDEV bias and EDF","text":"Before any ratio is trusted, the harness verifies that the synthesized noise reproduces the analytic MHVAR power law σ²_MH(τ) ∝ τ^{μ(α)}:\n\nα Noise μ(α) (variance slope)\n2 WHPM −3\n1 FLPM −2\n0 WHFM −1\n−1 FLFM 0\n−2 RWFM +1\n\n(μ(α) = −α − 1 for α ≤ 0; the modified estimator rolls WHPM/FLPM off as τ^{−3}/τ^{−2} in variance, which is what lets it disambiguate the two PM noises.) The measured log MHVAR vs log τ slope must match μ(α) to within a small tolerance, validating both the noise_gen calibration and the MHDEV reference.","title":"Analytic cross-check"},{"category":"section","location":"theory/mhtotdev_bias_edf/#Monte-Carlo-design","page":"Theory: MHTOTDEV bias and EDF","text":"α ∈ {2, 1, 0, −1, −2}, using the known injected α (never identify_noise, to avoid noise-ID misclassification contaminating the per-α bins).\nN (record length, frequency samples) ∈ {1024, 2048, 4096, 8192, 16384, 32768}; octave m-grid 1 … N÷4, dropping cells with nsubs = N − 4m + 1 < 16. Pooling across N gives ≥30 (T/τ, edf) points per α for the fit.\nBoth the bias and the EDF use the τ/τ_0 ≥ 16 window. The small-m cells (especially m = 1, which is near-degenerate yet carries the tightest standard error, hence the largest weight) otherwise skew the bias ratio away from its physical value.\nReproducibility. Each realization draws from an independent stream Xoshiro(hash((seed, α, N, r))), so results are identical regardless of thread count. Synthesis takes an rng keyword for exactly this purpose.\nScale invariance. B and EDF are ratios of like-dimensioned quantities, so the arbitrary amplitude of the uncalibrated _gen_powerlaw_y draw cancels.\n\nThe harness emits a per-cell CSV and a JSON summary of metadata plus fitted coefficients (git SHA, seed, thread count, N grid, R, and the μ(α) check result). The authoritative full-sweep JSON is tracked at tools/artifacts/mhtotdev_mc_full.json; ad-hoc quick-run outputs stay local.\n\nnote: Provisional vs. authoritative numbers\nThe coefficient tables in _coeff_mhtot and bias_correction(:mhtot, …) are populated from the full sweep (N up to 32768, R = 3000), run on a workstation. The harness's reduced quick mode (N ≤ 2048, R = 200–400) reproduces the μ(α) slopes and the qualitative bias trend below; the published tables use the full-sweep values.","title":"Monte Carlo design"},{"category":"section","location":"theory/mhtotdev_bias_edf/#Result","page":"Theory: MHTOTDEV bias and EDF","text":"Full sweep (N = 1024 … 32768, R = 3000, both bias and EDF over τ/τ_0 ≥ 16, with the wrapper's default remove_drift=true global drift removal in the loop). All EDF fits are excellent (R² ≥ 0.9983):\n\nα Noise EDF b, c bias b0 b1\n2 WHPM 1.843, 5.539 1.064 −0.003\n1 FLPM 1.226, 3.930 0.984 −0.005\n0 WHFM 1.113, 3.921 1.020 −0.177\n−1 FLFM 1.031, 3.921 1.214 −0.702\n−2 RWFM 0.803, 3.089 1.950 −4.910\n\nMHTOTDEV is ≈ unbiased for the PM noises (b0 ≈ 1, b1 ≈ 0) and reads progressively high for redder FM at small τ — for random-walk FM, B ≈ 1.9. The bias is modeled τ/T-linearly, B = b0 + b1·(τ/T). The b1 term now carries two physical effects with the same sign: the boundary-extension bias falling off toward the record length, and the suppression of totalized low-frequency power by the global least-squares drift removal as τ → T (the well-known effect of drift removal on red noise). Together they cut the bias-fit residual by ~53 % (WHFM), ~68 % (FLFM), and ~97 % (RWFM); for the PM noises b1 ≈ 0 and the bias is constant in τ/T.\n\nTwo methodology points matter for reproducing the table: (1) restricting the bias to the same τ/τ_0 ≥ 16 window as the EDF — the near-degenerate m = 1 cell otherwise dominates the weighted ratio and pulls the apparent bias spuriously below 1; and (2) the τ/τ_0 ≥ 16 floor on the EDF fit itself, which holds R² ≥ 0.998 across all noise types. The tracked full-sweep artifact records the git SHA, seed, grid, and per-α fits.\n\nwarning: Edge zone at the reach limit\nThe kernel needs N − 4m + 1 ≥ 1, so MHTOTDEV's reach limit is τ = T/4 (T/τ ≥ 4). The fitted linear form crosses ν = 1 near T/τ ≈ 3.6–5.1 depending on α (highest at the red end), and a χ² interval computed from ν < 1 is degenerate (the bounds collapse). Treat confidence intervals as valid for T/τ ≳ 6 and indicative only in the final octave before the reach limit; the σ values themselves are valid all the way to τ = T/4.","title":"Result"},{"category":"section","location":"migration_from_allantools/#Migrating-from-allantools","page":"From allantools","text":"If you come from allantools, the statistics are the same — phase or frequency input, the overlapping estimators, the power-law noise model, an octave-spaced τ grid by default. SigmaTau is numerically cross-validated against allantools (see Validation). What changes is the calling pattern and the result container: instead of passing data= + rate= + data_type= on every call, you wrap the samples once in a typed record; instead of a (taus, devs, errs, ns) tuple, you get a single StabilityResult that also carries per-τ noise identification, χ² confidence bounds, and equivalent degrees of freedom.","title":"Migrating from allantools"},{"category":"section","location":"migration_from_allantools/#Deviation-name-mapping","page":"From allantools","text":"allantools function SigmaTau function Notes\nadev adev SigmaTau's adev is always overlapping — it is allantools' oadev. There is no non-overlapping call.\noadev adev one-to-one\nmdev mdev \ntdev tdev \nhdev hdev always overlapping — it is allantools' ohdev\nohdev hdev one-to-one\ntotdev totdev SigmaTau bias-corrects by default; see Differences that change numbers\nmtotdev mtotdev same\nttotdev ttotdev same\nhtotdev htotdev same\nmtie mtie \npdev pdev \ntheo1 theo1 same eq. 30 values at equal m (cross-validated to machine precision), but allantools reports them at τ = m·τ0 while SigmaTau reports the SP1065/Stable32 effective τ = 0.75·m·τ0; SigmaTau also applies ThêoBR bias removal by default — pass correct_bias=false to match allantools' raw values. See Theory: Allan family\ntierms tierms same SP1065 §5.2.18 eq. 37 statistic; cross-validated to machine precision\ngradev — none no gap-robust ADEV; the SigmaTau route is fillgaps (Howe–Schlossberger imputation), then adev\nthree_cornered_hat_phase nch operates on pairwise StabilityResults rather than raw phase, and generalizes to N ≥ 3 clocks; see the three-cornered-hat tutorial\ngcodev — none no Groslambert codeviation\nnoise.*, noise_kasdin noise_gen power-law noise synthesis from sigma1 or h coefficient targets\nautocorr_noise_id identify_noise runs automatically inside every ci=true deviation call; see below\n\nFour SigmaTau deviations have no allantools counterpart:\n\ntheoh — the SP1065 ThêoH composite (overlapping ADEV below the 20 %-of-T crossover, bias-removed ThêoBR above); allantools has no ThêoBR/ThêoH helper. See Theory: Allan family.\nmhdev — modified Hadamard deviation, a phase-averaged third difference: drift-insensitive like HDEV while still separating white-PM from flicker-PM like MDEV. See Theory: Allan family.\nhtdev — Hadamard time deviation, the σ_x form of mhdev (scaled by τ/√(10/3)); it is to MHDEV what TDEV is to MDEV. Original to this package. Same theory page.\nmhtotdev — modified Hadamard total deviation, the boundary-extended (total) form of mhdev for long-τ estimates. Original to this package; its bias and EDF have no published model and are measured by Monte Carlo, documented in MHTOTDEV bias and EDF.","title":"Deviation name mapping"},{"category":"section","location":"migration_from_allantools/#Calling-convention-mapping","page":"From allantools","text":"allantools passes the sampling metadata into every call; SigmaTau attaches it to the data once:\n\nallantools argument SigmaTau equivalent\ndata=x, data_type=\"phase\" PhaseData(x, tau0)\ndata=y, data_type=\"freq\" FrequencyData(y, tau0)\nrate=r (sampling rate, Hz) tau0 = 1/r (sample interval, seconds)\ntaus=\"octave\" (the default) Octave (also the default)\ntaus=\"all\" AllTaus\ntaus=\"decade\" Decade\ntaus=array (seconds) m_values::Vector{Int} (averaging factors, τ = m·τ₀)\n\nTwo unit conversions to watch:\n\nrate is a frequency in Hz; tau0 is the sample interval in seconds, so tau0 = 1/rate. Data taken at 10 Hz is PhaseData(x, 0.1).\nAn explicit allantools taus array is in seconds; an explicit SigmaTau grid is integer averaging factors m, with τ = m·τ₀. tau_values(Octave, length(p.x), :adev) materializes a TauMode grid as that integer vector if you want to inspect or edit it.\n\nOctave, AllTaus, and Decade are instances of the TauMode selector, passed in place of the explicit vector — adev(p, Decade). SigmaTau adds HalfOctave, QuarterOctave, and HalfDecade spacings, which have no allantools string equivalent.","title":"Calling-convention mapping"},{"category":"section","location":"migration_from_allantools/#Return-values","page":"From allantools","text":"Every allantools deviation returns a tuple (taus, ad, ade, ns). Every SigmaTau deviation returns a StabilityResult:\n\nallantools tuple element StabilityResult field\ntaus r.tau (seconds)\nad r.dev\nade (= ad/√ns) r.ci — χ²-based absolute (lo, hi) bounds per τ, not a ± half-width; ci_lower(r) / ci_upper(r) give plain vectors\nns r.neff — the number of analysis windows; r.edf separately carries the equivalent degrees of freedom\n\nThe result also carries r.noise_type (the per-τ power-law identification, as SP1065 symbols :WHPM, :FLPM, :WHFM, :FLFM, :RWFM) and r.deviation_type (which estimator produced it). The noise_type, ci, and edf vectors are populated when ci=true (the default) and empty when ci=false; r.neff is populated either way. mtie and tierms are the exceptions: they have no published CI model (mtie is a deterministic envelope), so they return noise_type, ci, and edf empty even when ci=true.","title":"Return values"},{"category":"section","location":"migration_from_allantools/#The-same-analysis-in-both-libraries","page":"From allantools","text":"allantools:\n\nimport numpy as np\nimport allantools\n\nx = np.loadtxt(\"clock.dat\")          # phase residuals in seconds, 1 Hz\ntaus, devs, errs, ns = allantools.oadev(\n    x, rate=1.0, data_type=\"phase\", taus=\"octave\")\nmtaus, mdevs, merrs, mns = allantools.mdev(\n    x, rate=1.0, data_type=\"phase\", taus=\"octave\")\n\nSigmaTau:\n\nusing SigmaTau\n\np = read_phase(\"clock.dat\"; time_col=0, value_col=1, tau0=1.0)\n\nr  = adev(p)        # octave grid by default, like taus=\"octave\"\nrm = mdev(p)\n\nr.tau               # τ in seconds            (allantools taus)\nr.dev               # σ_y(τ)                  (allantools ad)\nci_lower(r)         # χ² lower bounds, 68.3 % (no allantools analog in the tuple)\nci_upper(r)         # χ² upper bounds — or per-τ tuples via r.ci[i].lo / r.ci[i].hi\nr.neff              # number of analysis windows (allantools ns)\nr.noise_type        # identified power-law type at each τ\nr.edf               # equivalent degrees of freedom\n\nAn explicit grid is adev(p, [1, 2, 4, 8, 16]). Where in allantools you would loop over deviation functions (or Dataset.compute one statistic at a time), stability(p; devs=(:adev, :mdev, :hdev, :tdev)) computes several in one call and returns a StabilitySuite indexed by symbol (suite[:adev]).","title":"The same analysis in both libraries"},{"category":"section","location":"migration_from_allantools/#Differences-that-change-numbers","page":"From allantools","text":"For the same input and the same τ grid, the σ values agree to the documented validation tolerance. Three things legitimately differ:\n\nError bars. allantools' deviation functions return the simple estimate ade = ad/√ns, where ns is the number of analysis windows (SigmaTau's r.neff). SigmaTau instead identifies the noise type at each τ, computes the Greenhall–Riley equivalent degrees of freedom (Greenhall and Riley, 2003), and maps the deviation through the χ² distribution into asymmetric (lo, hi) bounds in r.ci at the 68.3 % level (DEFAULT_CONFIDENCE = 0.683; override per call with confidence=0.95). The two are different statistics and will not match numerically — allantools can produce the χ² interval too, but only through separate manual calls to edf_greenhall and confidence_interval. See Theory: Confidence intervals.\n\nBias correction on the total family. totdev, mtotdev, ttotdev, htotdev, and mhtotdev apply noise-type-dependent unbias corrections by default (correct_bias=true), because the raw total estimators are biased relative to their parent statistics — MTOT reads high by roughly 3 – 14 % in σ depending on noise type, HTOT reads low. That percentage is the estimator's own bias against the parent statistic, not a kernel disagreement between libraries: with correct_bias=false SigmaTau reproduces allantools' totals to machine precision on the validation fixture. allantools always returns the raw estimators (its source marks the corrections as to-do); Stable32's policy is per-estimator — it corrects TOTDEV and HTOTDEV but reports MTOTDEV raw, so correct_bias=false matches Stable32 only for MTOTDEV. See Theory: Total family and Validation: Stable32.\n\nNoise identification is automatic and feeds everything. Both packages use lag-1-autocorrelation noise identification (Riley and Greenhall, Apr 2004); allantools exposes it as the separate autocorr_noise_id function, while SigmaTau runs it per τ inside every ci=true call except mtie/tierms (with a B1-ratio fallback) and reports the result in r.noise_type. Because the EDF — and, for the total family, the bias factor — depend on the identified α, a different identification at some τ moves the confidence interval (and the corrected σ) at that τ. See Theory: Noise identification.","title":"Differences that change numbers"},{"category":"section","location":"migration_from_allantools/#Performance","page":"From allantools","text":"On identical inputs the measured speedup over allantools ranges from a few times (totdev) through roughly 20× (adev) to several thousand times (mtotdev / htotdev); the timings and method are on the Performance page.","title":"Performance"},{"category":"section","location":"migration_from_allantools/#See-also","page":"From allantools","text":"Getting Started — installation and a first run.\nMigrating from Stable32 — the same guide for Stable32 users.\nValidation Methodology — how the cross-validation against allantools and Stable32 is done.","title":"See also"},{"category":"section","location":"reference/types/#Shared-types","page":"Shared types","text":"Core data types used across the package and re-exported from SigmaTau.","title":"Shared types"},{"category":"section","location":"reference/types/#Types","page":"Shared types","text":"","title":"Types"},{"category":"section","location":"reference/types/#Accessors","page":"Shared types","text":"","title":"Accessors"},{"category":"section","location":"reference/types/#Tables.jl","page":"Shared types","text":"StabilityResult and StabilitySuite implement the Tables.jl row-table interface when Tables is loaded:\n\nusing SigmaTau, Tables\n\nrows = Tables.rows(adev(data; ci=true))\ncols = Tables.columntable(stability(data; devs=(:adev, :mdev)))\n\nThe extension is optional. Tables is a weak dependency, so loading SigmaTau alone does not load the wider tabular-data stack. Empty confidence fields from ci=false are exposed as missing table values.","title":"Tables.jl"},{"category":"type","location":"reference/types/#SigmaTau.AbstractTimingData","page":"Shared types","text":"AbstractTimingData\n\nSupertype of timing data records (PhaseData, FrequencyData).\n\n\n\n\n\n","title":"SigmaTau.AbstractTimingData"},{"category":"type","location":"reference/types/#SigmaTau.PhaseData","page":"Shared types","text":"PhaseData{T<:AbstractFloat}\n\nPhase residuals x(t) sampled at uniform interval tau0 (default 1.0 s).\n\nx::Vector{T} where T<:AbstractFloat: Phase samples in seconds.\ntau0::Float64: Base sample interval τ₀ in seconds.\nsource::String: Provenance: \"user\" for directly constructed records, the originating file path for records returned by read_phase. Preserved by detrend, fillgaps, and remove_outliers, and recorded in the header written by save.\n\n\n\n\n\n","title":"SigmaTau.PhaseData"},{"category":"type","location":"reference/types/#SigmaTau.FrequencyData","page":"Shared types","text":"FrequencyData{T<:AbstractFloat}\n\nFractional-frequency samples y(t) at uniform interval tau0 (default 1.0 s).\n\ny::Vector{T} where T<:AbstractFloat: Fractional-frequency samples (dimensionless).\ntau0::Float64: Base sample interval τ₀ in seconds.\nsource::String: Provenance: \"user\" for directly constructed records, the originating file path for records returned by read_frequency. Preserved by detrend, fillgaps, and remove_outliers, and recorded in the header written by save.\n\n\n\n\n\n","title":"SigmaTau.FrequencyData"},{"category":"type","location":"reference/types/#SigmaTau.StabilityResult","page":"Shared types","text":"StabilityResult\n\nUnified return type for every stability calculation.\n\nThe noise_type, ci, and edf vectors are empty when the calculation was invoked with ci=false (and always for mtie/tierms); neff is always populated. Per-τ confidence bounds read as r.ci[i].lo / r.ci[i].hi; the ci_lower / ci_upper accessors return them as plain vectors.\n\ndeviation_type::Symbol: Which deviation produced this result (e.g. :adev, :mdev).\ntau::Vector{Float64}: Analysis intervals τ in seconds.\ndev::Vector{Float64}: Stability deviation σ_y(τ) per interval.\nnoise_type::Vector{Symbol}: Noise-type symbol identified at each τ (empty unless ci=true).\nci::Vector{@NamedTuple{lo::Float64, hi::Float64}}: Confidence interval (lo, hi) per τ (empty unless ci=true).\nedf::Vector{Float64}: Equivalent degrees of freedom (empty unless ci=true).\nneff::Vector{Int64}: Number of analysis windows averaged at each τ (0 where the kernel returns NaN).\n\n\n\n\n\n","title":"SigmaTau.StabilityResult"},{"category":"type","location":"reference/types/#SigmaTau.StabilitySuite","page":"Shared types","text":"StabilitySuite\n\nOrdered collection of StabilityResults computed from one timing record in a single stability call, plus the session metadata needed to reproduce and serialize the analysis.\n\nIndex by deviation symbol to recover a result (suite[:adev]) or by position (suite[1]). The suite is iterable in request order and supports keys, haskey, length, and pairs, so a CLI or GUI can render it as an ordered table.\n\nresults::Vector{StabilityResult}: Per-deviation results, in the order they were requested.\ndata_kind::Symbol: Input record kind: :phase or :frequency.\ntau0::Float64: Base sample interval τ₀ in seconds.\nn::Int64: Number of input samples N.\nconfidence::Float64: Confidence level used for the CI bounds (NaN when ci=false).\ntau_mode::Symbol: Tau-grid selector, recorded as a Symbol (e.g. :Octave, or :explicit).\n\n\n\n\n\n","title":"SigmaTau.StabilitySuite"},{"category":"type","location":"reference/types/#SigmaTau.SpectralResult","page":"Shared types","text":"SpectralResult\n\nUnified return type for every spectral-density estimate (Sy, Sx, L).\n\nMirrors StabilityResult: a flat, non-parametric record carrying the one-sided frequency grid, the estimated spectrum, and the Welch parameters that produced it, so a saved or plotted result is self-describing.\n\nspectral_type::Symbol: Which estimate produced this result: :Sy, :Sx, or :L.\nfreq::Vector{Float64}: One-sided frequency grid f in Hz.\npsd::Vector{Float64}: Spectral density at each f; interpret per units.\nunits::Symbol: Units of psd: :per_Hz (Sy, 1/Hz), `:s2perHz` (Sx, s²/Hz), or :dBc_per_Hz (ℒ(f)).\nnperseg::Int64: Welch segment length (samples).\nnoverlap::Int64: Welch segment overlap (samples).\nwindow::Symbol: Window applied to each segment: :hann, :hamming, or :rectangular.\n\n\n\n\n\n","title":"SigmaTau.SpectralResult"},{"category":"function","location":"reference/types/#SigmaTau.ci_lower","page":"Shared types","text":"ci_lower(r::StabilityResult) → Vector{Float64}\n\nLower confidence bounds of r as a plain vector (getfield.(r.ci, :lo)). Empty when the result carries no CIs.\n\n\n\n\n\n","title":"SigmaTau.ci_lower"},{"category":"function","location":"reference/types/#SigmaTau.ci_upper","page":"Shared types","text":"ci_upper(r::StabilityResult) → Vector{Float64}\n\nUpper confidence bounds of r as a plain vector (getfield.(r.ci, :hi)). Empty when the result carries no CIs.\n\n\n\n\n\n","title":"SigmaTau.ci_upper"},{"category":"section","location":"theory/allan_family/#Theory:-Allan-Family","page":"Theory: Allan Family","text":"The Allan family covers the six estimators built from second- and third-difference operators on phase data: ADEV, MDEV, TDEV, HDEV, MHDEV, and HTDEV. They share the same boundary handling (no extension) and differ in difference order, phase-averaging, and final scaling.","title":"Theory: Allan Family"},{"category":"section","location":"theory/allan_family/#ADEV-—-overlapping-Allan-deviation","page":"Theory: Allan Family","text":"The original two-sample variance, in its overlapping estimator form (SP1065 Eq. 14):\n\nsigma^2_y(tau) = frac12(N - 2m)(mtau_0)^2\nsum_i=1^N-2m bigl(x_i+2m - 2x_i+m + x_ibigr)^2\n\nwith τ = m·τ₀ and N phase samples. ADEV slope versus α: WPM/FPM both give μdev = −1 (degeneracy), WFM gives μdev = −1/2, FFM gives μdev = 0, RWFM gives μdev = +1/2.\n\nIn SigmaTau:\n\nadev(PhaseData(x, τ₀), [1, 2, 4, 8, 16])\n\n(Definition and overlapping form per SP1065 §5 (Riley, Jul 2008).)","title":"ADEV — overlapping Allan deviation"},{"category":"section","location":"theory/allan_family/#MDEV-—-modified-Allan-deviation","page":"Theory: Allan Family","text":"A phase-averaged second difference. SP1065 Eq. 16:\n\nmathrmMVAR(tau) = frac12 m^4 tau_0^2  N_e\nsum_j=1^N_e bigglsum_k=0^m-1\nbigl(x_j+k+2m - 2x_j+k+m + x_j+kbigr)biggr^2\nqquad N_e = N - 3m + 1\n\nThe inner phase-averaging step decouples WPM (μdev = −3/2) from FPM (μdev = −1). In src/kernels.jl this is implemented in third-difference form via prefix sums — algebraically identical to the SP1065 form above; the equivalence is Greenhall's third-difference construction (Greenhall, 1997).\n\nmdev(PhaseData(x, τ₀), τs)","title":"MDEV — modified Allan deviation"},{"category":"section","location":"theory/allan_family/#PDEV-—-parabolic-deviation","page":"Theory: Allan Family","text":"The parabolic variance PVAR (with PDEV = √PVAR) is Vernotte, Lenczner, Bourgeois & Rubiola's wavelet variance built on a least-squares fit: instead of the finite-difference kernels of the Allan family, the estimator weights the phase with a quadratic (parabolic) window (Vernotte et al., 2016). The least-squares construction gives it better long-τ noise-type discrimination than MVAR at comparable confidence, which is the property that motivated it (Vernotte et al., 2016).\n\nmathrmPVAR(tau) = frac2N_em^2tau_0^2\nsum_i bigglsum_k=0^m c_kx_i+kbiggr^2 \n\nwith c_k the quadratic weighting kernel (parabolic shape over the window) constructed so that the operator integrates to zero against both constants and linear ramps but responds nonzero to quadratic curvature in x(t) (Vernotte et al., 2016).\n\nnote: Implementation status\nImplemented as pdev (parabolic deviation σ = √PVAR); m=1 reproduces ADEV exactly and a linear-trend annihilation test pins the kernel. χ² confidence intervals use the Vernotte–Chen–Rubiola closed-form EDF (Vernotte et al., 2020); see Theory: PDEV confidence.","title":"PDEV — parabolic deviation"},{"category":"section","location":"theory/allan_family/#TDEV-—-time-deviation","page":"Theory: Allan Family","text":"Time deviation. TDEV measures time-error stability, so its units are seconds (it is a \\sigma_x quantity, not \\sigma_y). SP1065 Eq. 17 defines it as a scaled MDEV:\n\nsigma_x^2(tau) = mathrmTVAR(tau) = fractau^23mathrmMVAR(tau)\n\ntdev(...) wraps mdev(...) and applies the \\tau / \\sqrt{3} scaling. The factor of \\sqrt{3} comes from the integral of the second-difference sampling function squared against the fractional-frequency-to-time-error kernel.\n\nUse case. TDEV is the standard tool for characterizing the time error of a clock or a time-distribution system — telecommunications synchronization networks (PTP / SyncE / Recommendation G.810), GNSS time transfer, atomic-clock comparison links, and any infrastructure where the operative question is \"how badly will a downstream consumer's clock drift away from the reference over τ seconds?\" SP1065 §5 (Riley, Jul 2008) frames it as the way to characterize \"the time error of a time source (clock) or distribution system.\" For records dominated by white PM noise, TVAR reduces to the standard variance of the time deviations themselves; for the other power-law noises it remains a convergent estimator and inherits MDEV's WPM/FPM disambiguation.","title":"TDEV — time deviation"},{"category":"section","location":"theory/allan_family/#HDEV-—-overlapping-Hadamard-deviation","page":"Theory: Allan Family","text":"A third-difference variant with two advantages over ADEV: linear frequency drift is filtered out automatically, and the variance integral converges over a wider range of low-frequency noise. SP1065 §5.4:\n\nsigma^2_Hy(tau) = frac16(N - 3m)(mtau_0)^2\nsum_i=1^N-3m bigl(x_i+3m - 3x_i+2m + 3x_i+m - x_ibigr)^2 \n\nThe 1/6 prefactor arises from the third-difference variance normalization.\n\nDrift insensitivity. A linear-in-t term in y(t) (constant frequency drift) is annihilated by the third difference, so HDEV is not contaminated by drift the way ADEV is. SP1065 demonstrates this on a simulated rubidium record: ADEV picks up a +τ slope at long τ without prior detrending, while HDEV gives essentially the same answer as drift-removed ADEV (Riley, Jul 2008).\n\nNoise-type convergence. ADEV's variance integral diverges for α ≤ −3 (flicker walk FM, random run FM) at the low-frequency end. HDEV remains finite down to α = −4, because the third difference adds an extra factor of f² to the kernel that suppresses the f → 0 singularity (Greenhall, 1997). In practice this matters only for records with very-low-frequency power-law content; most laboratory clocks are well-described in −2 ≤ α ≤ +2 and ADEV suffices.\n\nHDEV is the preferred Allan-family estimator for clocks with known drift (Cs, Rb, H-maser) and for any analysis that needs to characterize the longest power-law tails.\n\nhdev(PhaseData(x, τ₀), τs)","title":"HDEV — overlapping Hadamard deviation"},{"category":"section","location":"theory/allan_family/#MHDEV-—-modified-Hadamard-deviation","page":"Theory: Allan Family","text":"A phase-averaged third difference. Combining the SP1065 form with the prefix-sum / third-difference equivalence from Greenhall 1997 (Greenhall, 1997):\n\nmathrmMHVAR(tau) = frac16m^4 tau_0^2  N_e\nsum_j=1^N_e bigglsum_k=0^m-1\nbigl(x_j+k+3m - 3x_j+k+2m + 3x_j+k+m - x_j+kbigr)biggr^2\nqquad N_e = N - 4m + 1\n\nThe relationship between MHDEV and HDEV mirrors the MDEV / ADEV relationship: phase-averaging the inner third-difference window splits the WPM/FPM degeneracy (slope μ_dev is −3/2 under WPM, −1 under FPM), while the third-difference kernel preserves HDEV's drift insensitivity. MHDEV is the right choice when a record contains linear frequency drift and phase noise that ADEV / MDEV cannot disambiguate. SigmaTau's kernel uses the prefix-sum form for performance; it is algebraically identical to the textbook expression above (the same telescoping identity as the MDEV case, one difference order higher).\n\nmhdev(PhaseData(x, τ₀), τs)","title":"MHDEV — modified Hadamard deviation"},{"category":"section","location":"theory/allan_family/#Theo1-—-theoretical-variance-#1","page":"Theory: Allan Family","text":"Theo1 (Theoretical Variance #1) extends the usable averaging-time range of a stability estimator by sampling longer windows than ADEV permits on the same record (Riley, Jul 2008). Where overlapping ADEV uses pairs separated by mτ₀ with a maximum m ≤ N/2, Theo1 operates over windows of length (N − 1)·τ₀ regardless of m and extracts stability information from internal cross-products inside that long window. The estimator's headline benefit is factor-of-3 extension of the long-τ reach at fixed record length, with a noise-type-dependent bias that is corrected via the Theo1 bias table.\n\nmathrmTheo1(m tau_0 N) =\nfrac1075(N - m)(mtau_0)^2\nsum_i=1^N-msum_delta=0^m2-1\nfrac1m2 - delta\nbigl(x_i - x_i-delta+m2) + (x_i+m - x_i+delta+m2)bigr^2 \n\nfor even m with 2 ≤ m ≤ N − 1 (SP1065 eq. 30; the printed equation omits the 0.75 normalization of the original Howe & Peppler definition, but SP1065's own Table 2 bias value of 1.00 for white FM, Stable32, and allantools all require it) (Riley, Jul 2008). The effective averaging time of a Theo1 value is τ = 0.75·m·τ₀, and that is the τ SigmaTau reports.\n\nnote: Implemented\ntheo1(data, m_values; ci=true, correct_bias=true, confidence=…), with the usual TauMode / zero-argument / FrequencyData dispatch chains. m_values are even Theo1 averaging factors (tau_values(mode, N, :theo1) rounds a grid to even and dedupes); r.tau holds the effective 0.75·m·τ₀. correct_bias=true (default) applies the ThêoBR automatic bias removal of SP1065 eq. 33 — the average of the matched-τ AVAR(9+3i)/Theo1(12+4i) variance ratios over i = 0…⌊N/6−3⌋ — so the default output is the bias-removed ThêoBR; correct_bias=false gives raw eq. 30 Theo1. EDF/CIs use the per-noise-type empirical formulas of SP1065 §5.2.15 Table 3 with r = 0.75m. The raw kernel is cross-validated against allantools 2024.06 to machine precision (test/stab/theo.jl); allantools reports the same values at τ = m·τ₀, a definitional offset resolved here in favor of SP1065/Stable32.","title":"Theo1 — theoretical variance #1"},{"category":"section","location":"theory/allan_family/#ThêoH-—-composite-Theo1-ADEV-estimator","page":"Theory: Allan Family","text":"ThêoH (pronounced \"theo-H\") is a composite estimator that uses overlapping ADEV at short averaging factors and switches to the bias-corrected Theo1 (TheoBR) at longer averaging factors, producing a single continuous σ_y(τ) curve from m = 1 out to the Theo1 long-τ limit (Riley, Jul 2008). The crossover point is chosen so that the resulting curve has tight CIs across the entire τ range that neither ADEV nor Theo1 alone could provide on a fixed-length record. Banerjee & Matsakis recommend ThêoH as the default long-record stability summary in modern timekeeping practice (Banerjee and Matsakis, 2023).\n\nPer SP1065 eq. 34 the composite is\n\nmathrmThêoH(m tau_0 N) =\nbegincases\nmathrmAVAR(m tau_0 N)  textfor  tau = mtau_0 le k2pt\nmathrmThêoBR(m_theta tau_0 N)  textfor  tau = 075m_thetatau_0  k m_theta text even\nendcases\n\nwith crossover k = 20 % of the record span T = (N−1)·τ₀ (Riley, Jul 2008).\n\nnote: Implemented\ntheoh(data, m_values; ci=true, confidence=…), with the usual TauMode / zero-argument / FrequencyData dispatch chains. m_values are τ-grid factors (target τ = m·τ₀, not necessarily even): points with m·τ₀ ≤ k come from overlapping ADEV at m, points above from ThêoBR at the even Theo1 factor nearest 4m/3 (exact whenever 3 | m), each reported at its realized τ. CIs use the Greenhall–Riley ADEV EDF on the ADEV segment and the SP1065 Table 3 Theo1 EDF on the ThêoBR segment. ThêoH always carries the eq. 33 bias removal; raw Theo1 is available via theo1(…; correct_bias=false).","title":"ThêoH — composite Theo1 + ADEV estimator"},{"category":"section","location":"theory/allan_family/#Dynamic-Allan-deviation-—-DADEV","page":"Theory: Allan Family","text":"The dynamic Allan deviation generalizes σy(τ) to a time-resolved stability map: instead of a single scalar curve versus τ, DADEV produces a 2-D surface σy(t, τ) by sliding an analysis window across the record and computing ADEV inside each window (McKelvy et al., 2025). The original construction is due to Galleani & Tavella (2009); McKelvy et al. 2025 apply it to telemetry-based stability estimation of high- precision atomic clocks and demonstrate its ability to localize non-stationary stability events that a static ADEV averages over.\n\nThe estimator structure is\n\nsigma_y^2(t_ctau) = frac12(N_w - 2m)(mtau_0)^2\nsum_i in W(t_c) bigl(x_i+2m - 2 x_i+m + x_ibigr)^2 \n\nwhere W(t_c) is an analysis window of N_w phase samples centered at coordinate time t_c. The window slides across the record to produce DADEV at every t_c of interest (McKelvy et al., 2025).\n\nnote: Planned implementation\nThe mathematical definition is documented above. The dadev function is not yet implemented in SigmaTau.","title":"Dynamic Allan deviation — DADEV"},{"category":"section","location":"theory/allan_family/#HTDEV-—-Hadamard-time-deviation","page":"Theory: Allan Family","text":"HTDEV is to MHDEV what TDEV is to MDEV: a τ-scaled time-domain deviation built on the third-difference (Hadamard) kernel instead of the second-difference (Allan) kernel. Like TDEV, HTDEV has units of seconds (it is a \\sigma_x quantity). SigmaTau implements it as mhdev followed by the scaling τ / √(10/3):\n\nsigma_xmathrmHT(tau) = fractausqrt103  sigma_ymathrmMH(tau)\n\nhtdev(PhaseData(x, τ₀), τs)\n\nThe \\sqrt{10/3} factor mirrors the \\sqrt{3} factor in TDEV: each arises from the integral of the kernel's sampling function squared against the frequency-to-time-error map. The second-difference (Allan) kernel produces 1/3; the third-difference (Hadamard) kernel produces 3/10.\n\nUse case. HTDEV inherits TDEV's purpose — characterizing the time error of a clock or time-distribution system — and adds two properties TDEV cannot offer:\n\nLinear frequency-drift insensitivity. The third-difference kernel annihilates terms linear in t, so a record from a Cesium, Rubidium, or H-maser clock with significant drift can be analyzed without first removing the drift. SP1065 §5 makes the equivalent point for HDEV vs ADEV: \"the Hadamard deviation may be used to reject linear frequency drift when a stability analysis is performed\" (Riley, Jul 2008). HTDEV carries that benefit into the time-domain.\nWider noise-type convergence. The Hadamard family converges over α ∈ {−4, −3} (frequency walk-walk and random-run FM) where the Allan family diverges (Greenhall, 1997). HTDEV is the time-domain extension of that range.\n\nFor records dominated by white-PM noise without drift, TDEV is fine and slightly more efficient (tighter CI per τ on shorter records). For records with drift, divergent low-frequency noise, or both, HTDEV gives a numerically valid time-stability summary where TDEV would be contaminated.\n\ninfo: Original contribution\nHTDEV is original to SigmaTau. The standard time-and-frequency references — SP1065 (Riley, Jul 2008), IEEE 1139-2022 (IEEE Ultrasonics and Society, 2022), NBS-TN-1337 (Sullivan et al., Mar 1990) — do not define it. The authoritative definition lives in the package source itself; the public wrapper sits at src/deviations.jl and uses the same τ-rescale-of-a-Modified-deviation pattern as TDEV (SP1065 §5.2.7 (Riley, Jul 2008)) applied to the modified-Hadamard kernel rather than the modified-Allan kernel. The earlier name ldev was retained temporarily as a deprecated alias and removed before the public release.","title":"HTDEV — Hadamard time deviation"},{"category":"section","location":"theory/allan_family/#Slope-vs-noise-table","page":"Theory: Allan Family","text":"For each estimator, the deviation slope μ_dev versus the spectral exponent α of S_y(f):\n\nα ADEV MDEV HDEV MHDEV TDEV HTDEV\n+2 (WPM) −1 −3/2 −1 −3/2 −1/2 −1/2\n+1 (FPM) −1* −1 −1* −1 0 0\n0 (WFM) −1/2 −1/2 −1/2 −1/2 +1/2 +1/2\n−1 (FFM) 0 0 0 0 +1 +1\n−2 (RWFM) +1/2 +1/2 +1/2 +1/2 +3/2 +3/2\n\n(*) ADEV/HDEV's response to flicker PM includes a log(2π·m) factor (see SP1065 §5).","title":"Slope vs noise table"},{"category":"section","location":"theory/allan_family/#Demonstration","page":"Theory: Allan Family","text":"using SigmaTau, Random\nRandom.seed!(0)\n\n# Pure WPM noise\nN = 8192\nx = randn(N)\nτs = [1, 4, 16, 64, 256]\n\na  = adev(PhaseData(x, 1.0), τs).dev\nm  = mdev(PhaseData(x, 1.0), τs).dev\n\n# Slopes (deviation log-log)\nslope(σ) = (log10(σ[end]) - log10(σ[1])) / (log10(τs[end]) - log10(τs[1]))\nround.([slope(a), slope(m)]; sigdigits=3)\n\nADEV slope should be near −1; MDEV slope should be near −3/2 — the characteristic split that makes MDEV able to disambiguate WPM from FPM.","title":"Demonstration"},{"category":"section","location":"theory/allan_family/#Implementation-notes","page":"Theory: Allan Family","text":"All cores in src/kernels.jl take Vector{Float64} and return raw arrays; the public API in src/deviations.jl wraps them and returns StabilityResult.\nMDEV/MHDEV use a prefix-sum form algebraically equivalent to the textbook 1/m⁴ form (Greenhall's third-difference construction (Greenhall, 1997)); the kernels are pinned against direct textbook-form reference implementations in test/stab/legacy_kernels.jl.\ntdev and htdev are scaling wrappers; they do no extra kernel work.","title":"Implementation notes"},{"category":"section","location":"theory/allan_family/#See-also","page":"Theory: Allan Family","text":"Theory: Total family — boundary-extended estimators.\nTheory: Confidence — EDF and CI for these estimators.\nAPI: SigmaTau — function signatures.","title":"See also"},{"category":"section","location":"theory/allan_family/#References","page":"Theory: Allan Family","text":"SP1065 §5 (Riley, Jul 2008).\nNBS Technical Note 1337 (Sullivan et al., Mar 1990).\nGreenhall, Third-difference approach to MVAR, IEEE T-IM 1997 (Greenhall, 1997).\nIEEE 1139-2022 (IEEE Ultrasonics and Society, 2022) for canonical ADEV / MDEV / HDEV / TDEV / MHDEV definitions; HTDEV is not defined there.","title":"References"},{"category":"section","location":"migration_from_stable32/#Migrating-from-Stable32","page":"From Stable32","text":"If you already think in Stable32 terms, this page maps that workflow onto SigmaTau.jl. The vocabulary is the same — phase/frequency data, averaging factors, the Allan/Hadamard/Total families, χ² confidence intervals, lag-1 noise identification — only the interface changes from a desktop GUI to a Julia API you can script and embed.\n\nSigmaTau is numerically cross-validated against Stable32 (see Validation); for the same input and the same τ grid you should get the same σ to within the documented tolerance.","title":"Migrating from Stable32"},{"category":"section","location":"migration_from_stable32/#Deviation-name-mapping","page":"From Stable32","text":"Stable32's run-types map to SigmaTau functions one-to-one:\n\nStable32 run / column SigmaTau function\nAllan (ADEV) adev\nOverlapping Allan (OADEV) adev (always overlapping)\nModified Allan (MDEV) mdev\nTime (TDEV) tdev\nHadamard (HDEV) hdev\nOverlapping Hadamard (OHDEV) hdev (always overlapping)\nTotal (TOTDEV) totdev\nModified Total (MTOT) mtotdev\nTime Total (TTOT) ttotdev\nHadamard Total (HTOT) htotdev\nMTIE mtie\nThêo1 / ThêoBR theo1 (correct_bias=false is the raw Thêo1 run; the default correct_bias=true is ThêoBR)\nThêoH theoh\n\nSigmaTau's Allan and Hadamard deviations are always the overlapping estimators (the Stable32 \"O…\" variants), which is the standard choice for stability analysis. There is no separate non-overlapping call.\n\nFour SigmaTau deviations have no Stable32 run type:\n\nmhdev — the modified Hadamard deviation (phase-averaged third difference); see Theory: Allan family.\nhtdev — the Hadamard time deviation, the σ_x form of mhdev; original to this package. Same theory page.\nmhtotdev — the modified Hadamard total deviation, the boundary-extended form of mhdev; original to this package, with Monte-Carlo-calibrated bias and EDF (Theory: MHTOTDEV bias and EDF).\npdev — the parabolic deviation (PVAR); see Theory: Allan family.","title":"Deviation name mapping"},{"category":"section","location":"migration_from_stable32/#Result-field-mapping","page":"From Stable32","text":"A single call returns a StabilityResult. Its fields line up with the columns Stable32 prints in a deviation run:\n\nStable32 column StabilityResult field\nTau r.tau (seconds)\nSigma / Dev r.dev\nMin Sigma / Max Sigma (CI) r.ci — one (lo, hi) tuple per τ (r.ci[1].lo); ci_lower(r) / ci_upper(r) give plain vectors\n# (analysis points) r.neff\nEDF r.edf\nNoise type (α) r.noise_type\n\nNoise-type symbols follow the SP1065 power-law labels: :WHPM, :FLPM, :WHFM, :FLFM, :RWFM (α = +2, +1, 0, −1, −2). The confidence and EDF fields are populated when ci=true (the default) and left empty when ci=false; r.neff is populated either way.","title":"Result-field mapping"},{"category":"section","location":"migration_from_stable32/#A-complete-session","page":"From Stable32","text":"","title":"A complete session"},{"category":"section","location":"migration_from_stable32/#1.-Load-a-data-file","page":"From Stable32","text":"Stable32 reads a column of phase (or frequency) values. So does SigmaTau, via read_phase / read_frequency. A typical .DAT file is a single column of phase values; give the sample interval τ₀ explicitly:\n\nusing SigmaTau\n\n# Single column of phase residuals (seconds), τ₀ = 1 s.\n# time_col=0 says \"no time column\"; value_col=1 reads the lone column.\np = read_phase(\"clock.DAT\"; time_col=0, value_col=1, tau0=1.0)\n\nIf the file has a time column and a value column (the common two-column layout), point at the value column — this is the default, so tau0 is inferred from the time column if you omit it:\n\np = read_phase(\"clock.DAT\"; time_col=1, value_col=2)\n\nread_frequency is identical but returns a FrequencyData. Both accept scaling (multiply the values, e.g. to convert units), detrend (:none, :mean, :endpoint, :linear), and fillgaps=true for the Howe–Schlossberger gap imputation — the preprocessing you'd otherwise reach for in Stable32's data menu, done in the same call.\n\nYou can also build data in memory without a file:\n\np = PhaseData(phase_vector, 1.0)        # τ₀ = 1 s\nf = FrequencyData(freq_vector, 1.0)","title":"1. Load a data file"},{"category":"section","location":"migration_from_stable32/#2.-Run-a-deviation","page":"From Stable32","text":"A Stable32 \"Run\" on a single deviation is a single function call. Pass the averaging factors m (τ = m·τ₀):\n\nr = adev(p, [1, 2, 4, 8, 16, 32, 64])\n\nOr let SigmaTau pick the grid for you. The default is an octave-spaced grid (the Stable32 default); other spacings are available through the TauMode selector:\n\nr = adev(p)                 # octave-spaced, like Stable32's default\nr = adev(p, AllTaus)        # every averaging factor\nr = adev(p, Decade)         # decade-spaced\n\nConfidence intervals are on by default (ci=true) and use the Greenhall–Riley EDF with a χ² mapping — the same machinery behind Stable32's error bars. Turn them off for a faster bare-σ run with ci=false.","title":"2. Run a deviation"},{"category":"section","location":"migration_from_stable32/#3.-Run-a-whole-suite-at-once","page":"From Stable32","text":"Stable32 runs one deviation per pass. SigmaTau can compute several in one call with stability, returning a StabilitySuite you index by deviation symbol:\n\nsuite = stability(p; devs=(:adev, :mdev, :hdev, :tdev))\n\nsuite[:adev].dev      # the ADEV curve\nsuite[:hdev].tau      # its τ grid\nkeys(suite)           # which deviations are present, in order\n\ndevs defaults to (:adev, :mdev, :hdev, :tdev). Detrend and bias options passed to stability are forwarded only to the total family.","title":"3. Run a whole suite at once"},{"category":"section","location":"migration_from_stable32/#4.-Save-and-plot","page":"From Stable32","text":"Where Stable32 writes a results table and a plot, SigmaTau round-trips results to a self-describing text file and plots through any Plots-compatible backend:\n\nusing Plots\n\nsave_result(\"adev.tsv\", r)          # single result\nsuite_path = save_suite(\"run.tsv\", suite)   # a whole suite, with session metadata\n\nplot(r)                             # log-log τ–σ with CI error bars\nplot(suite)                         # overlay every deviation in the suite\n\nload_result / load_suite read them back. The suite file also records the package version, a timestamp, τ₀, N, the confidence level, and the τ-mode, so a saved run is self-documenting.","title":"4. Save and plot"},{"category":"section","location":"migration_from_stable32/#Quick-API-reference","page":"From Stable32","text":"Task Stable32 SigmaTau\nLoad phase file File → Open read_phase(path; tau0=…)\nLoad frequency file File → Open read_frequency(path; tau0=…)\nDetrend / remove slope Edit menu detrend(data; method=:linear) or detrend= kwarg on read\nFill gaps Edit menu fillgaps(data) or fillgaps=true\nOverlapping Allan OADEV run adev(data, m)\nConfidence intervals Sigma error bars ci=true (default)\nNoise identification automatic r.noise_type (per τ)\nRun several deviations one pass each stability(data; devs=…)\nExport results table Save results save_result / save_suite\nPlot Plot window plot(r) / plot(suite)","title":"Quick API reference"},{"category":"section","location":"migration_from_stable32/#See-also","page":"From Stable32","text":"Getting Started — installation and a first run.\nTutorial 0: Julia for metrologists — a longer walkthrough aimed at Stable32 users new to Julia.\nPerformance — how the runtime compares to allantools.","title":"See also"},{"category":"section","location":"plotting/#Plotting","page":"Plotting","text":"SigmaTau ships its plot recipes in a package extension (ext/SigmaTauRecipesBaseExt.jl). The extension loads automatically when RecipesBase is in the session — in practice, the moment you load Plots (or any other package that loads RecipesBase; a backend package alone, such as GR, does not). SigmaTau itself takes no plotting dependency, so headless batch analysis never pays for a graphics stack.\n\nThis page is a cookbook: each section is a recipe you can paste into the REPL.","title":"Plotting"},{"category":"section","location":"plotting/#Setup","page":"Plotting","text":"using Plots, SigmaTau\n\nPlots defaults to the GR backend, which needs no extra installation. The recipes are backend-agnostic; the figures on this page are rendered through the docs build's backend (PGFPlotsX when a LaTeX engine is available, GR otherwise).","title":"Setup"},{"category":"section","location":"plotting/#A-single-result","page":"Plotting","text":"plot(result) on a StabilityResult draws the σ–τ curve as a line on log-log axes:\n\nusing Plots, SigmaTau, Random\nRandom.seed!(42)\n\n# Synthetic record: WFM at σ_y(1 s) = 1e-12 plus RWFM at 3e-14,\n# 8192 phase samples at τ₀ = 1 s — a fall-then-rise curve.\np = noise_gen(PhaseData, 8192, 1.0; sigma1 = Dict(0 => 1e-12, -2 => 3e-14))\nr = adev(p)\n\nplot(r)\n\nThe recipe sets, as overridable defaults:\n\nxscale = :log10, yscale = :log10,\nxlabel = \"Averaging Time τ (s)\",\nylabel and legend label equal to the deviation name (\"ADEV\" here, taken from result.deviation_type),\ncircle markers at each computed τ (markershape = :circle, markersize = 3),\nticks at integer powers of 10 spanning the data range on both axes, with major gridlines at the decades and fainter minor gridlines between (minorgrid = true). Computing the tick positions from the data keeps a narrow-range axis on integer-decade labels instead of fractional powers like 10^2.5.\n\nWhen the result carries confidence bounds (ci=true, the default), the recipe attaches asymmetric error bars: the lower offset is dev .- ci_lower(r) and the upper offset is ci_upper(r) .- dev, so each bar spans exactly [r.ci[i].lo, r.ci[i].hi]. The bars are asymmetric because the χ² confidence interval is asymmetric about the point estimate.\n\nEvery default yields to a standard Plots attribute passed at the call site — marker = :square changes the marker, marker = :none removes it, xticks = :auto restores backend tick selection. The one exception is the series type, which the recipe fixes to a line path — restyle with marker and line attributes rather than seriestype = :scatter:\n\nplot(r; lw = 1.5, marker = :diamond, ms = 4,\n     title = \"Overlapping Allan deviation\", legend = :bottomleft)","title":"A single result"},{"category":"section","location":"plotting/#Confidence-interval-styles","page":"Plotting","text":"The recipe supports one custom attribute, ci_band:\n\nCall CI rendering\nplot(r) error bars (default)\nplot(r; ci_band = true) filled band (ribbon)\nplot(adev(p; ci = false)) none — the ci vector is empty\n\nplot(r; ci_band = true, fillalpha = 0.3)\n\nci_band is consumed by the recipe, so it never reaches the backend as an unknown attribute. The band edges are ci_lower(r) and ci_upper(r), the same bounds the error bars span; only the rendering changes.\n\nThe result stores the bounds as one (lo, hi) tuple per τ in r.ci; the ci_lower / ci_upper accessor functions return them as plain vectors, so you can also draw them yourself when you want full control over the style:\n\nplot(r.tau, r.dev;\n     xscale = :log10, yscale = :log10,\n     xlabel = \"Averaging Time τ (s)\", label = \"ADEV\")\nplot!(r.tau, ci_lower(r); ls = :dot, color = :gray, label = \"68.3 % CI\")\nplot!(r.tau, ci_upper(r); ls = :dot, color = :gray, label = \"\")\n\nThe default confidence level is DEFAULT_CONFIDENCE = 0.683 (1σ); pass confidence = 0.95 to the deviation call to widen the bounds. mtie has no published EDF model, so its results carry no CI and always plot as a bare curve.","title":"Confidence-interval styles"},{"category":"section","location":"plotting/#Overlaying-several-deviations","page":"Plotting","text":"plot(suite) on a StabilitySuite overlays every result on one set of axes, one labelled series per deviation, with the generic y-label \"Deviation\":\n\nsuite = stability(p)          # :adev, :mdev, :hdev, :mhdev by default\nplot(suite; legend = :bottomleft)\n\nThe default suite is all σy quantities (dimensionless fractional-frequency deviations), so the shared ordinate is unit-consistent. Time deviations such as tdev and htdev are σx quantities in seconds — request them explicitly and plot them on their own axes, e.g. plot(tdev(p)).\n\nA Vector{StabilityResult} overlays the same way — useful for comparing clocks rather than deviations: plot([adev(p1), adev(p2)]). Both series then inherit the same default label (\"ADEV\"), so for clock comparisons the manual form with explicit labels is usually clearer:\n\np2 = noise_gen(PhaseData, 8192, 1.0; sigma1 = Dict(0 => 3e-12))\n\nplot(adev(p);   label = \"clock A\", legend = :bottomleft)\nplot!(adev(p2); label = \"clock B\")\n\nEvery plot! call adds to the existing figure; the user-supplied label overrides the recipe's default.","title":"Overlaying several deviations"},{"category":"section","location":"plotting/#Log-log-conventions-and-slope-guides","page":"Plotting","text":"Because the log scales and the integer-decade ticks are recipe defaults, the usual Plots attributes restyle the axes. An explicit xticks vector replaces the default tick positions — here, labelling every other decade:\n\nplot(r; xticks = 10.0 .^ (0:2:4),\n     xlabel = raw\"\\(\\tau\\) (s)\", ylabel = raw\"\\(\\sigma_y(\\tau)\\)\",\n     legend = :bottomleft)\n\nOn log-log axes the slope of σy(τ) encodes the dominant noise type (Riley, Jul 2008); α is the spectral exponent of the fractional-frequency PSD, `Sy(f) ∝ f^α`:\n\nNoise type α ADEV slope\nWhite PM (:WHPM) 2 τ⁻¹\nFlicker PM (:FLPM) 1 ≈ τ⁻¹\nWhite FM (:WHFM) 0 τ^(−1/2)\nFlicker FM (:FLFM) −1 τ⁰\nRandom-walk FM (:RWFM) −2 τ^(+1/2)\n\nADEV cannot separate white PM from flicker PM — both fall as roughly τ⁻¹ — which is one reason mdev (slope τ^(−3/2) under white PM) is computed alongside it.\n\nA reference slope line is an ordinary series anchored at a point on the curve. For the τ^(−1/2) guide of white FM, anchored at the first point:\n\nguide = r.dev[1] .* (r.tau ./ r.tau[1]) .^ (-1/2)\n\nplot(r; legend = :bottomleft)\nplot!(r.tau, guide; ls = :dash, color = :gray,\n      label = raw\"\\(\\tau^{-1/2}\\) (WHFM)\")\n\nThe synthetic record above follows the guide at short τ (the white-FM regime) and peels upward at long τ where the random-walk FM component takes over.","title":"Log-log conventions and slope guides"},{"category":"section","location":"plotting/#Spectral-densities","page":"Plotting","text":"The same extension plots SpectralResults from Sy, Sx, and L. S_y(f) and S_x(f) are power densities and render on log-log axes with the units in the y-label; ℒ(f) is already in dBc/Hz, so it gets a log frequency axis with a linear (dB) ordinate. The recipe drops the DC bin, which is undefined on a log frequency axis:\n\nplot(Sy(p))\n\nL needs the carrier frequency to convert from Sx: pass `fcarrier(in Hz) to theLcall itself, e.g.plot(L(p; f_carrier = 10e6))`.","title":"Spectral densities"},{"category":"section","location":"plotting/#Publication-export","page":"Plotting","text":"size (pixels) and dpi are standard Plots attributes; savefig picks the format from the file extension. dpi affects only raster formats such as PNG — PDF and SVG are vector formats and ignore it:\n\nfig = plot(r; ci_band = true, size = (800, 500), dpi = 300,\n           lw = 1.5, legend = :bottomleft)\n\nsavefig(fig, \"adev.png\")   # raster, 300 dpi\nsavefig(fig, \"adev.pdf\")   # vector\n\nFor LaTeX documents, switch the backend to PGFPlotsX (Plots.pgfplotsx(), requires a TeX engine) and the figure fonts will match the surrounding text — this is what the docs build itself does.","title":"Publication export"},{"category":"section","location":"plotting/#Where-to-next","page":"Plotting","text":"Tutorial 2: Computing the Allan deviation — the recipe applied to a worked WPM + RWFM record.\nTutorial 6: Three-cornered hat — a fully manual log-log plot for derived (non-StabilityResult) quantities.\nTheory: Overview — what the slopes mean physically.","title":"Where to next"},{"category":"section","location":"bibliography/#Bibliography","page":"Bibliography","text":"PDFs are not bundled with the source distribution; see DOI/URL per entry or contact the author.\n\nAllan, D. W.; Ashby, N. and Hodge, C. C. (1997). The Science of Timekeeping. Hewlett-Packard Application Note 1289.\n\n\n\nBanerjee, P. and Matsakis, D. (2023). An Introduction to Modern Timekeeping and Time Transfer. Springer Series in Measurement Science and Technology (Springer Nature Switzerland), eBook ISBN 978-3-031-30780-5.\n\n\n\nGreenhall, C. A. (1997). The Third-Difference Approach to Modified Allan Variance. IEEE Transactions on Instrumentation and Measurement 46, 696–703.\n\n\n\nGreenhall, C. A.; Howe, D. A. and Percival, D. B. (1999). Total Variance, an Estimator of Long-Term Frequency Stability. IEEE Transactions on Ultrasonics, Ferroelectrics, and Frequency Control 46, 1183–1191.\n\n\n\nGreenhall, C. A. and Riley, W. J. (2003). Uncertainty of Stability Variances Based on Finite Differences. In: Proceedings of the 35th Annual Precise Time and Time Interval (PTTI) Meeting; pp. 267–280.\n\n\n\nHowe, D. A.; Beard, R.; Greenhall, C. A.; Vernotte, F. and Riley, W. J. (Nov 2000). A Total Estimator of the Hadamard Function Used for GPS Operations. In: Proceedings of the 32nd Annual Precise Time and Time Interval (PTTI) Meeting (Reston, VA); pp. 255–268.\n\n\n\nHowe, D. A.; Beard, R.; Greenhall, C. A.; Vernotte, F. and Riley, W. J. (2001). Total Hadamard Variance: Application to Clock Steering by Kalman Filtering. In: Proceedings of the IEEE International Frequency Control Symposium.\n\n\n\nHowe, D. A.; Beard, R. L.; Greenhall, C. A.; Vernotte, F.; Riley, W. J. and Peppler, T. K. (2005). Enhancements to GPS Operations and Clock Evaluations Using a ``Total'' Hadamard Deviation. IEEE Transactions on Ultrasonics, Ferroelectrics, and Frequency Control 52, 1253–1261.\n\n\n\nHowe, D. A. and Vernotte, F. (Dec 1999). Generalization of the Total Variance Approach to the Modified Allan Variance. In: Proceedings of the 31st Annual Precise Time and Time Interval (PTTI) Meeting (Dana Point, CA); pp. 267–276.\n\n\n\nMcKelvy, J. A.; Shin, G.; McDaniel, F.; Owings, B. and Bandi, T. N. (2025). A Machine Learning Approach for Telemetry-Based Frequency Stability Estimation of High Precision Clocks. IEEE Open Journal of Ultrasonics, Ferroelectrics, and Frequency Control. Author version, accepted for publication.\n\n\n\nRiley, W. J. (Jul 2008). Handbook of Frequency Stability Analysis. NIST Special Publication 1065 (National Institute of Standards and Technology, Boulder, CO). Time and Frequency Division, prepared under contract for NIST.\n\n\n\nRiley, W. J. (Jun 2020). Frequency Stability Analysis Using R. Hamilton Technical Services technical note, Rev. C, Beaufort, SC. Dated June 4, 2020.\n\n\n\nRiley, W. J. and Greenhall, C. A. (Apr 2004). Power Law Noise Identification Using the Lag 1 Autocorrelation. In: Proceedings of the 18th European Frequency and Time Forum (EFTF) (Guildford, UK).\n\n\n\nSullivan, D. B.; Allan, D. W.; Howe, D. A. and Walls, F. L. (Mar 1990). Characterization of Clocks and Oscillators. NIST Technical Note 1337 (National Institute of Standards and Technology, Boulder, CO); p. 352. CODEN: NTNOEF; replaces methods/definitions sections of NBS Monograph 140 (Blair 1974).\n\n\n\nVernotte, F.; Chen, S. and Rubiola, E. (2020). Response and Uncertainty of the Parabolic Variance PVAR to Non-Integer Exponents of the Power Law, arXiv:2005.13631 [physics.data-an], arXiv:2005.13631.\n\n\n\nVernotte, F.; Lenczner, M.; Bourgeois, P.-Y. and Rubiola, E. (2016). The Parabolic Variance (PVAR): A Wavelet Variance Based on the Least-Square Fit. IEEE Transactions on Ultrasonics, Ferroelectrics, and Frequency Control 63, 611–623, arXiv:1506.00687.\n\n\n\nIEEE Ultrasonics, F. and Society, F. C. (2022). IEEE Standard Definitions of Physical Quantities for Fundamental Frequency and Time Metrology—Random Instabilities. IEEE Std 1139-2022 (Revision of IEEE Std 1139-2008). Approved 16 June 2022; published 8 December 2022.\n\n\n\n","title":"Bibliography"}]
}
