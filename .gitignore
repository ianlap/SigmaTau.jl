# macOS
.DS_Store

# Julia
Manifest.toml
*.cov
*.jl.cov
*.jl.mem
*.log
*_output.log
deps/deps.jl
deps/build.log
docs/build/
docs/site/
LocalPreferences.toml
/.julia/

# Editors / IDEs
.vscode/
.idea/
*.swp
*.swo
*~

# Local agent / tool config
.claude/
.aider*
.gemini/
# Agent-context briefs — local-only, never publish to the public repo
CLAUDE.md
AGENTS.md

# Environment
.env
.envrc

# Internal planning / migration artifacts (not for public repo)
legacy/
legdocs/

# Throwaway diagnostic plot scripts + their generated PNGs.
# `benchmarks/bench/` is real perf-measurement infrastructure and stays tracked.
benchmarks/plots/

# Proprietary / large clock recordings used for off-repo validation
reference/clock_data/

# Scratchpad files (Ian's REPL throwaways, never checked in)
scratch.jl
scratch.py
