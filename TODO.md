# SigmaTau.jl — Roadmap

Working list of outstanding engineering work, sorted by priority. Items move
from this file to `CHANGELOG.md` once landed.

> **Audit date**: 2026-05-07. Source-truth audit performed against the actual
> code (not stale logs). Several items previously listed as "blocking" in the
> superseded `next_step_prompts.md` were already implemented by a prior agent
> session; those are noted under "✅ Recently completed (verify)".

---

## 🔴 Verify before next push

These appear complete in the source tree but were not verified by a green test
run before the prior session was interrupted. Re-run tests to confirm.

- [ ] **Kalman filter parity (`SigmaTauEnsemble`)** — `legacy_compat` kwarg,
  `safe_sqrt_sq`, and `clamp_covariance_diag` are wired into
  [`filters.jl`](lib/SigmaTauEnsemble/src/estimators/filters.jl). The stale
  [`test_output.log`](lib/SigmaTauEnsemble/test_output.log) (gitignored) shows
  failures from before the clamp was hooked up — `mtime(filters.jl)` is ~1h
  newer than `mtime(test_output.log)`. **Action**: run
  `julia --project=lib/SigmaTauEnsemble -e 'using Pkg; Pkg.test()'` and confirm
  4/4 parity tests pass. If they still fail, re-open as a debugging task.
- [ ] **Workspace package resolution** — root [`Project.toml`](Project.toml)
  has `[workspace]`, `[deps]`, and `[sources.*]` for all three subpackages;
  each subpackage's `Project.toml` declares relative `[sources]` for its local
  deps. **Action**: `julia --project=. -e 'using Pkg; Pkg.resolve()'` from the
  repo root and `using SigmaTau` in a fresh REPL.

---

## 🔴 Critical (correctness)

- [ ] **Numerical parity tests for stability deviations** _(was PROMPT 3)_.
  Current [`runtests.jl`](lib/SigmaTauStability/test/runtests.jl) only checks
  `isfinite` and shape — would pass even with order-of-magnitude wrong values.
  Generate reference outputs from `legacy/julia/src/deviations/*` on seeded
  power-law fixtures, hardcode them in the test file, compare at `rtol=1e-12`
  for at least 5 m-values × 9 kernels. (Reference data must be generated by
  running the legacy code; agent cannot fabricate values.)

---

## 🟡 High (completeness)

---

## 🟡 Medium (correctness gaps)

- [ ] **MTOTDEV multi-noise validation** _(was PROMPT 5)_. Verify
  `_mtotdev_core` and the `:mtot` bias correction match legacy across all 5
  noise types (WPM, FLPM, WHFM, FLFM, RWFM) at multiple m-values. Reference
  generators live in `legacy/julia/src/noise_gen.jl`.
- [ ] **`_coeff_totvar` α=2,1 entries** _(was PROMPT 7)_. Currently
  [returns `(NaN, NaN)`](lib/SigmaTauStability/src/stats/edf.jl#L114) for
  WPM/FLPM, so `calculate_edf(:totdev, ...)` produces NaN under those noise
  types. **Note**: the legacy implementation has the same gap, so this
  requires reading SP1065 / Greenhall–Riley 2003 — not a copy job. Same
  question applies to `_coeff_htot` for α=2,1.

---

## 🟢 Low (polish)

- [ ] **`RelativisticClock`** — empty struct in
  [`clocks.jl`](lib/SigmaTauEnsemble/src/models/clocks.jl#L20). Lunar PNT
  future work; no concrete need yet.
- [ ] **`UDFactorizedFilter`, `KuramotoOscillator`** — empty structs in
  [`filters.jl`](lib/SigmaTauEnsemble/src/estimators/filters.jl). Stubs
  reserved for low-observability lunar-distance and SWaP-constrained
  nearest-neighbor estimators respectively.
- [ ] **PID steering port** — legacy `kalman_filter` includes a PID
  controller; the new `predict!`/`update!` loop is steering-free. Required
  before clock-steering examples can be ported.
- [ ] **`examples/`** — directory exists but is empty.
- [ ] **CI** — no GitHub Actions / workflow files yet.

---

## 🟢 Documentation

- [ ] Author `README.md` (project intro, install, quickstart, link to docs).
- [ ] Maintain `CHANGELOG.md` (Keep-a-Changelog format).
- [ ] Author user-facing docs (Documenter.jl) in `docs/`.

---

## ✅ Recently completed (since 2026-05-07 session start)

See [CHANGELOG.md](CHANGELOG.md) once authored. In-flight items are tracked in
this file; once a checkbox here gets ticked and the change is committed, move
the entry to the changelog and delete it from here.
